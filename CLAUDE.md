- lembre que é possivel usar o mcp sem ter que o configurar com a ANTHROPIC_API_KEY, exemplo "sequential-thinking": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-sequential-thinking"
      ]
    },
    "memory": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-memory"
      ]
    },
- lembre que quero que um texto NORMAL relacionado a concurso publico seja transformado em questoes para que eu possa aprender estudando por questoes.
- quando o ankinator for utilizado, ele nao deve buscar APENAS questoes já prontas, ele deve, alem de aproveitar as questoes prontas, ele deve criar questoes relacionadas ao conteudo do texto em pdf
- use sempre mcp sequential thinking.