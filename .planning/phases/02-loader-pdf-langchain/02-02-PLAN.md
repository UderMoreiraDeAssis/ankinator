---
phase: 02-loader-pdf-langchain
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/odl_langchain_loader.py
  - tools/requirements.txt
autonomous: true
requirements: [PDF-01]
user_setup:
  - service: "python-venv-langchain"
    why: "Sidecar opcional do loader LangChain (Phase 2) — opt-in; o fluxo Node default roda SEM isto"
    env_vars:
      - name: ANKINATOR_LANGCHAIN_PYTHON
        source: "Caminho do interpretador do venv onde langchain-opendataloader-pdf==2.0.0 foi instalado (fallback: ODL_PYTHON)"
    dashboard_config:
      - task: "Criar venv dedicado, pip install -r tools/requirements.txt, e garantir Java 11+ (Adoptium) no PATH"
        location: "Terminal local (não há dashboard; é instalação local)"
must_haves:
  truths:
    - "tools/odl_langchain_loader.py aceita argv <pdf> [--pages] [--password] (espelha LoadOptions)"
    - "o script instancia OpenDataLoaderPDFLoader(format='markdown', split_pages=True) e imprime JSON [{page_content, metadata}] no stdout (sem temp dir)"
    - "tools/requirements.txt pina langchain-opendataloader-pdf==2.0.0 e documenta Java 11+"
  artifacts:
    - path: "tools/odl_langchain_loader.py"
      provides: "Sidecar Python PDF→Document[] markdown→JSON stdout (PDF-01)"
      contains: "OpenDataLoaderPDFLoader"
    - path: "tools/requirements.txt"
      provides: "Pin de dependência Python (D-13)"
      contains: "langchain-opendataloader-pdf==2.0.0"
  key_links:
    - from: "tools/odl_langchain_loader.py"
      to: "stdout"
      via: "json.dump([{page_content, metadata}], sys.stdout)"
      pattern: "json\\.dump.*sys\\.stdout"
---

<objective>
Criar o sidecar Python `tools/odl_langchain_loader.py` que extrai um PDF em `Document[]` por página em markdown (via `langchain-opendataloader-pdf`) e imprime um array JSON `[{page_content, metadata}]` no stdout (D-01, SEM temp dir), espelhando `LoadOptions` no CLI (`<pdf> [--pages] [--password]`, D-11) e configurando modo markdown-por-página (D-12). Mais `tools/requirements.txt` com o pin `==2.0.0` (D-13) e a nota de Java 11+.

Purpose: Entregar PDF-01. É a outra metade independente da Wave 1 (sem overlap de arquivos com o núcleo `normalize` do Plano 01). O contrato stdout é consumido pelo `langchain-loader.ts` (Wave 2).
Output: `tools/odl_langchain_loader.py`, `tools/requirements.txt`. O `pip install` num venv é setup OPCIONAL do usuário (ver frontmatter `user_setup`) — o loader é opt-in e o caminho Node default roda SEM ele. Legitimidade do pacote já CONFIRMADA por verificação adversarial (org `opendataloader-project`, mesmo motor do `@opendataloader/pdf` em produção, v2.0.0 não-yanked) — sem gate humano.

NOTA: `tools/` ainda não existe; o executor cria o diretório. Os arquivos `.py`/`.txt` vivem fora de `src` (não compilados pelo tsc; lidos por path em runtime).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-loader-pdf-langchain/02-CONTEXT.md
@.planning/phases/02-loader-pdf-langchain/02-RESEARCH.md
@.planning/phases/02-loader-pdf-langchain/02-PATTERNS.md

<interfaces>
<!-- Contrato CLI que o sidecar DEVE satisfazer (consumido por langchain-loader.ts no Plano 03). -->

argv esperado (D-11 — espelha LoadOptions de document-loader.ts:20-27):
  <pdf>            (posicional, obrigatório)
  --pages   STR   (opcional, ex.: "1,3,5-7")
  --password STR  (opcional)

stdout esperado (D-01): JSON `[{ "page_content": <str>, "metadata": { "source": <str>, "format": "markdown", "page": <int 1-indexed> } }]`
exit code: 0 em sucesso; != 0 em erro de runtime (o Node THROW/propaga — D-04).

API confirmada (02-RESEARCH §Verificação Adversarial C5 + §Code Examples):
  `from langchain_opendataloader_pdf import OpenDataLoaderPDFLoader`
  `OpenDataLoaderPDFLoader(file_path, format="markdown", split_pages=True, quiet=True, image_output="off", table_method="cluster", reading_order="xycut", pages=…, password=…)`
  `.load()` → 1 `Document`/página; `Document.metadata == {'source','format','page'}` (page 1-indexed).

Paridade com o loader Node (document-loader.ts:49-58 — ConvertOptions): `format` markdown, `tableMethod:'cluster'`, `readingOrder:'xycut'`, `imageOutput:'off'`, `quiet:true`.
</interfaces>

<read_first>
- ankinator-app/server/src/core/ocr-loader.ts (linhas 47-76 — CONTRATO de spawn/argv de `runOcrLoader`; idioma de push `--pages`/`--password` em 62-63 a espelhar no CLI do Python)
- ankinator-app/server/src/core/document-loader.ts (linhas 20-27 `LoadOptions`; linhas 49-58 `ConvertOptions` — os valores que o sidecar deve espelhar)
- 02-RESEARCH.md §"Code Examples" (a implementação de referência completa do sidecar, linhas 295-333) + §"Standard Stack > Installation" (conteúdo canônico do requirements.txt) + Pitfall 2 (Java 11+) + Pitfall 5 (image_output='off')
- 02-RESEARCH.md §"Verificação Adversarial" (C3 Java obrigatório; C4 pin 2.0.0; C5 API)
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar tools/requirements.txt (pin D-13)</name>
  <files>tools/requirements.txt</files>
  <read_first>02-RESEARCH.md §"Standard Stack > Installation" (linhas 105-111 — conteúdo canônico) + Pitfall 2 (Java 11+)</read_first>
  <action>Criar o diretório `tools/` (se ainda não existir) e o arquivo `tools/requirements.txt` com o pin EXATO `langchain-opendataloader-pdf==2.0.0` (D-13; CONFIRMED em 02-RESEARCH §Verificação Adversarial C4). NÃO declarar as deps transitivas (`langchain-core<2.0,>=1.0`, `opendataloader-pdf>=2.0.0`) — vêm automaticamente. Incluir comentários PT-BR documentando: que é sidecar OPCIONAL (não dependência do Node); que requer Python `>=3.10,<4.0` E **Java 11+ no PATH (Adoptium)** — R2: o pacote é casca sobre um CLI Java, e o availability-check por `import` NÃO detecta Java ausente (vira erro de runtime); e que se instale num venv dedicado apontado por `ANKINATOR_LANGCHAIN_PYTHON`.</action>
  <verify>
    <automated>grep -c '^langchain-opendataloader-pdf==2.0.0$' tools/requirements.txt</automated>
  </verify>
  <acceptance_criteria>`grep -c '^langchain-opendataloader-pdf==2.0.0$' tools/requirements.txt` retorna 1; `grep -iv '^#' tools/requirements.txt | grep -c .` retorna 1 (uma única linha não-comentário = o pin); o comentário menciona `Java 11+` (`grep -ci 'java 11' tools/requirements.txt` >= 1).</acceptance_criteria>
  <done>requirements.txt pina 2.0.0, documenta Python>=3.10 + Java 11+, e não declara transitivas.</done>
</task>

<task type="auto">
  <name>Task 2: Criar tools/odl_langchain_loader.py (sidecar PDF-01; D-01/D-11/D-12)</name>
  <files>tools/odl_langchain_loader.py</files>
  <read_first>
    - 02-RESEARCH.md §"Code Examples" (linhas 295-333 — implementação de referência) + Pitfall 5 (`image_output="off"`)
    - ankinator-app/server/src/core/document-loader.ts (linhas 49-58 — ConvertOptions a espelhar) + (linhas 20-27 — LoadOptions = contrato dos flags)
  </read_first>
  <action>Criar `tools/odl_langchain_loader.py` (comentários PT-BR, NÃO usa API key — só processamento local). Usar `argparse` com: posicional `pdf`; `--pages` (default None); `--password` (default None) — D-11 espelha `LoadOptions`. Importar `from langchain_opendataloader_pdf import OpenDataLoaderPDFLoader`. Instanciar `OpenDataLoaderPDFLoader(file_path=args.pdf, format="markdown", split_pages=True, quiet=True, image_output="off", table_method="cluster", reading_order="xycut", pages=args.pages, password=args.password)` — D-12 modo markdown-por-página + paridade com `ConvertOptions` do Node. Chamar `docs = loader.load()`. D-01: montar `out = [{"page_content": d.page_content, "metadata": d.metadata} for d in docs]` e `json.dump(out, sys.stdout, ensure_ascii=False)` — array JSON no STDOUT, SEM temp dir. `main()` retorna 0; guard `if __name__ == "__main__": sys.exit(main())`. NÃO escrever em arquivo, NÃO criar tmpdir, NÃO emitir binário (image_output='off' garante texto — Pitfall 5). Nota inline (A1): os nomes de param `table_method`/`reading_order`/`image_output` devem ser confirmados no smoke do Plano 04 antes de considerar fixos; defaults do pacote já produzem markdown utilizável caso algum nome divirja.</action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('tools/odl_langchain_loader.py').read()); print('sintaxe OK')"</automated>
  </verify>
  <acceptance_criteria>`python3 -c "import ast; ast.parse(...)"` confirma sintaxe Python válida; `grep -c 'OpenDataLoaderPDFLoader' tools/odl_langchain_loader.py` >= 1; `grep -c 'format="markdown"' tools/odl_langchain_loader.py` == 1; `grep -c 'split_pages=True' tools/odl_langchain_loader.py` == 1; `grep -c 'json.dump' tools/odl_langchain_loader.py` >= 1; NÃO há `mkdtemp`/`tempfile`/`open(.*w` (grep retorna 0 — D-01 stdout, sem temp dir). NÃO valida execução (pacote não instalado nesta sessão; cobre no smoke do Plano 04).</acceptance_criteria>
  <done>Sidecar Python existe, sintaticamente válido, com CLI espelhando LoadOptions, modo markdown-por-página, e saída JSON no stdout (sem temp dir).</done>
</task>

<!-- Task 3 (checkpoint humano de legitimidade antes do pip install) REMOVIDO: a legitimidade do pacote
     foi CONFIRMADA por verificação adversarial independente (5 agentes — RESEARCH §Verificação Adversarial C4:
     org oficial `opendataloader-project`, mesmo publisher do `@opendataloader/pdf` npm já em produção,
     v2.0.0 não-yanked, deps exatas via PyPI JSON). O install do pacote é setup OPCIONAL do usuário,
     documentado em `user_setup` no frontmatter — não há gate humano de execução. -->

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Node → Python sidecar (argv) | `pdf`/`--pages`/`--password` passados como argv. No injection: spawn é argv-array (Plano 03), não shell. O sidecar só repassa para o construtor do loader. |
| pip → PyPI (supply chain) | `pip install` baixa o pacote + transitivas de PyPI. Risco de slopsquatting/tampering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | `pip install langchain-opendataloader-pdf` (supply chain) | mitigate | Legitimidade CONFIRMADA por verificação adversarial (5 agentes): org oficial `opendataloader-project`, mesmo publisher do `@opendataloader/pdf` npm já em produção, v2.0.0 não-yanked, deps exatas via PyPI JSON. Pin de versão exata (`==2.0.0`) evita drift silencioso. Install opt-in num venv dedicado (`user_setup`). Sem gate humano (legitimidade resolvida). |
| T-02-03 | Tampering | argv (`--pages`/`--password`) repassados ao construtor Python | mitigate | Spawn argv-array no Plano 03 (sem shell) → sem injection de comando. `argparse` valida shape. `--password` é segredo do usuário; não logar. |
| T-02-04 | Information Disclosure | stdout pode vazar conteúdo do PDF em logs | accept | Conteúdo é do próprio PDF do usuário (local single-user). stdout é consumido por `JSON.parse` no Node; não persistido. LOW para CLI local. |
</threat_model>

<verification>
- `python3 -c "import ast; ast.parse(open('tools/odl_langchain_loader.py').read())"` (sintaxe válida)
- `grep '^langchain-opendataloader-pdf==2.0.0$' tools/requirements.txt` (pin exato)
- Legitimidade do pacote verificada adversarialmente (RESEARCH §Verificação Adversarial C4) — install opt-in em venv (user_setup), sem gate humano bloqueante
</verification>

<success_criteria>
- `tools/odl_langchain_loader.py` aceita `<pdf> [--pages] [--password]`, configura markdown-por-página, e imprime JSON `[{page_content, metadata}]` no stdout (PDF-01; D-01/D-11/D-12).
- `tools/requirements.txt` pina `==2.0.0` e documenta Python>=3.10 + Java 11+ (D-13).
- Legitimidade do pacote verificada adversarialmente (org `opendataloader-project`, v2.0.0 PyPI); install é opt-in (user_setup), sem gate humano bloqueante.
</success_criteria>

<output>
Create `.planning/phases/02-loader-pdf-langchain/02-02-SUMMARY.md` when done
</output>
