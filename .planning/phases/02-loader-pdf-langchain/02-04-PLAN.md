---
phase: 02-loader-pdf-langchain
plan: 04
type: execute
wave: 3
depends_on: [02-03]
files_modified:
  - ankinator-app/server/src/core/document-loader.ts
  - ankinator-app/server/src/scripts/guard-default-loader.ts
  - ankinator-app/server/src/scripts/smoke-langchain-loader.ts
autonomous: true
requirements: [PDF-02, PDF-03]
must_haves:
  truths:
    - "com ANKINATOR_PDF_LOADER=langchain e pacote disponível, loadDocument usa o caminho langchain e loga '[ankinator] loader: langchain'"
    - "com langchain selecionado mas indisponível, loadDocument cai (fallback silencioso) para o caminho Node e loga '(fallback→node)'"
    - "com opts.ocr=true E loader=langchain, o OCR vence (D-06)"
    - "com ANKINATOR_PDF_LOADER unset, loadDocument usa o caminho @opendataloader/pdf de hoje byte-idêntico, SEM Python/Java (D-15)"
    - "o guard D-15 prova o ConvertOptions/plano default byte-idêntico CLI-free (sem spawnar Java/Python)"
  artifacts:
    - path: "ankinator-app/server/src/core/document-loader.ts"
      provides: "branch D-03/D-06 + helper puro buildConvertOptions (alvo do guard D-15)"
      contains: "ANKINATOR_PDF_LOADER"
    - path: "ankinator-app/server/src/scripts/guard-default-loader.ts"
      provides: "Guard CLI-free de não-regressão do default (D-15)"
      contains: "process.exit"
    - path: "ankinator-app/server/src/scripts/smoke-langchain-loader.ts"
      provides: "Smoke gated em disponibilidade (D-14)"
      contains: "isLangchainAvailable"
  key_links:
    - from: "ankinator-app/server/src/core/document-loader.ts"
      to: "ankinator-app/server/src/core/langchain-loader.ts"
      via: "import { runLangchainLoader, isLangchainAvailable }"
      pattern: "import.*runLangchainLoader.*langchain-loader"
    - from: "ankinator-app/server/src/scripts/guard-default-loader.ts"
      to: "ankinator-app/server/src/core/document-loader.ts"
      via: "import { buildConvertOptions } (helper puro)"
      pattern: "import.*buildConvertOptions"
---

<objective>
Fazer o wiring final de PDF-02 e provar a não-regressão de PDF-03 (D-15). Três peças:
1. O branch ADITIVO em `loadDocument` (D-03): após o branch OCR (D-06: OCR vence), se `ANKINATOR_PDF_LOADER === 'langchain'` e `isLangchainAvailable()`, usa `runLangchainLoader` (log D-05); senão fallback silencioso para o default. O default (env unset) NÃO entra no branch (D-15).
2. Extrair o build do `ConvertOptions` num helper PURO `buildConvertOptions(opts)` para que o guard D-15 possa assertá-lo CLI-free (espelha como `buildSpawnArgs` foi extraído na Phase 1).
3. O guard `guard-default-loader.ts` (D-15, espelha `smoke-runner --assert-args`): prova que com env unset o `ConvertOptions` é byte-idêntico ao de hoje, SEM spawnar Java/Python e SEM chamar `isLangchainAvailable()`. Mais o `smoke-langchain-loader.ts` gated (D-14).

Purpose: Fecha a fase. É a regressão mais perigosa (o fluxo de produção roda SEM Python — Pitfall 3). Wave 3 — depende do loader (Plano 03).
Output: `document-loader.ts` (branch + helper exportado), `guard-default-loader.ts` (CLI-free), `smoke-langchain-loader.ts` (gated).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-loader-pdf-langchain/02-CONTEXT.md
@.planning/phases/02-loader-pdf-langchain/02-RESEARCH.md
@.planning/phases/02-loader-pdf-langchain/02-PATTERNS.md

<interfaces>
<!-- Estado ATUAL de loadDocument (alvo da modificação + guard) e o loader (Plano 03). -->

De ankinator-app/server/src/core/document-loader.ts (estado atual — a estender):
```typescript
export interface LoadOptions { ocr?: boolean; pages?: string; password?: string; }

export async function loadDocument(pdfPath: string, opts: LoadOptions = {}): Promise<LoadedDocument> {
  if (opts.ocr) {                                   // D-06: OCR (mais específico) vence — INTOCADO
    if (!(await isOcrAvailable())) { throw new Error('OCR solicitado, mas …'); }
    return runOcrLoader(pdfPath, opts);
  }
  const outDir = await fs.mkdtemp(path.join(os.tmpdir(), 'ankinator-odl-'));
  try {
    const options: ConvertOptions = {              // ← EXTRAIR para buildConvertOptions(opts) (puro)
      outputDir: outDir,
      format: ['json', 'markdown'],
      tableMethod: 'cluster',
      readingOrder: 'xycut',
      imageOutput: 'off',
      quiet: true,
      ...(opts.pages ? { pages: opts.pages } : {}),
      ...(opts.password ? { password: opts.password } : {}),
    };
    await convert([pdfPath], options);
    return await parseOdlOutput(outDir, pdfPath);
  } finally { fs.rm(outDir, { recursive: true, force: true }).catch(() => {}); }
}
```

De ankinator-app/server/src/core/langchain-loader.ts (Plano 03):
```typescript
export async function isLangchainAvailable(): Promise<boolean>;
export async function runLangchainLoader(pdfPath: string, opts: LoadOptions): Promise<LoadedDocument>;
```

Idioma de guard CLI-free (smoke-runner.ts — Phase 1):
```typescript
function fail(msg: string): never { console.error('   ✗ FALHA: ' + msg); process.exit(1); }
// compare length-first, depois element-by-element; --assert-args early-exit em process.exit(0)
```
</interfaces>

<read_first>
- ankinator-app/server/src/core/document-loader.ts (INTEIRO — 64 linhas; OCR branch 34-42 INTOCADO; `ConvertOptions` 49-58 a extrair; `convert`+`parseOdlOutput` 59-60; `finally` 61-63; import idioma linha 18)
- ankinator-app/server/src/core/langchain-loader.ts (Plano 03 — `isLangchainAvailable`/`runLangchainLoader`)
- ankinator-app/server/src/scripts/smoke-runner.ts (INTEIRO — 85 linhas; `fail()` 51-54, compare 56-72, `--assert-args` early-exit 75-78 — o template do guard)
- ankinator-app/server/src/scripts/smoke-loader.ts (INTEIRO — 30 linhas; harness a clonar para o smoke)
- 02-RESEARCH.md Pitfall 3 (D-15 branch não pode desviar o default) + §"Architecture Patterns > Pattern 2"
- 02-PATTERNS.md §"document-loader.ts" (branch aditivo + guard) + §"<assert-normalize>/<guard-default>" (discrição: extrair helper de ConvertOptions p/ o guard ser CLI-free)
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extrair buildConvertOptions + adicionar branch langchain em document-loader.ts (D-03/D-06/D-05; D-15 prep)</name>
  <files>ankinator-app/server/src/core/document-loader.ts</files>
  <read_first>ankinator-app/server/src/core/document-loader.ts (linhas 12-64 INTEIRAS) + 02-RESEARCH Pitfall 3 + Pattern 2</read_first>
  <action>Duas mudanças, ambas preservando o caminho default byte-idêntico (D-15):
  (A) EXTRAIR helper PURO exportado `export function buildConvertOptions(outDir: string, opts: LoadOptions): ConvertOptions` que retorna EXATAMENTE o objeto de hoje (linhas 49-58): `{ outputDir: outDir, format: ['json','markdown'], tableMethod: 'cluster', readingOrder: 'xycut', imageOutput: 'off', quiet: true, ...(opts.pages?{pages}:{}) , ...(opts.password?{password}:{}) }`. No corpo de `loadDocument`, substituir o literal inline por `const options = buildConvertOptions(outDir, opts);`. Os valores e a ORDEM das chaves DEVEM ficar idênticos (o guard D-15 compara). Motivo: tornar o plano default assertável CLI-free, espelhando como `buildSpawnArgs` foi extraído na Phase 1.
  (B) ADICIONAR o branch de D-03 ENTRE o branch OCR (linhas 34-42, INTOCADO — D-06 OCR vence) e o caminho default (linha 44+). Importar no topo `import { runLangchainLoader, isLangchainAvailable } from './langchain-loader.js';` (espelha linha 18). O branch: `if ((process.env.ANKINATOR_PDF_LOADER?.trim().toLowerCase()) === 'langchain') { if (await isLangchainAvailable()) { console.error('[ankinator] loader: langchain'); return runLangchainLoader(pdfPath, opts); } console.error('[ankinator] loader: langchain indisponível (fallback→node)'); }` — early-return SÓ quando disponível (D-04 fallback silencioso); quando indisponível, NÃO retorna → cai no default abaixo (D-05 log). CRÍTICO (D-15/Pitfall 3): o branch é PURAMENTE ADITIVO — quando `ANKINATOR_PDF_LOADER` está unset, a condição é false, NADA do langchain roda (nem `isLangchainAvailable()`), e o fluxo segue idêntico ao de hoje. NÃO mover o branch OCR. NÃO alterar `convert`/`parseOdlOutput`/`finally`. Comentários PT-BR (D-03/D-04/D-05/D-06/D-15).</action>
  <verify>
    <automated>cd ankinator-app/server && npx tsc --noEmit && grep -c 'ANKINATOR_PDF_LOADER' src/core/document-loader.ts</automated>
  </verify>
  <acceptance_criteria>`npx tsc --noEmit` passa; `buildConvertOptions` é exportado (`grep 'export function buildConvertOptions'` retorna 1); o branch langchain só faz early-return dentro do `if (... === 'langchain')` (a condição testa igualdade a 'langchain', então env unset não entra); o branch OCR (linhas 34-42 originais) permanece textualmente antes do branch langchain; `convert([pdfPath], options)` e `parseOdlOutput(outDir, pdfPath)` inalterados (grep confirma presença).</acceptance_criteria>
  <done>`loadDocument` tem o branch langchain aditivo (D-03/D-04/D-05) após o OCR (D-06); `buildConvertOptions` extraído puro; o caminho default permanece byte-idêntico (preparado para o guard D-15).</done>
</task>

<task type="auto">
  <name>Task 2: Criar guard-default-loader.ts (D-15 não-regressão CLI-free)</name>
  <files>ankinator-app/server/src/scripts/guard-default-loader.ts</files>
  <read_first>
    - ankinator-app/server/src/scripts/smoke-runner.ts (linhas 34-78 — disciplina `fail()` + compare + `--assert-args`)
    - ankinator-app/server/src/core/document-loader.ts (Task 1 — `buildConvertOptions` exportado)
    - 02-RESEARCH Pitfall 3 + 02-VALIDATION §"Per-Task Verification Map" (guard default linha)
  </read_first>
  <action>Criar script `tsx` standalone que prova CLI-free (sem spawnar Java/Python, sem chamar `isLangchainAvailable()`) que o plano default é byte-idêntico ao de hoje. Importar `buildConvertOptions` de `../core/document-loader.js`. Definir `fail(msg): never { console.error('   ✗ FALHA: ' + msg); process.exit(1); }` (smoke-runner.ts:51-54). Construir o objeto ESPERADO literalmente igual ao de hoje (document-loader.ts:49-58, com um `outDir` fixo de teste, ex.: `/tmp/guard`): `{ outputDir, format: ['json','markdown'], tableMethod:'cluster', readingOrder:'xycut', imageOutput:'off', quiet:true }`. Chamar `const actual = buildConvertOptions('/tmp/guard', {})` (opts vazio = caminho default sem pages/password). Asserir byte-a-byte: cada chave conhecida (`outputDir`, `tableMethod`, `readingOrder`, `imageOutput`, `quiet`) igual ao esperado via `fail()` em mismatch; `format` comparado element-by-element (length-first); e que `actual` NÃO tem chaves `pages`/`password` quando opts vazio (`!('pages' in actual)` etc.). Também asserir o cenário com pages/password: `buildConvertOptions('/tmp/guard', { pages:'1,3', password:'x' })` adiciona exatamente `pages:'1,3'` e `password:'x'`. CRÍTICO: o script NÃO importa `langchain-loader`, NÃO chama `isLangchainAvailable()`, NÃO faz `convert`/`spawn` — é o guard de que o default roda SEM Python/Java (D-15). Adicionar o early-exit idioma `if (process.argv.includes('--assert')) { console.log('--assert: guard default CLI-free OK; ConvertOptions byte-idêntico.'); process.exit(0); }` ao final do bloco de asserções (ou rodar as asserções sempre e sair 0 — o `--assert` espelha smoke-runner). Comentários PT-BR explicando que isto é o guard SPEC-01-equivalente da Phase 2 (Pitfall 3).</action>
  <verify>
    <automated>cd ankinator-app/server && tsx src/scripts/guard-default-loader.ts --assert</automated>
  </verify>
  <acceptance_criteria>`tsx src/scripts/guard-default-loader.ts --assert` sai 0 em < 2s; o script NÃO importa `langchain-loader` nem chama `isLangchainAvailable`/`convert`/`spawn` (grep `langchain-loader\|isLangchainAvailable\|child_process\|convert(` retorna 0); alterar deliberadamente um valor esperado (ex.: `tableMethod`) faz o script sair 1 via `fail()`.</acceptance_criteria>
  <done>O guard D-15 prova CLI-free que o `ConvertOptions` default é byte-idêntico (sem Java/Python), fechando o Pitfall 3. É o gate per-wave.</done>
</task>

<task type="auto">
  <name>Task 3: Criar smoke-langchain-loader.ts (smoke gated, D-14)</name>
  <files>ankinator-app/server/src/scripts/smoke-langchain-loader.ts</files>
  <read_first>
    - ankinator-app/server/src/scripts/smoke-loader.ts (INTEIRO — 30 linhas; harness a clonar)
    - ankinator-app/server/src/core/langchain-loader.ts (Plano 03 — `isLangchainAvailable`)
    - 02-RESEARCH Pitfall 2 (smoke revela Java ausente) + 02-VALIDATION §"Manual-Only Verifications"
  </read_first>
  <action>Clonar o harness de `smoke-loader.ts` (argv-PDF check + timing + preview de seções) com um GATE no topo (D-14 — smoke opt-in, não falha o CI): importar `isLangchainAvailable` de `../core/langchain-loader.js` e, se `!(await isLangchainAvailable())`, imprimir `'langchain indisponível — smoke pulado.'` e `process.exit(0)` (pula gracioso). Forçar o caminho langchain: definir `process.env.ANKINATOR_PDF_LOADER = 'langchain'` ANTES de chamar `loadDocument` (importado de `../core/document-loader.js`), OU chamar `runLangchainLoader(pdf, {})` direto (qualquer um — documentar a escolha). Exigir argv `<pdf>` (`if (!pdf) { console.error('Uso: smoke-langchain-loader <caminho.pdf>'); process.exit(1); }`). Após carregar, imprimir `doc.fileName`, `doc.numPages`, `doc.sections.length`, `doc.elements.length`, `doc.markdown.length`, tempo (ms), e um preview das primeiras seções + 600c de markdown (espelha smoke-loader.ts:17-29). Este smoke é o que REVELA Java ausente em runtime (Pitfall 2: stderr com `java`/`ClassNotFound`) — o erro PROPAGA (D-04). Comentários PT-BR. Confirma A1/A2/A3 das Open Questions quando rodado com PDF real + venv + Java.</action>
  <verify>
    <automated>cd ankinator-app/server && tsx src/scripts/smoke-langchain-loader.ts</automated>
  </verify>
  <acceptance_criteria>`tsx src/scripts/smoke-langchain-loader.ts` (sem argv ou sem ambiente) sai limpo: ou exit 1 com a mensagem de uso (sem PDF), ou exit 0 com 'smoke pulado' (gate `isLangchainAvailable()` false) — NÃO trava nem lança não-tratado; o script importa `isLangchainAvailable` (grep retorna 1) e força `ANKINATOR_PDF_LOADER=langchain` OU chama `runLangchainLoader` direto. `npx tsc --noEmit` passa.</acceptance_criteria>
  <done>O smoke gated de D-14 existe, pula gracioso quando indisponível (CI-safe), e exercita o caminho langchain real (revela Java ausente) quando o ambiente está pronto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env var ANKINATOR_PDF_LOADER → seleção de loader | Var de ambiente do usuário decide o caminho. Default seguro 'node' (env unset = produção atual). |
| Node → @opendataloader/pdf (Java) / sidecar Python | Caminho default spawna Java; branch langchain spawna Python (Plano 03). Ambos via libs/argv, sem shell. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Tampering | branch novo em `loadDocument` pode desviar o default (regressão PIPE-03/D-15) | mitigate | Branch PURAMENTE ADITIVO (early-return só em `=== 'langchain'` + disponível); guard `guard-default-loader.ts` prova CLI-free o `ConvertOptions` byte-idêntico com env unset (Pitfall 3). |
| T-02-09 | Denial of Service | Java ausente → erro de runtime no caminho langchain | accept | D-04: erro de runtime PROPAGA (não mascara bug). Não afeta o default Node. Documentado (R2 / requirements.txt). Smoke gated revela. LOW (opt-in). |
| T-02-10 | Information Disclosure | log `[ankinator] loader: …` em stderr | accept | Só o NOME do loader (não conteúdo/segredo). Local single-user. INFO. |
</threat_model>

<verification>
- `cd ankinator-app/server && npx tsc --noEmit` (build verde com branch + guard + smoke)
- `cd ankinator-app/server && tsx src/scripts/guard-default-loader.ts --assert` (D-15 verde — default byte-idêntico CLI-free)
- `cd ankinator-app/server && tsx src/scripts/assert-langchain-normalize.ts` (Plano 01 — re-confirma o núcleo após wiring)
- `cd ankinator-app/server && tsx src/scripts/smoke-langchain-loader.ts <pdf>` (gated — manual, exige venv+Java; confirma A1/A2/A3)
- Não-regressão: `git diff src/core/document-loader.ts` mostra só o branch aditivo + a extração de `buildConvertOptions` (mesmos valores/ordem)
</verification>

<success_criteria>
- `loadDocument` seleciona langchain por env (D-03), com fallback silencioso por indisponibilidade (D-04) + log (D-05), OCR vence (D-06), e o default (env unset) byte-idêntico (D-15) — PDF-02 completo.
- Guard `guard-default-loader.ts --assert` verde, CLI-free, prova o `ConvertOptions` default intocado SEM Java/Python (PDF-03 não-regressão; Pitfall 3 fechado).
- `smoke-langchain-loader.ts` gated existe (D-14), CI-safe, exercita o caminho real (PDF-01/PDF-02 ponta-a-ponta com ambiente pronto).
</success_criteria>

<output>
Create `.planning/phases/02-loader-pdf-langchain/02-04-SUMMARY.md` when done
</output>
