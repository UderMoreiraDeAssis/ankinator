---
phase: 02-loader-pdf-langchain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ankinator-app/server/src/core/langchain-normalize.ts
  - ankinator-app/server/src/core/odl-parse.ts
  - ankinator-app/server/src/scripts/assert-langchain-normalize.ts
autonomous: true
requirements: [PDF-03]
must_haves:
  truths:
    - "normalize(Document[], pdfPath) produz um LoadedDocument com sections derivadas dos headings ATX `#`"
    - "numPages = max(metadata.page) (page é 1-indexed; SEM +1)"
    - "title = primeiro heading ATX ou null quando não há heading inicial"
    - "elements[] tem 1 'text block' por Document emitido (não por página física)"
    - "o unit puro CLI-free roda sem Python/Java e falha com process.exit(1) em mismatch"
  artifacts:
    - path: "ankinator-app/server/src/core/langchain-normalize.ts"
      provides: "normalize(docs, pdfPath) -> LoadedDocument (núcleo puro PDF-03)"
      exports: ["normalize"]
      min_lines: 50
    - path: "ankinator-app/server/src/scripts/assert-langchain-normalize.ts"
      provides: "Unit puro CLI-free com fixture Document[] (D-14)"
      contains: "process.exit(1)"
    - path: "ankinator-app/server/src/core/odl-parse.ts"
      provides: "cleanMarkdown exportado para reuso (D-09)"
      contains: "export function cleanMarkdown"
  key_links:
    - from: "ankinator-app/server/src/core/langchain-normalize.ts"
      to: "ankinator-app/server/src/core/odl-parse.ts"
      via: "import { cleanMarkdown }"
      pattern: "import.*cleanMarkdown.*odl-parse"
    - from: "ankinator-app/server/src/scripts/assert-langchain-normalize.ts"
      to: "ankinator-app/server/src/core/langchain-normalize.ts"
      via: "import { normalize }"
      pattern: "import.*normalize.*langchain-normalize"
---

<objective>
Construir o núcleo PURO da fase: a função `normalize(Document[]) → LoadedDocument` que converte a saída per-page do sidecar LangChain no tipo `LoadedDocument` existente (D-07..D-10), reusando `cleanMarkdown` de `odl-parse.ts`, mais o unit determinístico CLI-free que a valida (D-14).

Este é o ponto de maior risco de regressão da fase porque `chunkDocument` consome SÓ `doc.sections[]`. O módulo NÃO faz I/O (sem spawn, sem Python, sem Java) — é testável sempre no CI.

Purpose: Entregar o coração de PDF-03 isolado e provado antes de qualquer wiring de spawn/branch (Wave 1, sem dependências). Os planos de loader (Wave 2) e branch (Wave 3) consomem este `normalize`.
Output: `langchain-normalize.ts` (export `normalize`), `cleanMarkdown` exportado em `odl-parse.ts`, e `assert-langchain-normalize.ts` (unit puro, scaffold Wave 0 de D-14).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-loader-pdf-langchain/02-CONTEXT.md
@.planning/phases/02-loader-pdf-langchain/02-RESEARCH.md
@.planning/phases/02-loader-pdf-langchain/02-PATTERNS.md

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do codebase. Use-os diretamente — sem exploração. -->

De ankinator-app/server/src/core/types.ts (NÃO ALTERAR — D-05/D-08/D-10):
```typescript
export type ElementType = 'heading' | 'paragraph' | 'caption' | 'table' | 'image' | 'list' | 'text block' | string;

export interface DocElement {
  type: ElementType;
  id?: number;
  page: number;
  headingLevel?: number;
  content?: string;
}

export interface Section {
  id: string;
  title: string;
  level: number;
  pageStart: number;
  pageEnd: number;
  markdown: string;   // inclui o título + texto até o próximo título de mesmo nível ou superior
  charCount: number;
}

export interface LoadedDocument {
  fileName: string;
  numPages: number;
  title: string | null;
  markdown: string;
  sections: Section[];
  elements: DocElement[];
  usedOcr: boolean;
}
```

De ankinator-app/server/src/core/odl-parse.ts (REUSO — alvos de D-07/D-09):
- `cleanMarkdown(md: string): string` (linhas 76-86) — HOJE NÃO exportada. Remove linhas de tabela vazias (`/^\|[\s|:-]*\|$/` sem alfanumérico) + colapsa 3+ linhas em branco. Idempotente, sem estado.
- `elementToMarkdown(el)` (linhas 88-94) — clamp de nível: `Math.min(Math.max(level, 1), 6)` → `'#'.repeat(level) + ' ' + content`. Idioma a espelhar ao re-emitir heading.
- `buildSections(elements, fallbackTitle)` (linhas 96-139) — esqueleto a ESPELHAR: `flush` closure cria `Section` com `id: crypto.randomUUID()`, `pageStart/pageEnd`, `charCount: markdown.length`; filtro final de seção vazia: `sections.filter((s) => s.markdown.replace(/^#+\s.*$/m, '').trim().length > 0 || sections.length === 1)`. ATENÇÃO: a `buildSections` atual parte de `DocElement[]` com `headingLevel`; o novo loader recebe markdown bruto → adaptar a entrada.

De ankinator-app/server/src/core/chunker.ts (CONSUMIDOR — INTOCADO):
- `chunkDocument(doc)` consome SÓ `doc.sections[]` (linhas 54-56). É a razão de D-07 (sections via headings, não page-as-section).
</interfaces>

<read_first>
- ankinator-app/server/src/core/odl-parse.ts (INTEIRO — 175 linhas; `cleanMarkdown` 76-86, `elementToMarkdown` 88-94, `buildSections`/`flush` 96-139, filtro de seção vazia 136-138, return shape de `parseOdlOutput` 165-173)
- ankinator-app/server/src/core/types.ts (INTEIRO — `DocElement` 22-30, `Section` 36-45, `LoadedDocument` 48-60, `'text block'` ∈ `ElementType` linha 18)
- ankinator-app/server/src/core/chunker.ts (linhas 54-60 — confirma que só `doc.sections[]` é consumido)
- ankinator-app/server/src/scripts/smoke-runner.ts (linhas 51-78 — idioma `fail()` + `process.exit(1)` + compare length-first/element-by-element + `--assert-args` early-exit)
- 02-RESEARCH.md §"Common Pitfalls" (Pitfall 1 page 1-indexed; Pitfall 4 title=null) + §"Refinamentos para o PLANEJAMENTO" (R1 páginas não-contíguas)
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Exportar cleanMarkdown de odl-parse.ts</name>
  <files>ankinator-app/server/src/core/odl-parse.ts</files>
  <read_first>ankinator-app/server/src/core/odl-parse.ts (linhas 76-86 — a função `cleanMarkdown`)</read_first>
  <action>Mudança CIRÚRGICA: adicionar a keyword `export` à declaração de `cleanMarkdown` na linha 76 (`function cleanMarkdown` → `export function cleanMarkdown`). NÃO alterar o corpo da função, NÃO tocar `parseOdlOutput`/`buildSections`/`flatten`/`renderTable`/`elementToMarkdown`/qualquer outra coisa no arquivo. Motivo: D-09 reusa `cleanMarkdown` no novo módulo de normalização; a discrição do CONTEXT recomenda exportar (idempotente, sem estado) em vez de duplicar. Comentário PT-BR opcional acima do export se ajudar a rastreabilidade (D-09).</action>
  <verify>
    <automated>cd ankinator-app/server && grep -n '^export function cleanMarkdown' src/core/odl-parse.ts</automated>
  </verify>
  <acceptance_criteria>`grep -n '^export function cleanMarkdown' src/core/odl-parse.ts` retorna exatamente 1 linha; `git diff src/core/odl-parse.ts` mostra APENAS a adição de `export` (nenhuma outra linha alterada).</acceptance_criteria>
  <done>`cleanMarkdown` é importável de `./odl-parse.js`; o resto de odl-parse.ts byte-idêntico.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar langchain-normalize.ts (normalize puro, D-07..D-10)</name>
  <files>ankinator-app/server/src/core/langchain-normalize.ts</files>
  <read_first>
    - ankinator-app/server/src/core/odl-parse.ts (linhas 88-139 — `elementToMarkdown` clamp + `buildSections`/`flush`/filtro de seção vazia a espelhar)
    - ankinator-app/server/src/core/types.ts (linhas 18, 22-60 — `'text block'`, `DocElement`, `Section`, `LoadedDocument`)
    - 02-RESEARCH.md §Code Examples (a função `normalize` de referência) + Pitfall 1/Pitfall 4 + R1
  </read_first>
  <behavior>
    - Fixture 3 páginas contíguas (page 1,2,3), página 1 começa com `# Cap 1`, página 2 com `## Seção 2` → `numPages === 3`; `title === 'Cap 1'`; `elements.length === 3`; `sections.length >= 2` (uma por heading ATX).
    - Fixture com páginas NÃO-contíguas (page 1 e page 3, sem Document para page 2 — R1) → `numPages === 3`; `elements.length === 2` (1 por Document emitido, não por página física).
    - Fixture sem nenhum heading ATX inicial (texto puro) → `title === null` (Pitfall 4); cai em 1 seção fallback com `fallbackTitle = basename(pdf)`.
    - `markdown` final = `cleanMarkdown(docs.map(d=>d.page_content).join('\n\n'))`, SEM marcadores de página (D-09).
  </behavior>
  <action>Criar `normalize(docs: RawDoc[], pdfPath: string): LoadedDocument` PURO (sem I/O — sem `fs`, sem spawn). Imports: `crypto` (node:crypto), `path` (node:path), `cleanMarkdown` de `./odl-parse.js`, tipos `DocElement`/`LoadedDocument`/`Section` de `./types.js`. Definir interface local `RawDoc { page_content: string; metadata: { source?: string; format?: string; page?: number } }` (espelha D-01). Const `ATX = /^(#{1,6})\s+(.+?)\s*#*$/`.
  Implementar exatamente:
  (1) D-08 `elements`: `docs.map(d => ({ type: 'text block', page: d.metadata.page ?? 1, content: d.page_content }))` — 1 `'text block'` por Document EMITIDO (R1: não iterar range 1..N).
  (2) D-09 `markdown`: `cleanMarkdown(docs.map(d => d.page_content).join('\n\n'))` — sem marcadores de página.
  (3) D-07 `sections`: escrever `buildSectionsFromMarkdown(docs, path.basename(pdfPath))` NOVO que itera CADA Document (sabe a `metadata.page`), quebra `page_content` por linha, ao casar `ATX` faz `flush` e abre nova seção (espelhando o `flush` closure de `buildSections`: `id: crypto.randomUUID()`, `pageStart/pageEnd`, `charCount: markdown.length`), re-emite o heading com o clamp `Math.min(Math.max(level,1),6)` + `'#'.repeat(level)`; antes do 1º heading acumula numa seção fallback (`title = fallbackTitle`, `level: 1`); aplicar o MESMO filtro final de seção vazia de `buildSections` (linhas 136-138). `pageEnd` recebe `Math.max` com a página corrente conforme o conteúdo avança entre Documents.
  (4) D-10 campos-topo: `const pages = docs.map(d => d.metadata.page ?? 1); const numPages = pages.length ? Math.max(...pages) : 1;` — **page é 1-indexed → SEM `+1`** (Pitfall 1). `fileName = path.basename(pdfPath)`. `title` = 1º match de `ATX` no markdown bruto OU `null` (Pitfall 4 — só de linha que casa `^#{1,6}\s+`, nunca a 1ª linha de texto). `usedOcr: false`.
  Retornar `LoadedDocument` com o conjunto EXATO de campos do tipo. Comentários PT-BR. NÃO estender `LoadedDocument` (D-05). NÃO chamar API/LLM.</action>
  <verify>
    <automated>cd ankinator-app/server && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>`npx tsc --noEmit` passa (sem erro de tipo); `langchain-normalize.ts` exporta `normalize`; não importa `node:fs`/`node:child_process` (grep `child_process\|node:fs` retorna 0 linhas no arquivo); `numPages` usa `Math.max(...pages)` SEM `+1` (grep `\+ *1` na linha de numPages retorna 0).</acceptance_criteria>
  <done>`normalize` compila, é puro (sem I/O), e produz `LoadedDocument` com sections via ATX, `numPages=max(page)`, `title=1º heading|null`, 1 `'text block'`/Document.</done>
</task>

<task type="auto">
  <name>Task 3: Criar assert-langchain-normalize.ts (unit puro CLI-free, D-14)</name>
  <files>ankinator-app/server/src/scripts/assert-langchain-normalize.ts</files>
  <read_first>
    - ankinator-app/server/src/scripts/smoke-runner.ts (linhas 51-72 — `fail()` + compare length-first + element-by-element)
    - ankinator-app/server/src/core/langchain-normalize.ts (o `normalize` recém-criado — assinatura e shape de retorno)
    - 02-VALIDATION.md §"Wave 0 Requirements" (fixture: ≥2 páginas, ≥1 heading ATX, ≥1 página sem heading, números de página NÃO-contíguos)
  </read_first>
  <action>Criar script `tsx` standalone que importa `normalize` de `../core/langchain-normalize.js` e o exercita com uma fixture INLINE `RawDoc[]` SEM Python/Java (CLI-free, determinístico). Definir helper `fail(msg): never { console.error('   ✗ FALHA: ' + msg); process.exit(1); }` (idioma de smoke-runner.ts:51-54). Fixture cobre os 3 cenários de R1/Pitfall:
  (a) ≥2 Documents com `metadata.page` NÃO-contíguos — ex.: page 1 (`# Cap 1\n\ntexto`), page 3 (`## Seção B\n\ntexto`), SEM Document para page 2 (R1);
  (b) page 1 começa com heading ATX (`# Cap 1`) → asserir `title === 'Cap 1'`;
  (c) opcional: um 2º cenário (chamada separada a `normalize`) com fixture SEM heading inicial → asserir `title === null` (Pitfall 4).
  Asserções OBRIGATÓRIAS (cada uma chama `fail()` em mismatch):
  - `doc.numPages === 3` (max(page) com pages [1,3]; Pitfall 1 — provar SEM `+1`);
  - `doc.elements.length === <número de Documents emitidos>` (R1 — 1 por Document, não por página física);
  - `doc.elements.every(e => e.type === 'text block')` (D-08);
  - `doc.sections.length >= 2` E cada `section.id` é string não-vazia (D-07);
  - `doc.title === 'Cap 1'` (cenário b) e `doc.title === null` (cenário c, Pitfall 4);
  - `doc.usedOcr === false`;
  - `doc.markdown` NÃO contém marcador de página (D-09 — ex.: assert `!/page \d+ of/i.test(doc.markdown)`).
  Ao final imprimir uma linha de sucesso e sair 0 (sem `--assert-args` necessário aqui — o script inteiro é CLI-free). Comentários PT-BR.</action>
  <verify>
    <automated>cd ankinator-app/server && tsx src/scripts/assert-langchain-normalize.ts</automated>
  </verify>
  <acceptance_criteria>`tsx src/scripts/assert-langchain-normalize.ts` sai com código 0 em < 2s, SEM spawnar Python/Java (grep `child_process\|ANKINATOR_LANGCHAIN_PYTHON\|spawn` no script retorna 0); injetar deliberadamente um erro (ex.: alterar uma asserção esperada) faz o script sair 1 via `fail()`.</acceptance_criteria>
  <done>O unit puro de D-14 existe, valida `normalize` contra fixture com R1/Pitfall 1/Pitfall 4, e é o gate per-task commit (CLI-free, < 2s).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhum nesta plano) | `normalize` é função PURA sobre dados já em memória; sem rede, sem subprocess, sem FS. O `JSON.parse` do stdout do sidecar acontece no plano de loader (Wave 2), não aqui. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `normalize(docs)` — input `RawDoc[]` potencialmente malformado | accept | Input é dado local já parseado (não entrada de rede não-confiável). Acesso defensivo já presente (`d.metadata.page ?? 1`). Sem `eval`/`Function`. Risco LOW para CLI local single-user. |
| T-02-02 | Denial of Service | markdown muito grande (apostila gigante) → `join`/regex | accept | Apostilas de concurso têm tamanho limitado; sem loop não-limitado. O chunker já lida com documentos grandes downstream. LOW. |
</threat_model>

<verification>
- `cd ankinator-app/server && npx tsc --noEmit` (build do server verde com o novo módulo)
- `cd ankinator-app/server && tsx src/scripts/assert-langchain-normalize.ts` (unit puro D-14 verde, CLI-free)
- `git diff src/core/odl-parse.ts` mostra só a adição de `export` (não-regressão do parser Node)
</verification>

<success_criteria>
- `normalize(Document[], pdfPath)` compila e produz `LoadedDocument` com sections via ATX, `numPages = max(page)`, `title = 1º heading|null`, 1 `'text block'`/Document (PDF-03 núcleo).
- `cleanMarkdown` exportado e reusado (D-09); `odl-parse.ts` sem outra mudança.
- Unit puro CLI-free (D-14) verde e determinístico (cobre Pitfall 1, Pitfall 4, R1).
- Nenhuma dependência de Python/Java introduzida neste plano.
</success_criteria>

<output>
Create `.planning/phases/02-loader-pdf-langchain/02-01-SUMMARY.md` when done
</output>
