---
phase: 03-classificador-de-deck-card-educativo
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - ankinator-app/web/src/App.tsx
  - ankinator-app/web/src/components/StructurePanel.tsx
  - ankinator-app/web/src/components/ProgressPanel.tsx
  - ankinator-app/web/src/components/CardTable.tsx
autonomous: false
requirements: [PIPE-02]
user_setup: []

must_haves:
  truths:
    - "O StructurePanel mostra o bloco 'Modo educativo' com 2 toggles: Classificar deck+tags (default ON) e Card educativo (default OFF)"
    - "O progresso de enrich (classificando.../reescrevendo...) aparece DENTRO do passo generating no ProgressPanel, sem novo passo no Stepper"
    - "A CardTable mostra deck e tags read-only quando preenchidos"
    - "O App.tsx escuta o evento SSE enrich-progress e repassa ao ProgressPanel"
  artifacts:
    - path: "ankinator-app/web/src/components/StructurePanel.tsx"
      provides: "Bloco Modo educativo com toggles classificar/cardBuilder"
      contains: "Modo educativo"
    - path: "ankinator-app/web/src/App.tsx"
      provides: "Listener SSE enrich-progress + defaults de options"
      contains: "enrich-progress"
    - path: "ankinator-app/web/src/components/CardTable.tsx"
      provides: "Badges deck+tags read-only"
      contains: "deck"
  key_links:
    - from: "ankinator-app/web/src/App.tsx"
      to: "ankinator-app/web/src/components/ProgressPanel.tsx"
      via: "prop enrichProgress"
      pattern: "enrichProgress"
    - from: "ankinator-app/web/src/components/StructurePanel.tsx"
      to: "options.classificar / options.cardBuilder"
      via: "checkbox onChange setOptions"
      pattern: "classificar|cardBuilder"
---

<objective>
Entregar a UI do modo educativo (PIPE-02): bloco "Modo educativo" com 2 toggles no `StructurePanel` (D-14/D-15), progresso de enrich dentro do passo `generating` no `ProgressPanel` (D-16), badges de deck+tags read-only na `CardTable` (D-17), e o listener SSE `enrich-progress` no `App.tsx` (D-02/D-16). Sem adicionar 5º step no Stepper.

Purpose: Fecha PIPE-02 — o usuário liga/desliga os estágios antes de gerar, vê o progresso de enrich na mesma tela, e confere deck/tags antes de exportar. Roda em paralelo ao Plano 02 (arquivos disjuntos: web/* vs server/*).
Output: toggles funcionais com defaults; progresso de enrich renderizado; deck/tags exibidos; build web verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-classificador-de-deck-card-educativo/03-CONTEXT.md
@.planning/phases/03-classificador-de-deck-card-educativo/03-RESEARCH.md
@.planning/phases/03-classificador-de-deck-card-educativo/03-PATTERNS.md

<interfaces>
<!-- Contratos do Plano 01 (já no web/src/types.ts) — consumir diretamente. -->
web/src/types.ts:
  GenerateOptions { maxPerChunk?; incluirExtraidas?; incluirCriadas?; tags?; classificar?: boolean; cardBuilder?: boolean }
  EnrichProgress { estagio: 'classificando' | 'reescrevendo'; index: number; total: number; erro?: string }
  Questao { ...; deck?: string; tags?: string[] }  // NÃO tocar
StructurePanel props (atuais): { extract; selected; onToggle; onSelectAll; options; setOptions; tagsInput; setTagsInput; onGenerate; canGenerate; provider; busy }
ProgressPanel props (atuais): { total; progress: ChunkProgress[]; fileName }
App.tsx SSE: es.addEventListener('progress'|'done', ...); options via useState<GenerateOptions>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: App.tsx — listener enrich-progress + defaults de options + repasse ao ProgressPanel</name>
  <read_first>
    - ankinator-app/web/src/App.tsx (useState options ~20; handleGenerate setProgress([])/setCards([])/setDropped ~60-66; listeners SSE progress/done ~74-96; render ProgressPanel ~161)
    - ankinator-app/web/src/types.ts (GenerateOptions com classificar/cardBuilder + EnrichProgress — do Plano 01)
    - .planning/phases/03-classificador-de-deck-card-educativo/03-PATTERNS.md secao App.tsx (estado enrichProgress, listener entre progress e done, prop ao ProgressPanel)
    - .planning/phases/03-classificador-de-deck-card-educativo/03-RESEARCH.md Research flag 4 (id-churn: setCards substitui o array inteiro no done — sem race)
  </read_first>
  <action>Em `ankinator-app/web/src/App.tsx`:
  (1) Defaults (D-15): no `useState<GenerateOptions>(...)` inicial, adicionar `classificar: true` (default ON) e `cardBuilder: false` (default OFF) ao objeto existente (preservar maxPerChunk/incluirExtraidas/incluirCriadas atuais).
  (2) Estado novo: `const [enrichProgress, setEnrichProgress] = useState<EnrichProgress | null>(null)` (importar `EnrichProgress` de `./types`).
  (3) Limpeza: em `handleGenerate`, junto com `setProgress([])`, chamar `setEnrichProgress(null)`.
  (4) Listener: adicionar `es.addEventListener('enrich-progress', (ev) => { const data = JSON.parse((ev as MessageEvent).data) as EnrichProgress; setEnrichProgress(data); })` INSERIDO entre os listeners `progress` e `done` (D-16). NÃO mexer no listener `done` (Research flag 4: `setCards(job.questoes)` já substitui o array inteiro — ids derivados do split estão estáveis; sem race com dropped/onEdit).
  (5) Repasse: passar `enrichProgress={enrichProgress}` como prop ao `<ProgressPanel ... />` na render do passo `generating`.</action>
  <verify>
    <automated>cd ankinator-app/web && npx tsc --noEmit 2>&1 | head -10 && grep -q "enrich-progress" src/App.tsx && grep -q "setEnrichProgress" src/App.tsx && grep -q "classificar: true" src/App.tsx && grep -q "cardBuilder: false" src/App.tsx && echo "App OK"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` no web passa sem erros.
    - `App.tsx` registra `es.addEventListener('enrich-progress', ...)` e chama `setEnrichProgress`.
    - O estado inicial de `options` contém `classificar: true` e `cardBuilder: false` (D-15).
    - `handleGenerate` reseta `enrichProgress` para null junto com o reset de `progress`.
    - `<ProgressPanel>` recebe a prop `enrichProgress`.
    - O listener `done` NÃO foi alterado.
  </acceptance_criteria>
  <done>Listener enrich-progress ativo; defaults D-15 aplicados; enrichProgress repassado; sem alteração no done.</done>
</task>

<task type="auto">
  <name>Task 2: StructurePanel — bloco "Modo educativo" + ProgressPanel render enrich (D-14/D-15/D-16)</name>
  <read_first>
    - ankinator-app/web/src/components/StructurePanel.tsx (Props ~4-17; checkbox pattern incluirExtraidas ~84-100; posição do botão Gerar)
    - ankinator-app/web/src/components/ProgressPanel.tsx (Props ~3-7; lista de progress ~36-48)
    - ankinator-app/web/src/components/Stepper.tsx (4 steps fixos — NÃO adicionar 5º, D-16)
    - .planning/phases/03-classificador-de-deck-card-educativo/03-PATTERNS.md secoes StructurePanel.tsx e ProgressPanel.tsx (markup exato dos toggles e da linha de progresso)
  </read_first>
  <action>Em `ankinator-app/web/src/components/StructurePanel.tsx`: adicionar um bloco "Modo educativo" APÓS o slider de geração e ANTES do botão Gerar (D-14). Seguir o MESMO markup dos checkboxes existentes (label flex justify-between + input checkbox classes `h-4 w-4 rounded border-slate-300 text-brand-600`). Dois toggles: "Classificar deck + tags" com `checked={options.classificar !== false}` (default ON, D-15) e `onChange={(e) => setOptions({ ...options, classificar: e.target.checked })}`; "Card educativo" com `checked={options.cardBuilder === true}` (default OFF, D-15) e `onChange={(e) => setOptions({ ...options, cardBuilder: e.target.checked })}`. As Props NÃO mudam — `options.classificar`/`options.cardBuilder` já chegam via `options`. Manter o bloco extensível (Phase 4 adiciona mnemônico/imagem aqui — comentário PT-BR sinalizando).
  Em `ankinator-app/web/src/components/ProgressPanel.tsx`: adicionar `enrichProgress?: EnrichProgress | null` às Props (importar `EnrichProgress` de `../types`). Renderizar, ABAIXO da lista de chunks, um bloco condicional `{enrichProgress && (...)}` que mostra "Classificando deck + tags…" quando `estagio === 'classificando'` ou "Reescrevendo card {index+1}/{total}…" quando `'reescrevendo'`, e um indicador de erro quando `enrichProgress.erro`. Layout exato é discrição (seguir o estilo das linhas de progresso existentes). NÃO tocar o Stepper.</action>
  <verify>
    <automated>cd ankinator-app/web && npx tsc --noEmit 2>&1 | head -10 && grep -q "Modo educativo" src/components/StructurePanel.tsx && grep -q "classificar" src/components/StructurePanel.tsx && grep -q "cardBuilder" src/components/StructurePanel.tsx && grep -q "enrichProgress" src/components/ProgressPanel.tsx && grep -q "classificando" src/components/ProgressPanel.tsx && echo "panels OK"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` no web passa sem erros.
    - `StructurePanel.tsx` contém o texto "Modo educativo" e dois checkboxes controlando `options.classificar` e `options.cardBuilder`.
    - O toggle Classificar usa `!== false` (ON por default) e o Card educativo usa `=== true` (OFF por default) — D-15.
    - `ProgressPanel.tsx` aceita prop `enrichProgress` e renderiza rótulos `classificando`/`reescrevendo` condicionalmente.
    - `Stepper.tsx` NÃO foi modificado (sem 5º step — D-16).
  </acceptance_criteria>
  <done>Bloco Modo educativo com defaults corretos; progresso de enrich renderizado no ProgressPanel; Stepper intacto.</done>
</task>

<task type="auto">
  <name>Task 3: CardTable — badges deck + tags read-only (D-17)</name>
  <read_first>
    - ankinator-app/web/src/components/CardTable.tsx (badges existentes tipo/página/banca ~51-69)
    - ankinator-app/web/src/types.ts (Questao com deck?/tags? — campos opcionais SPEC-05)
    - .planning/phases/03-classificador-de-deck-card-educativo/03-PATTERNS.md secao CardTable.tsx (markup sugerido dos badges deck/tags)
  </read_first>
  <action>Em `ankinator-app/web/src/components/CardTable.tsx`: adicionar, junto à fileira de badges existente (tipo/página/banca), dois badges read-only condicionais (D-17): (1) badge de deck renderizado SÓ quando `c.deck` está preenchido — mostrar `c.deck` (ex.: classe `rounded bg-violet-100 px-1.5 py-0.5 text-xs font-medium text-violet-700`); (2) badge de tags renderizado SÓ quando `c.tags?.length` — mostrar as primeiras tags (ex.: `c.tags.slice(0,3).join(' ')` com `+N` quando houver mais). Read-only: SEM estado de edição, SEM input (D-17 — promover a editável é incremento futuro). Layout exato é discrição. Não tocar a lógica de descartar/restaurar/onEdit existente.</action>
  <verify>
    <automated>cd ankinator-app/web && npx tsc --noEmit 2>&1 | head -10 && grep -q "c.deck" src/components/CardTable.tsx && grep -q "c.tags" src/components/CardTable.tsx && npm run build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` no web passa sem erros.
    - `CardTable.tsx` renderiza `c.deck` condicionalmente (só quando preenchido) e `c.tags` condicionalmente (só quando `length`).
    - Não há input/onChange de edição de deck/tags (read-only — D-17).
    - `npm run build` do web conclui com sucesso (bundle gerado).
    - A lógica existente de descartar/onEdit não foi alterada.
  </acceptance_criteria>
  <done>Badges deck+tags read-only exibidos quando preenchidos; build web verde; sem edição.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint humano — verificar UI do modo educativo</name>
  <action>Checkpoint de verificação humana da UI (D-14..D-17). Pausar para o usuário confirmar visualmente os toggles, o progresso de enrich, os badges read-only e a ausência de 5º step antes de fechar a fase.</action>
  <what-built>UI completa do modo educativo: bloco "Modo educativo" com 2 toggles (Classificar ON / Card educativo OFF) no StructurePanel, progresso de enrich (classificando/reescrevendo) dentro do passo generating no ProgressPanel, e badges deck+tags read-only na CardTable. App.tsx escuta o evento SSE enrich-progress.</what-built>
  <how-to-verify>
    1. Suba a app (server + web dev). Em `ankinator-app/`: rodar o server e o web conforme os scripts existentes.
    2. Carregue um PDF de concurso e avance até a tela de estrutura. Confirme que o bloco "Modo educativo" aparece com "Classificar deck + tags" MARCADO e "Card educativo" DESMARCADO por default (D-15).
    3. Clique Gerar. Durante o passo "generating", confirme que após os blocos aparece "Classificando deck + tags…" (e, se ligar Card educativo, "Reescrevendo card N/M…"). Confirme que NÃO surge um 5º passo no Stepper (D-16).
    4. Na revisão (CardTable), confirme que cada card mostra um badge de deck (ex.: `Matéria::Assunto::Subtópico`) e badges de tags, em modo leitura (sem campo editável) — D-17.
    5. Desligue ambos os toggles, gere de novo, e confirme que o fluxo roda como antes (sem fase de enrich) — sanity de PIPE-03 na UI.
    Se algo destoar (toggle invertido, progresso não aparece, badges ausentes, 5º step), descreva; caso contrário aprove.
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva os problemas observados.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE server → App.tsx | Eventos enrich-progress são consumidos e renderizados no cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | conteúdo de enrich-progress renderizado na UI | accept | App local single-user; React escapa texto por padrão (sem dangerouslySetInnerHTML). deck/tags são strings exibidas como texto. Risco nulo. |

Nenhuma ameaça acima de MEDIUM. Este plano é UI read-only sobre dados locais; não introduz nova superfície de ataque.
</threat_model>

<verification>
- `npx tsc --noEmit` (web) verde.
- `npm run build` (web) conclui.
- Checkpoint humano: toggles com defaults corretos, progresso de enrich visível, badges read-only, Stepper sem 5º step, PIPE-03 sanity na UI.
</verification>

<success_criteria>
- Toggles do modo educativo com defaults inteligentes (D-15) — PIPE-02.
- Progresso de enrich no passo generating sem novo step (D-16).
- Deck/tags read-only na revisão (D-17).
- Listener SSE enrich-progress ativo (D-02).
</success_criteria>

<output>
Create `.planning/phases/03-classificador-de-deck-card-educativo/03-03-SUMMARY.md` when done
</output>
