---
phase: 03-classificador-de-deck-card-educativo
plan: 01
type: execute
wave: 1
depends_on: ["03-00"]
files_modified:
  - ankinator-app/server/src/core/specialists/enrich.ts
  - ankinator-app/server/src/core/specialists/prompts/deck-classifier.md
  - ankinator-app/server/src/core/specialists/prompts/card-builder.md
  - ankinator-app/server/src/store.ts
  - ankinator-app/server/src/core/types.ts
  - ankinator-app/web/src/types.ts
autonomous: true
requirements: [PIPE-01, DECK-01, DECK-02, CARD-01, CARD-02]
user_setup: []

must_haves:
  truths:
    - "Com classificar=true, enrichAll faz 1 chamada com TODOS os cards e preenche q.deck/q.tags casando por id (nunca posicional)"
    - "Com cardBuilder=true, enrichAll reescreve por-card: criada pode dividir 1→N; extraida só ajusta o verso"
    - "Falha em um estágio/card não derruba o job — mantém o card original e registra o erro no enrich-progress"
    - "Toggles off → deveRodarEnrich retorna false (gate de PIPE-03)"
  artifacts:
    - path: "ankinator-app/server/src/core/specialists/enrich.ts"
      provides: "enrichAll, parseClassificacoesJson, parseSingleCard, deveRodarEnrich, EnrichOpts, EnrichProgress"
      exports: ["enrichAll", "parseClassificacoesJson", "parseSingleCard", "deveRodarEnrich", "EnrichOpts", "EnrichProgress"]
      min_lines: 80
    - path: "ankinator-app/server/src/store.ts"
      provides: "JobEvent estendido com variante enrich-progress"
      contains: "enrich-progress"
  key_links:
    - from: "ankinator-app/server/src/core/specialists/enrich.ts"
      to: "deck-classifier.md / card-builder.md"
      via: "loadPrompt + runClaudeCli"
      pattern: "loadPrompt\\('(deck-classifier|card-builder)'\\)"
    - from: "ankinator-app/server/src/store.ts"
      to: "ankinator-app/server/src/core/specialists/enrich.ts"
      via: "import type EnrichProgress"
      pattern: "EnrichProgress"
---

<objective>
Construir o coração backend da fase: o módulo `core/specialists/enrich.ts` com `enrichAll()` (PIPE-01), os parsers tolerantes do classificador e do card-builder, o gate `deveRodarEnrich`, a extensão da union `JobEvent` com `enrich-progress` (D-02), o espelho de `GenerateOptions` (server + web no MESMO commit, Pitfall 1), e o refinamento dos `.md` canônicos para fixar o formato de saída JSON.

Purpose: Este plano define os CONTRATOS (tipos `EnrichOpts`/`EnrichProgress`, assinatura de `enrichAll`, gate `deveRodarEnrich`) que os Planos 02 (exporters/api) e 03 (UI) consomem. Entrega DECK-01/DECK-02 (lado classificação), CARD-01/CARD-02 (lado reescrita) e PIPE-01.
Output: enrich.ts funcional; testes unit de parsers/split/sequência verdes; guard-enrich.ts verde; tipos espelhados; `.md` com formato de saída explícito.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-classificador-de-deck-card-educativo/03-CONTEXT.md
@.planning/phases/03-classificador-de-deck-card-educativo/03-RESEARCH.md
@.planning/phases/03-classificador-de-deck-card-educativo/03-PATTERNS.md
@.planning/phases/03-classificador-de-deck-card-educativo/03-VALIDATION.md

<interfaces>
<!-- Extraído do codebase — usar diretamente, sem explorar. -->
runner.ts:
  export interface RunClaudeCliInput { systemPrompt: string; userMessage: string; model?: string }
  export function runClaudeCli({ systemPrompt, userMessage, model = 'sonnet' }: RunClaudeCliInput): Promise<string>
prompt-loader.ts:
  export function loadPrompt(nome: NomeCanonico): string   // allowlist inclui 'deck-classifier' e 'card-builder'
types.ts (server) Questao:
  { id: string; pergunta: string; resposta: string; tipo: 'extraida'|'criada'; pageStart?; pageEnd?; metadata?; deck?: string; tags?: string[]; mnemonico?; mnemonicoSvg? }
generation.ts molde:
  parseQuestoesJson(text): RawQuestao[]  — remove cercas ```, recorta primeiro { ao último }, JSON.parse, [] em falha
  generateAll(provider, chunks, opts, onProgress) — loop com try/catch POR-unidade, nunca re-throw
store.ts:
  export type JobEvent = {type:'progress';data:ChunkProgress} | {type:'done';data:{total;erros}} | {type:'error';data:{message}}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar enrich.ts (tipos, parsers, deveRodarEnrich, enrichAll)</name>
  <read_first>
    - ankinator-app/server/src/core/generation.ts (parseQuestoesJson ~57-72 = molde do parser tolerante; generateAll ~74-97 = molde do loop + isolamento de erro; uso de crypto.randomUUID para ids derivados)
    - ankinator-app/server/src/core/specialists/runner.ts (runClaudeCli — model default 'sonnet', D-04)
    - ankinator-app/server/src/core/specialists/prompt-loader.ts (loadPrompt — allowlist; carregar SEMPRE via loadPrompt, nunca inline)
    - ankinator-app/server/src/core/types.ts (Questao + campos deck?/tags? de SPEC-05; NÃO tocar Questao)
    - ankinator-app/server/src/core/specialists/enrich.test.ts (testes RED a tornar verde — nomes em VALIDATION.md)
    - .planning/phases/03-classificador-de-deck-card-educativo/03-PATTERNS.md secao enrich.ts (clones exatos)
  </read_first>
  <behavior>
    - parseClassificacoesJson com `{"classificacoes":[{"id":"abc","deck":"A::B","tags":["t1"]}]}` retorna `[{id:'abc',deck:'A::B',tags:['t1']}]`; cercas json toleradas; texto invalido retorna `[]`
    - parseSingleCard com `{"cards":[{"pergunta":"p1","resposta":"r1"},{"pergunta":"p2","resposta":"r2"}]}` retorna array de 2 elementos
    - deveRodarEnrich({classificar:false,cardBuilder:false}) === false; ({classificar:true,...}) === true
    - "split extraida": dado q.tipo==='extraida', enrichAll com cardBuilder=true mantem UM card e NAO altera pergunta nem cria filhos; so q.resposta pode mudar
    - "card-builder extraida verso": q.tipo==='extraida' produz pergunta/gabarito intactos, resposta substituida pelo verso ajustado
    - "enrichAll sequencia": com classificar=true E cardBuilder=true, roda classificador (preenche deck/tags) ANTES do card-builder; erro num card nao interrompe os demais
  </behavior>
  <action>Criar `ankinator-app/server/src/core/specialists/enrich.ts` (ESM, imports locais com `.js`). Exportar:
  (1) `interface EnrichOpts { classificar?: boolean; cardBuilder?: boolean }` e `interface EnrichProgress { estagio: 'classificando' | 'reescrevendo'; index: number; total: number; erro?: string }`.
  (2) `parseClassificacoesJson(text: string): RawClassificacao[]` onde `RawClassificacao = { id?: string; deck?: string; tags?: string[] }` — clone EXATO de parseQuestoesJson trocando a chave para `obj.classificacoes`. Discrição: extrair helper `parseJsonBlock<T>(text, key)` compartilhado é permitido desde que o build fique verde.
  (3) `parseSingleCard(text: string): { pergunta: string; resposta: string }[]` — mesmo padrão tolerante, lê `obj.cards` (formato `{"cards":[{pergunta,resposta}]}`); retorna `[]` em falha.
  (4) `deveRodarEnrich(opts: EnrichOpts): boolean` retornando `!!(opts.classificar || opts.cardBuilder)` (gate puro de PIPE-03 — importado pelo guard e por api.ts).
  (5) `enrichAll(questoes, opts, onProgress?): Promise<Questao[]>` espelhando generateAll: começa `let resultado = [...questoes]`.
    ESTÁGIO 1 (se `opts.classificar`): montar userMessage com `JSON.stringify` dos cards `{id,pergunta,resposta,tipo}` (NÃO interpolar `q.resposta`/`q.pergunta` cru — mitiga LLM injection); `runClaudeCli({ systemPrompt: loadPrompt('deck-classifier'), userMessage })` (model omitido → 'sonnet'); `parseClassificacoesJson` → merge por id via `const byId = new Map(classif.map(c=>[c.id,c]))` e para cada q `const c = byId.get(q.id); if (c) { q.deck = c.deck; q.tags = c.tags }` (classificações com id inexistente são ignoradas — defensivo, V5). Emitir `onProgress?.({estagio:'classificando', index:0, total:1})`; try/catch global que em falha emite `{estagio:'classificando', index:0, total:1, erro}` e segue com os cards crus.
    ESTÁGIO 2 (se `opts.cardBuilder`): loop por-card sobre um snapshot do array. Para cada q: `runClaudeCli({ systemPrompt: loadPrompt('card-builder'), userMessage: buildCardBuilderMessage(q) })`; `parseSingleCard`. Se `q.tipo === 'extraida'` empurra `{ ...q, resposta: parsed[0]?.resposta ?? q.resposta }` (D-10, nunca muda pergunta, nunca divide). Se `q.tipo === 'criada'` para cada card derivado empurra `{ ...q, id: crypto.randomUUID(), pergunta: d.pergunta, resposta: d.resposta }` herdando pageStart/pageEnd/tipo/deck/tags do original (D-09). try/catch POR-card: em falha emitir `{estagio:'reescrevendo', index:i, total, erro}` e empurrar o card ORIGINAL inalterado (nunca perde card, D-13). Emitir progresso `{estagio:'reescrevendo', index:i, total:N}` a cada card.
    Helper interno `buildCardBuilderMessage(q)` monta a entrada com tipo `[EXTRAÍDA]`/`[CRIADA]`, pergunta, resposta e referência de página (ver Code Examples do RESEARCH.md).
  NÃO tocar o tipo `Questao` (campos deck?/tags? já existem desde SPEC-05).</action>
  <verify>
    <automated>cd ankinator-app/server && npx vitest run -t "parseClassificacoesJson" -t "split extraida" -t "card-builder extraida verso" -t "enrichAll sequência" --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` no server passa os blocos `parseClassificacoesJson`, `split extraida`, `card-builder extraida verso`, `enrichAll sequência` (verde).
    - `enrich.ts` exporta `enrichAll`, `parseClassificacoesJson`, `parseSingleCard`, `deveRodarEnrich`, `EnrichOpts`, `EnrichProgress`.
    - O merge do classificador usa `Map` por `id` (grep `new Map` + `.get(q.id`) — NUNCA índice posicional.
    - O userMessage do classificador usa `JSON.stringify` (grep `JSON.stringify`) — nenhuma interpolação crua de `q.resposta`.
    - `tipo === 'extraida'` no estágio 2 produz exatamente 1 card e não altera `q.pergunta` (assertado no teste).
    - `loadPrompt('deck-classifier')` e `loadPrompt('card-builder')` aparecem em enrich.ts; nenhum system prompt inline.
  </acceptance_criteria>
  <done>enrich.ts implementado; unit de parsers/split/sequência verdes; isolamento de erro por-unidade; tipo Questao intacto.</done>
</task>

<task type="auto">
  <name>Task 2: Estender JobEvent (store.ts) + espelhar GenerateOptions (server + web)</name>
  <read_first>
    - ankinator-app/server/src/store.ts (JobEvent union ~35-38; jobStore.emit ~78-87 — confirmar que emit não precisa mudar)
    - ankinator-app/server/src/core/types.ts (GenerateOptions ~104-115)
    - ankinator-app/web/src/types.ts (GenerateOptions ~51-56 — espelho; Questao NÃO tocar)
    - ankinator-app/server/src/core/specialists/enrich.ts (EnrichProgress — fonte do tipo importado por store.ts)
    - .planning/phases/03-classificador-de-deck-card-educativo/03-PATTERNS.md secoes store.ts, types.ts e web/src/types.ts
  </read_first>
  <action>Em `ankinator-app/server/src/store.ts`: adicionar `import type { EnrichProgress } from './core/specialists/enrich.js'` e inserir na union `JobEvent` a variante `| { type: 'enrich-progress'; data: EnrichProgress }` posicionada ENTRE `progress` e `done` (D-02). NÃO mudar `jobStore.emit` (já é genérico sobre a union; só `progress` faz push em `job.progress`).
  Em `ankinator-app/server/src/core/types.ts`: adicionar a `GenerateOptions` exatamente dois campos opcionais — `classificar?: boolean` (comentário PT-BR: "Rodar especialista classificador de deck+tags após a geração. Default: true.") e `cardBuilder?: boolean` (comentário PT-BR: "Rodar especialista card-builder após a classificação. Default: false."). NÃO tocar `Questao`.
  Em `ankinator-app/web/src/types.ts`: adicionar os MESMOS dois campos `classificar?: boolean` e `cardBuilder?: boolean` a `GenerateOptions` — no MESMO COMMIT que a mudança server (Pitfall 1, tipo duplicado). NÃO tocar `Questao` no web. Adicionar TAMBÉM ao `web/src/types.ts` a interface ESPELHO `EnrichProgress` com os mesmos campos do server (`estagio: 'classificando' | 'reescrevendo'; index: number; total: number; erro?: string`) para o Plano 03 (App.tsx/ProgressPanel) consumir; se `ChunkProgress` ainda não estiver exportado no web, garantir que esteja.</action>
  <verify>
    <automated>cd ankinator-app/server && npx tsc --noEmit 2>&1 | head -10 && cd ../web && npx tsc --noEmit 2>&1 | head -10 && grep -q "enrich-progress" ../server/src/store.ts && grep -q "classificar" ../server/src/core/types.ts && grep -q "classificar" src/types.ts && echo "types mirrored OK"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passa SEM erros tanto no server quanto no web.
    - `store.ts` contém `| { type: 'enrich-progress'; data: EnrichProgress }` na union e importa `EnrichProgress` de enrich.ts.
    - `server/src/core/types.ts` e `web/src/types.ts` ambos contêm `classificar?: boolean` e `cardBuilder?: boolean` em `GenerateOptions` (espelho idêntico — Pitfall 1).
    - `web/src/types.ts` exporta `EnrichProgress`.
    - O tipo `Questao` NÃO foi modificado em nenhum dos dois arquivos (sem diff em linhas de `Questao`).
  </acceptance_criteria>
  <done>JobEvent estendido; GenerateOptions espelhado server+web no mesmo commit; EnrichProgress disponível no web; ambos builds verdes; Questao intacta.</done>
</task>

<task type="auto">
  <name>Task 3: Refinar deck-classifier.md e card-builder.md (formato de saída JSON)</name>
  <read_first>
    - ankinator-app/server/src/core/specialists/prompts/deck-classifier.md (secao Saída ~44-46 — hoje não pede `id` na saída)
    - ankinator-app/server/src/core/specialists/prompts/card-builder.md (secao Saída ~44-46 — hoje não especifica formato JSON do split)
    - .planning/phases/03-classificador-de-deck-card-educativo/03-RESEARCH.md secao Open Questions 1 e 2 (recomendações de formato)
    - .planning/phases/03-classificador-de-deck-card-educativo/03-PATTERNS.md secoes deck-classifier.md e card-builder.md (texto exato a inserir)
  </read_first>
  <action>Refinar APENAS a seção `## Saída` dos dois `.md` canônicos (fonte única — SPEC-02; as Skills referenciam por path, não duplicam, então não há cópia a sincronizar). Em PT-BR:
  `deck-classifier.md`: substituir a seção Saída para exigir JSON `{"classificacoes":[{"id":"<id-original-do-card>","deck":"<Matéria::Assunto::Subtópico>","tags":["tag1","tag2"]}]}`, instruindo a PRESERVAR o `id` exatamente como recebido e a NÃO alterar pergunta/resposta. Isto resolve a Open Question 1 (o parser casa por id — D-06).
  `card-builder.md`: substituir a seção Saída para exigir JSON `{"cards":[{"pergunta":"...","resposta":"..."}]}` — array de 1 elemento para card único, array de N para split; sempre incluir explicação curta + fonte (quando disponível) embutidas na `resposta` (D-11). Reforçar que cards `[EXTRAÍDA]` NÃO devem ser divididos e têm pergunta/gabarito preservados (D-09/D-10). Isto resolve a Open Question 2 (parseSingleCard lê `obj.cards`).
  NÃO mudar a allowlist do prompt-loader (os nomes já existem). NÃO duplicar conteúdo no código.</action>
  <verify>
    <automated>grep -q "classificacoes" ankinator-app/server/src/core/specialists/prompts/deck-classifier.md && grep -q "cards" ankinator-app/server/src/core/specialists/prompts/card-builder.md && cd ankinator-app/server && npx tsx -e "import {loadPrompt} from './src/core/specialists/prompt-loader.js'; const a=loadPrompt('deck-classifier'); const b=loadPrompt('card-builder'); if(!a.includes('classificacoes')||!b.includes('cards')) process.exit(1); console.log('prompts OK', a.length, b.length)"</automated>
  </verify>
  <acceptance_criteria>
    - `deck-classifier.md` contém a string `classificacoes` e instrui a preservar o `id` original na saída.
    - `card-builder.md` contém a string `cards` (formato `{"cards":[...]}`) e reforça que `[EXTRAÍDA]` não divide.
    - `loadPrompt('deck-classifier')` e `loadPrompt('card-builder')` carregam os `.md` refinados sem erro de allowlist (saída do tsx imprime os lengths).
    - Nenhum conteúdo de prompt foi copiado para código `.ts` (fonte única preservada).
  </acceptance_criteria>
  <done>Formato de saída JSON fixado nos dois `.md`; loadPrompt resolve ambos; alinhado a parseClassificacoesJson/parseSingleCard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM (claude CLI) → enrich.ts | Saída do modelo (deck/tags/cards) é entrada não confiável a ser parseada/mergeada |
| Card content (q.pergunta/q.resposta) → userMessage do LLM | Conteúdo derivado do PDF do usuário cruza para o prompt do especialista |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | id fabricado pelo LLM no parser de classificações | mitigate | Merge por `Map` com `byId.get(q.id)` — classificações com `id` inexistente em `questoes[]` são naturalmente ignoradas (defensivo, V5 parcial). |
| T-03-02 | Tampering | LLM injection via q.resposta/q.pergunta na userMessage | mitigate | Serializar o payload do classificador com `JSON.stringify` (não interpolar cru) — Pitfall mitigado no Task 1. |
| T-03-03 | Tampering | path traversal no loadPrompt | accept | Allowlist `NOMES` já existe em prompt-loader.ts; este plano NÃO adiciona nomes novos. Risco residual nulo. |

Nenhuma ameaça acima de MEDIUM. App local single-user, sem auth/sessão/crypto.
</threat_model>

<verification>
- `npx vitest run` (server) verde para parsers/split/verso/sequência.
- `npx tsx src/scripts/guard-enrich.ts` verde (deveRodarEnrich existe e gate correto) — fecha o RED do Plano 00.
- `npx tsc --noEmit` verde em server e web (tipos espelhados).
- loadPrompt resolve os dois `.md` refinados.
</verification>

<success_criteria>
- enrichAll encadeia classificador → card-builder com isolamento de erro por-unidade (PIPE-01).
- Classificação casa por id e preenche deck/tags (DECK-01/DECK-02 lado backend).
- Split só em `criada`, verso ajustado em `extraida` (CARD-01/CARD-02 lado backend).
- Tipos espelhados server+web no mesmo commit; Questao intacta.
</success_criteria>

<output>
Create `.planning/phases/03-classificador-de-deck-card-educativo/03-01-SUMMARY.md` when done
</output>
