---
phase: 03-classificador-de-deck-card-educativo
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - ankinator-app/server/src/api.ts
  - ankinator-app/server/src/core/exporters/csv.ts
  - ankinator-app/server/src/core/exporters/ankiconnect.ts
autonomous: true
requirements: [PIPE-01, PIPE-03, DECK-01, DECK-02]
user_setup: []

must_haves:
  truths:
    - "Com algum toggle ligado, o job roda generateAll() → enrichAll() em sequência no MESMO job e só emite done no fim"
    - "Com ambos toggles desligados, enrichAll NÃO é chamado e /generate fica equivalente ao atual (PIPE-03)"
    - "O CSV ganha coluna Deck + header #deck column:5 SÓ quando algum card tem q.deck; senão fica byte-idêntico ao atual"
    - "O AnkiConnect cria um subdeck por q.deck distinto e roteia deckName por-nota com fallback a opts.deck"
    - "Ambos exporters unem q.tags ao Set de tags sem duplicatas e sem quebrar quando q.tags é undefined"
  artifacts:
    - path: "ankinator-app/server/src/api.ts"
      provides: "Wiring de enrichAll encadeado no .then() de /generate com gate deveRodarEnrich"
      contains: "enrichAll"
    - path: "ankinator-app/server/src/core/exporters/csv.ts"
      provides: "Coluna Deck condicional + header #deck column + merge q.tags"
      contains: "#deck column"
    - path: "ankinator-app/server/src/core/exporters/ankiconnect.ts"
      provides: "deckName por-nota + createDeck por subdeck + merge q.tags"
      contains: "q.deck"
  key_links:
    - from: "ankinator-app/server/src/api.ts"
      to: "ankinator-app/server/src/core/specialists/enrich.ts"
      via: "import enrichAll/deveRodarEnrich + emit enrich-progress"
      pattern: "enrichAll\\("
    - from: "ankinator-app/server/src/core/exporters/csv.ts"
      to: "q.deck / q.tags"
      via: "coluna Deck + tagsDaQuestao merge"
      pattern: "q\\.deck"
---

<objective>
Plugar o backend de enrich no fluxo real: encadear `enrichAll()` no `.then()` de `/generate` (D-01) com o gate `deveRodarEnrich` (PIPE-03), emitindo `enrich-progress` no mesmo job/SSE; e tornar DECK-01/DECK-02 verificáveis ponta-a-ponta no export — coluna `Deck` + header `#deck column:5` no CSV (D-08), roteamento `deckName` por-nota + `createDeck` por subdeck no AnkiConnect (D-08), e merge de `q.tags` nos dois exporters (D-07).

Purpose: Sem este plano, enrichAll existe mas nunca roda, e o deck/tags classificados nunca chegam ao Anki. Aqui DECK-01/DECK-02 ficam verificáveis e PIPE-01/PIPE-03 fecham no caminho real.
Output: api.ts encadeando enrich com gate; exporters roteando deck e mergeando tags; guard-enrich.ts verde; unit de routing/csv column/tags merge verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-classificador-de-deck-card-educativo/03-CONTEXT.md
@.planning/phases/03-classificador-de-deck-card-educativo/03-RESEARCH.md
@.planning/phases/03-classificador-de-deck-card-educativo/03-PATTERNS.md
@.planning/phases/03-classificador-de-deck-card-educativo/03-VALIDATION.md

<interfaces>
<!-- Contratos do Plano 01 (já implementados) — consumir diretamente. -->
enrich.ts:
  export async function enrichAll(questoes: Questao[], opts: EnrichOpts, onProgress?: (e: EnrichProgress) => void): Promise<Questao[]>
  export function deveRodarEnrich(opts: EnrichOpts): boolean
  export interface EnrichOpts { classificar?: boolean; cardBuilder?: boolean }
csv.ts (estado atual): tagsDaQuestao(q, padrao): string (Set q.tipo + padrao + banca/ano); toAnkiCsv(questoes, opts) retorna '﻿' + stringify(...); CsvOptions { fonte?; tagsPadrao?; deck? }
ankiconnect.ts (estado atual): tagsDaQuestao(q, padrao): string[]; pushToAnki faz createDeck({deck:opts.deck}) + notes.map(deckName: opts.deck)
api.ts /generate (~129-161): monta genOptions; chama generateAll(...).then(result => {...emit done...})
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Encadear enrichAll no /generate com gate deveRodarEnrich (D-01/PIPE-03)</name>
  <read_first>
    - ankinator-app/server/src/api.ts (/generate ~107-207: construção de genOptions ~129-135; .then() ~148-162; SSE send + res.end em done/error ~198-206)
    - ankinator-app/server/src/core/specialists/enrich.ts (enrichAll, deveRodarEnrich, EnrichOpts — contratos do Plano 01)
    - ankinator-app/server/src/store.ts (jobStore.emit + variante enrich-progress do Plano 01)
    - .planning/phases/03-classificador-de-deck-card-educativo/03-PATTERNS.md secao api.ts (texto exato do .then async)
    - .planning/phases/03-classificador-de-deck-card-educativo/03-RESEARCH.md Pitfall 2 (enrich-progress não pode ser emitido depois do done)
  </read_first>
  <action>Em `ankinator-app/server/src/api.ts`: importar `enrichAll` e `deveRodarEnrich` de `'./core/specialists/enrich.js'`. Adicionar à montagem de `genOptions` os dois campos `classificar: options?.classificar` e `cardBuilder: options?.cardBuilder` (vindos de `req.body.options`). Trocar o callback `.then((result) => {...})` por `.then(async (result) => {...})`: dentro dele `let questoes = result.questoes;` e `const enrichOpts = { classificar: options?.classificar, cardBuilder: options?.cardBuilder };` — se `deveRodarEnrich(enrichOpts)` for true, `questoes = await enrichAll(questoes, enrichOpts, (e) => jobStore.emit(job, { type: 'enrich-progress', data: e }))`. Em seguida `job.result = result; job.questoes = questoes; job.status = 'done'; jobStore.emit(job, { type: 'done', data: { total: questoes.length, erros: result.erros } })`. CRÍTICO (Pitfall 2): o `await enrichAll(...)` DEVE completar ANTES de emitir `done` (o SSE fecha em done) — usar `async/await`, nunca fire-and-forget. NÃO tocar o bloco SSE `send`/`res.end` (já fecha em done/error). NÃO criar endpoint/job novo (D-01).</action>
  <verify>
    <automated>cd ankinator-app/server && npx tsc --noEmit 2>&1 | head -10 && npx tsx src/scripts/guard-enrich.ts 2>&1 | tail -5 && grep -q "deveRodarEnrich" src/api.ts && grep -q "enrich-progress" src/api.ts && echo "wiring OK"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` no server passa sem erros.
    - `npx tsx src/scripts/guard-enrich.ts` sai 0 (toggles off → deveRodarEnrich false; toggle on → true) — PIPE-03 guard verde.
    - `api.ts` importa e usa `deveRodarEnrich(enrichOpts)` como gate ANTES de `enrichAll`.
    - O `.then` é `async` e `enrichAll` é aguardado com `await` ANTES de `emit done` (grep confirma `await enrichAll`).
    - `api.ts` emite `{ type: 'enrich-progress', data: e }` dentro do callback de progresso de enrichAll.
    - Nenhum endpoint novo foi adicionado (grep não encontra rota `/enrich`).
  </acceptance_criteria>
  <done>enrichAll encadeado com gate; enrich-progress emitido antes de done; guard PIPE-03 verde; sem endpoint novo.</done>
</task>

<task type="auto">
  <name>Task 2: csv.ts — coluna Deck condicional + header #deck column + merge q.tags (D-07/D-08)</name>
  <read_first>
    - ankinator-app/server/src/core/exporters/csv.ts (tagsDaQuestao ~26-33; toAnkiCsv rows/columns ~50-76; retorno '﻿' + csv; CsvOptions; versoDaQuestao/fonteDaQuestao)
    - ankinator-app/server/src/core/specialists/enrich.test.ts (testes RED `tagsDaQuestao merge` e `csv deck column`)
    - .planning/phases/03-classificador-de-deck-card-educativo/03-PATTERNS.md secao csv.ts (extensão exata: temDeck, headerLines, ordem BOM)
    - .planning/phases/03-classificador-de-deck-card-educativo/03-RESEARCH.md Pitfalls 4, 6 e 7 (header #deck column:5, spread defensivo, ordem BOM)
  </read_first>
  <action>Em `ankinator-app/server/src/core/exporters/csv.ts`:
  (1) `tagsDaQuestao`: adicionar `...(q.tags ?? [])` ao `new Set([q.tipo, ...padrao, ...(q.tags ?? [])])` (D-07; `?? []` evita TypeError quando undefined — Pitfall 6). Manter banca/ano como estão.
  (2) `toAnkiCsv`: calcular `const temDeck = questoes.some((q) => q.deck)` e `const deckFallback = opts.deck ?? 'Ankinator'`. Quando `temDeck`, adicionar ao objeto de cada row o campo `deck: q.deck ?? deckFallback` e adicionar `{ key: 'deck', header: 'Deck' }` como 5ª coluna ao array `columns`; quando `!temDeck`, NÃO adicionar nem o campo nem a coluna (preserva byte-identidade — PIPE-03). Construir `const headerLines = temDeck ? '#separator:Semicolon\n#deck column:5\n' : ''` e retornar `'﻿' + headerLines + csvBody` (ordem BOM → headers → dados, Pitfall 7). O `#deck column:5` é obrigatório para o Anki rotear por-nota (Pitfall 4); o `5` é a posição 1-based de Deck.
  (3) Adicionar `deck?: string` à interface `CsvOptions` (fallback quando q.deck ausente).
  Para que os testes `tagsDaQuestao merge` e `csv deck column` (Plano 00) consigam importar, exportar `tagsDaQuestao` e garantir `toAnkiCsv` exportada — ou expor um helper testável; resolver os `it.todo` do scaffold transformando-os em testes reais.</action>
  <verify>
    <automated>cd ankinator-app/server && npx tsc --noEmit 2>&1 | head -10 && npx vitest run -t "tagsDaQuestao merge" -t "csv deck column" --reporter=verbose 2>&1 | tail -15 && grep -q "#deck column:5" src/core/exporters/csv.ts && grep -q "q.tags ?? \[\]" src/core/exporters/csv.ts && grep -q "export function tagsDaQuestao" src/core/exporters/csv.ts && echo "csv OK"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run -t "tagsDaQuestao merge"` e `-t "csv deck column"` passam (verde).
    - `csv.ts` contém `...(q.tags ?? [])` no Set de `tagsDaQuestao` (merge defensivo).
    - `csv.ts` exporta `tagsDaQuestao` para o teste: `grep -q "export function tagsDaQuestao" src/core/exporters/csv.ts` confirma o keyword `export` (fecha o gap entre o requisito de export e o teste).
    - Quando algum card tem `q.deck`, o CSV inclui a string `#deck column:5` e a coluna `Deck`; quando nenhum tem, o CSV NÃO inclui `#deck column` nem a coluna Deck (assertado no teste — não-regressão PIPE-03).
    - A ordem do retorno é BOM (`﻿`) + headerLines + corpo CSV.
    - `CsvOptions` tem `deck?: string`.
  </acceptance_criteria>
  <done>Coluna Deck e header condicionais; merge de tags defensivo; byte-identidade preservada quando sem deck.</done>
</task>

<task type="auto">
  <name>Task 3: ankiconnect.ts — deckName por-nota + createDeck por subdeck + merge q.tags (D-07/D-08)</name>
  <read_first>
    - ankinator-app/server/src/core/exporters/ankiconnect.ts (tagsDaQuestao ~70-75; pushToAnki createDeck + notes.map ~95-124; versoHtml ~77-88; PushResult)
    - ankinator-app/server/src/core/specialists/enrich.test.ts (teste RED `ankiconnect routing`)
    - .planning/phases/03-classificador-de-deck-card-educativo/03-PATTERNS.md secao ankiconnect.ts (extensão exata: subDecks Set, deckName por-nota)
    - .planning/phases/03-classificador-de-deck-card-educativo/03-RESEARCH.md Research flag 2 + Assumption A1 (createDeck explícito antes de addNotes p/ garantir subdeck)
  </read_first>
  <action>Em `ankinator-app/server/src/core/exporters/ankiconnect.ts`:
  (1) `tagsDaQuestao`: adicionar `...(q.tags ?? [])` ao `new Set([`ankinator`, q.tipo, ...padrao, ...(q.tags ?? [])])` (D-07, mesmo padrão do csv; Pitfall 6).
  (2) `pushToAnki`: trocar o único `createDeck({deck: opts.deck})` por um loop sobre os subdecks distintos — `const subDecks = new Set(questoes.map((q) => q.deck ?? opts.deck)); for (const d of subDecks) await invoke('createDeck', { deck: d }, url);` (createDeck explícito antes de addNotes garante o subdeck mesmo se `addNotes` não o criar automaticamente — Assumption A1). No `notes.map`, trocar `deckName: opts.deck` por `deckName: q.deck ?? opts.deck` (roteamento por-nota com fallback — D-08; fallback preserva fluxo atual quando q.deck undefined → não-regressão PIPE-03). NÃO mudar `modelName`/`fields`/`options`/`versoHtml`. `PushResult.deck` pode permanecer reportando `opts.deck` como deck principal (discrição) ou listar os subdecks — escolher uma e documentar no SUMMARY.
  Resolver o `it.todo` `ankiconnect routing` do scaffold em teste real que asserte que, dado q.deck='A::B', a nota gerada tem `deckName: 'A::B'`, e dado q.deck undefined, `deckName: opts.deck`.</action>
  <verify>
    <automated>cd ankinator-app/server && npx tsc --noEmit 2>&1 | head -10 && npx vitest run -t "ankiconnect routing" --reporter=verbose 2>&1 | tail -15 && grep -q "q.deck ?? opts.deck" src/core/exporters/ankiconnect.ts && grep -q "q.tags ?? \[\]" src/core/exporters/ankiconnect.ts && echo "ankiconnect OK"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run -t "ankiconnect routing"` passa (verde): q.deck preenchido → `deckName` igual a q.deck; q.deck undefined → `deckName` igual a opts.deck.
    - `ankiconnect.ts` cria um deck por `q.deck ?? opts.deck` distinto (grep `new Set(questoes.map`).
    - `ankiconnect.ts` contém `...(q.tags ?? [])` no Set de tags.
    - `npx tsc --noEmit` no server passa sem erros.
  </acceptance_criteria>
  <done>deckName roteado por-nota com fallback; subdecks criados; tags mergeadas; routing unit verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /generate (req.body.options) | Toggles classificar/cardBuilder vêm do cliente local |
| q.deck → createDeck/deckName (AnkiConnect) | String de deck (possivelmente com `::`) cruza para a API local do Anki |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | q.deck path-like (ex.: `..`) em createDeck/deckName | accept | AnkiConnect é local single-user; o Anki trata deckName como string de apresentação, não como path de filesystem. Risco baixo (RESEARCH §Security Domain). |
| T-03-05 | Information Disclosure | coluna Deck/headers vazam estrutura | accept | App local; CSV é gerado para o próprio usuário. Sem dado sensível. |

Nenhuma ameaça acima de MEDIUM introduzida por este plano. Mudanças aditivas com fallback preservando o fluxo atual.
</threat_model>

<verification>
- `npx tsx src/scripts/guard-enrich.ts` verde (PIPE-03 gate).
- `npx vitest run -t "tagsDaQuestao merge" -t "csv deck column" -t "ankiconnect routing"` verdes.
- `npx tsc --noEmit` (server) verde.
- Sem deck: CSV byte-idêntico ao atual (assertado).
</verification>

<success_criteria>
- enrichAll roda encadeado quando toggle ligado; não roda quando off (PIPE-01/PIPE-03).
- Deck hierárquico chega ao AnkiConnect e ao CSV (DECK-01 verificável).
- Tags do classificador chegam aos dois exporters sem duplicata (DECK-02 verificável).
</success_criteria>

<output>
Create `.planning/phases/03-classificador-de-deck-card-educativo/03-02-SUMMARY.md` when done
</output>
