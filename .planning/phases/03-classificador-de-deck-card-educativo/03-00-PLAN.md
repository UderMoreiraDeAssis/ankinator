---
phase: 03-classificador-de-deck-card-educativo
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - ankinator-app/server/package.json
  - ankinator-app/package-lock.json
  - ankinator-app/server/src/core/specialists/enrich.test.ts
  - ankinator-app/server/src/scripts/guard-enrich.ts
  - ankinator-app/server/src/scripts/smoke-enrich.ts
autonomous: false
requirements: [PIPE-03]
user_setup: []

must_haves:
  truths:
    - "vitest roda no workspace server e o comando `npx vitest run` executa sem erro de config"
    - "Os arquivos de teste/guard existem como scaffolding RED (referenciam funções de enrich.ts que ainda não existem ou stubs explícitos)"
    - "O guard CLI-free de PIPE-03 existe e roda sem spawnar o claude CLI"
  artifacts:
    - path: "ankinator-app/server/src/core/specialists/enrich.test.ts"
      provides: "Suite unit vitest cobrindo parseClassificacoesJson, tags merge, split, verso, sequência, routing, csv column"
      contains: "describe"
    - path: "ankinator-app/server/src/scripts/guard-enrich.ts"
      provides: "Guard CLI-free de não-regressão PIPE-03 (gate deveRodarEnrich)"
      contains: "deveRodarEnrich"
    - path: "ankinator-app/server/src/scripts/smoke-enrich.ts"
      provides: "Smoke gated opt-in (clone de smoke-runner.ts)"
      contains: "--assert-args"
  key_links:
    - from: "ankinator-app/server/package.json"
      to: "vitest"
      via: "devDependencies"
      pattern: "\"vitest\""
---

<objective>
Estabelecer a infraestrutura de testes da fase (Wave 0) antes de qualquer implementação: instalar `vitest` como devDependency no workspace `server`, criar o scaffolding RED dos testes unit (`enrich.test.ts`), o guard CLI-free de não-regressão PIPE-03 (`guard-enrich.ts`), e o smoke gated (`smoke-enrich.ts`).

Purpose: O projeto não tem test runner hoje (TESTING.md baseline "sem testes automatizados"). Nyquist exige que cada `<verify>` tenha `<automated>`; este plano cria o `MISSING` scaffold que todos os planos seguintes consomem. O guard PIPE-03 é o gate de não-regressão obrigatório.
Output: vitest instalado; 3 arquivos de scaffold criados; `npx vitest run` executa (mesmo que vermelho, por design RED).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-classificador-de-deck-card-educativo/03-CONTEXT.md
@.planning/phases/03-classificador-de-deck-card-educativo/03-RESEARCH.md
@.planning/phases/03-classificador-de-deck-card-educativo/03-PATTERNS.md
@.planning/phases/03-classificador-de-deck-card-educativo/03-VALIDATION.md

<interfaces>
<!-- Contratos que os testes RED referenciam. Implementação real vem no Plano 01. -->
De enrich.ts (a ser criado no Plano 01):
- `export function parseClassificacoesJson(text: string): RawClassificacao[]` — `RawClassificacao = { id?: string; deck?: string; tags?: string[] }`
- `export function parseSingleCard(text: string): { pergunta: string; resposta: string }[]`
- `export async function enrichAll(questoes: Questao[], opts: EnrichOpts, onProgress?: (e: EnrichProgress) => void): Promise<Questao[]>`
- `export interface EnrichOpts { classificar?: boolean; cardBuilder?: boolean }`
- `export interface EnrichProgress { estagio: 'classificando'|'reescrevendo'; index: number; total: number; erro?: string }`
- `export function deveRodarEnrich(opts: EnrichOpts): boolean` — gate puro: `!!(opts.classificar || opts.cardBuilder)`

De tagsDaQuestao (exporters, Plano 02): funções `tagsDaQuestao(q, padrao)` em csv.ts e ankiconnect.ts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar vitest como devDependency no workspace server</name>
  <files>ankinator-app/server/package.json, ankinator-app/package-lock.json</files>
  <read_first>
    - ankinator-app/server/package.json (estado atual de scripts/deps — confirmar que não há test runner)
    - ankinator-app/package.json (workspaces hoisting; lockfile único no root)
    - .planning/codebase/TESTING.md (baseline "sem test runner")
    - .planning/phases/03-classificador-de-deck-card-educativo/03-RESEARCH.md secao "Package Legitimacy Audit" (vitest 4.1.8 [ASSUMED OK])
  </read_first>
  <action>Rodar `npm install --save-dev vitest --workspace server` a partir de `ankinator-app/` (root do workspace, lockfile hoisted — conforme decisão Phase 01 P01: `npm ci`/install no ROOT, não per-workspace). Adicionar dois scripts ao `ankinator-app/server/package.json`: `"test": "vitest run"` e `"test:guard": "tsx src/scripts/guard-enrich.ts"`. Confirmar a versão instalada com `npm view vitest version` ANTES de instalar (esperado `4.1.8`; pacote `[ASSUMED OK]` no research — maintainer vitest-dev). NÃO criar `vitest.config.ts` salvo se a execução de `npx vitest run` falhar por falta de config (zero-config esperado com Node 24 ESM, Assumption A3 do research). NÃO instalar nenhum outro pacote.</action>
  <verify>
    <automated>cd ankinator-app/server && npx vitest run --reporter=verbose 2>&1 | head -20; node -e "const p=require('./package.json'); if(!p.devDependencies?.vitest) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `ankinator-app/server/package.json` tem `devDependencies.vitest` com versão começando em `4.` (esperado `4.1.8`).
    - `ankinator-app/package-lock.json` contém entrada para `vitest`.
    - `npx vitest run` no workspace server executa o runner (pode terminar vermelho por testes RED, mas NÃO pode falhar com "command not found" nem erro de config/ESM).
    - `package.json` server tem scripts `test` e `test:guard`.
  </acceptance_criteria>
  <done>vitest instalado e invocável; scripts adicionados; nenhum outro pacote tocado.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold RED de enrich.test.ts + guard-enrich.ts + smoke-enrich.ts</name>
  <files>ankinator-app/server/src/core/specialists/enrich.test.ts, ankinator-app/server/src/scripts/guard-enrich.ts, ankinator-app/server/src/scripts/smoke-enrich.ts</files>
  <read_first>
    - ankinator-app/server/src/core/generation.ts (parseQuestoesJson linhas ~57-72 e generateAll ~74-97 — molde dos parsers e do loop; tipo Questao/RawQuestao)
    - ankinator-app/server/src/scripts/guard-default-loader.ts (molde EXATO do guard CLI-free: função `fail`, cenários numerados, flag `--assert`, `process.exit`)
    - ankinator-app/server/src/scripts/smoke-runner.ts (molde do smoke gated: 3 passos, flag `--assert-args`, imports de runner/prompt-loader)
    - ankinator-app/server/src/core/types.ts (Questao: campos id/pergunta/resposta/tipo/pageStart/pageEnd/deck?/tags?)
    - .planning/phases/03-classificador-de-deck-card-educativo/03-VALIDATION.md secao "Per-Task Verification Map" (lista exata dos -t "..." que cada teste deve ter)
    - .planning/phases/03-classificador-de-deck-card-educativo/03-PATTERNS.md secao "enrich.test.ts" (padrão vitest)
  </read_first>
  <action>Criar `ankinator-app/server/src/core/specialists/enrich.test.ts` com `import { describe, it, expect } from 'vitest'` e importando de `'./enrich.js'`. Escrever os `describe`/`it` cujos nomes batem EXATAMENTE com os `-t` da VALIDATION.md Per-Task Verification Map: blocos contendo as strings `parseClassificacoesJson`, `tagsDaQuestao merge`, `split extraida`, `card-builder extraida verso`, `enrichAll sequência`, `ankiconnect routing`, `csv deck column`. Cada teste deve declarar o comportamento esperado com `expect(...)` concreto (ex.: `parseClassificacoesJson` com `{"classificacoes":[{"id":"abc","deck":"A::B","tags":["t1"]}]}` toEqual `[{id:'abc',deck:'A::B',tags:['t1']}]`; tolerar cercas json devolvendo `[]` para `{"classificacoes":[]}`). Estes testes ficam RED até os Planos 01/02 implementarem as funções — é o estado RED esperado de Wave 0. Para os testes de exporter (`tagsDaQuestao merge`, `ankiconnect routing`, `csv deck column`) importar de `'../exporters/csv.js'` e `'../exporters/ankiconnect.js'` — se essas funções forem privadas hoje, marcar o teste com `it.todo(...)` e deixar comentário `// TODO(Plano 02): exportar tagsDaQuestao/toAnkiCsv para teste` (NÃO exportar nada agora; o Plano 02 decide o que expor).

Criar `ankinator-app/server/src/scripts/guard-enrich.ts` clonando a estrutura de `guard-default-loader.ts`: header JSDoc explicando que prova PIPE-03 (toggles off → `deveRodarEnrich` retorna false → `enrichAll` não é chamado), função `fail(msg): never` com `process.exit(1)`, e dois cenários: Cenário 1 `deveRodarEnrich({classificar:false,cardBuilder:false})` deve ser `false` (senão `fail`), Cenário 2 `deveRodarEnrich({classificar:true,cardBuilder:false})` deve ser `true`. Importar `deveRodarEnrich` de `'../core/specialists/enrich.js'`. Flag `--assert` imprime OK e `process.exit(0)`. Como `enrich.ts` ainda não existe, este guard fica RED até o Plano 01.

Criar `ankinator-app/server/src/scripts/smoke-enrich.ts` clonando `smoke-runner.ts`: PASSO 1 `loadPrompt('deck-classifier')` + `loadPrompt('card-builder')` (valida resolução .md cross-mode); PASSO 2 `parseClassificacoesJson` com fixture (CLI-free); PASSO 3 `enrichAll` real com PDF/cards reais (exercita o CLI — só executa quando SEM `--assert-args`). Flag `--assert-args` roda só passos 1+2 e sai 0. Todos os imports locais com extensão `.js` (ESM).</action>
  <verify>
    <automated>test -f ankinator-app/server/src/core/specialists/enrich.test.ts && test -f ankinator-app/server/src/scripts/guard-enrich.ts && test -f ankinator-app/server/src/scripts/smoke-enrich.ts && grep -q "deveRodarEnrich" ankinator-app/server/src/scripts/guard-enrich.ts && grep -q -- "--assert-args" ankinator-app/server/src/scripts/smoke-enrich.ts && for t in parseClassificacoesJson "tagsDaQuestao merge" "split extraida" "card-builder extraida verso" "enrichAll sequência" "ankiconnect routing" "csv deck column"; do grep -qF "$t" ankinator-app/server/src/core/specialists/enrich.test.ts || { echo "MISSING test: $t"; exit 1; }; done; echo "scaffold OK"</automated>
  </verify>
  <acceptance_criteria>
    - `enrich.test.ts` contém, como literais de string em nomes de `describe`/`it`, TODOS os 7 termos: `parseClassificacoesJson`, `tagsDaQuestao merge`, `split extraida`, `card-builder extraida verso`, `enrichAll sequência`, `ankiconnect routing`, `csv deck column`.
    - `guard-enrich.ts` importa `deveRodarEnrich` de `'../core/specialists/enrich.js'` e contém função `fail` + flag `--assert`.
    - `smoke-enrich.ts` contém a flag `--assert-args` e chama `loadPrompt('deck-classifier')` e `loadPrompt('card-builder')`.
    - Todos os imports locais usam extensão `.js`.
    - É ACEITÁVEL que `npx vitest run` e `npx tsx src/scripts/guard-enrich.ts` falhem (RED) porque `enrich.ts` ainda não existe — isto é o estado esperado de Wave 0.
  </acceptance_criteria>
  <done>Três arquivos de scaffold criados; testes nomeados conforme o contrato de cobertura da VALIDATION.md; estado RED documentado no SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Checkpoint — verificar legitimidade do pacote vitest</name>
  <action>Checkpoint de legitimidade de dependência (não auto-aprovável). Pausar para o humano confirmar que `vitest` é um pacote legítimo antes de aceitar o lockfile.</action>
  <what-built>Instalação de `vitest` (devDependency, workspace server) — pacote de terceiros via npm.</what-built>
  <how-to-verify>
    O pacote `vitest` foi marcado `[ASSUMED OK]` no research (slopcheck indisponível no ambiente). Confirme a legitimidade antes de aceitar o lockfile:
    1. Abra https://www.npmjs.com/package/vitest e confirme: maintainer/org `vitest-dev`, repositório github.com/vitest-dev/vitest, downloads na casa de milhões/semana, última publicação recente.
    2. Confirme que a versão instalada (`npm ls vitest --workspace server`) é uma release estável da linha 4.x.
    3. Se algo destoar (typosquat, pacote novo/sem downloads, repo divergente), responda com o problema; caso contrário aprove.
  </how-to-verify>
  <resume-signal>Digite "approved" para aceitar a dependência, ou descreva o problema encontrado.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → workspace | Pacote de terceiros (`vitest`) entra na árvore de dependências |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | npm install `vitest` | mitigate | Pacote `[ASSUMED OK]` (slopcheck bloqueado no ambiente) → checkpoint humano blocking verifica npmjs.com/package/vitest (maintainer vitest-dev, downloads, repo) antes de aceitar o lockfile. Legitimacy checkpoint nunca auto-aprovável. |

Nenhuma ameaça acima de MEDIUM introduzida por este plano (apenas scaffolding de teste + 1 devDependency estabelecida).
</threat_model>

<verification>
- `npx vitest run` executa o runner (não falha por config/command-not-found).
- Os 3 arquivos de scaffold existem com os nomes/flags corretos.
- O checkpoint de legitimidade de `vitest` foi aprovado pelo humano.
- RED esperado: testes/guard vermelhos enquanto `enrich.ts` não existe (Plano 01 fecha).
</verification>

<success_criteria>
- vitest instalado e invocável no workspace server.
- enrich.test.ts cobre os 7 termos do contrato VALIDATION.md.
- guard-enrich.ts e smoke-enrich.ts criados como clones dos moldes existentes.
- Dependência de terceiros aprovada via checkpoint humano.
</success_criteria>

<output>
Create `.planning/phases/03-classificador-de-deck-card-educativo/03-00-SUMMARY.md` when done
</output>
