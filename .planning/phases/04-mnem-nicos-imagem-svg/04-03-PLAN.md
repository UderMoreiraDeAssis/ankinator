---
phase: 04-mnem-nicos-imagem-svg
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - ankinator-app/server/src/core/exporters/csv.ts
  - ankinator-app/server/src/core/exporters/ankiconnect.ts
  - ankinator-app/server/src/core/exporters/exporters-embed.test.ts
  - ankinator-app/server/src/api.ts
  - ankinator-app/server/src/scripts/guard-enrich.ts
  - ankinator-app/server/src/scripts/smoke-enrich.ts
autonomous: false
requirements: [IMG-03, MNEM-01]
user_setup: []

must_haves:
  truths:
    - "O verso exportado (CSV e AnkiConnect) anexa o mnemônico-texto e o <svg> inline ao fim, com o MESMO mecanismo nos dois exporters; o SVG NUNCA passa por escapeHtml."
    - "Quando q.mnemonico e q.mnemonicoSvg estão ausentes, a saída é byte-idêntica à da Phase 3 (PIPE-03)."
    - "O guard CLI-free prova que deveRodarEnrich cobre os 4 toggles e que tudo-off ⇒ byte-idêntico; o smoke gated valida o render SVG real no Anki (opt-in)."
    - "A rota /generate propaga mnemonico (default ON) e imagem (default OFF) ao enrichAll (D-12)."
  artifacts:
    - path: "ankinator-app/server/src/core/exporters/csv.ts"
      provides: "versoDaQuestao anexa mnemônico-texto + SVG inline com gate por campo opcional"
      contains: "mnemonicoSvg"
    - path: "ankinator-app/server/src/core/exporters/ankiconnect.ts"
      provides: "versoHtml (EXPORTADO) anexa mnemônico-texto (escapado) + SVG inline (NÃO escapado)"
      contains: "mnemonicoSvg"
      exports: ["versoHtml"]
    - path: "ankinator-app/server/src/core/exporters/exporters-embed.test.ts"
      provides: "Testes de embed: SVG presente → verso contém <svg; ausente → byte-idêntico; SVG não escapado; mnemônico-texto escapado (versoHtml positivo)"
      min_lines: 40
    - path: "ankinator-app/server/src/api.ts"
      provides: "genOptions + enrichOpts incluem mnemonico (default true) e imagem (default false)"
      contains: "mnemonico"
  key_links:
    - from: "ankinator-app/server/src/api.ts"
      to: "enrichAll"
      via: "enrichOpts com mnemonico/imagem (D-12 defaults)"
      pattern: "imagem"
    - from: "ankinator-app/server/src/core/exporters/csv.ts"
      to: "q.mnemonicoSvg"
      via: "concatenação inline sem escapeHtml"
      pattern: "q.mnemonicoSvg"
---

<objective>
Fechar o caminho de export (IMG-03): anexar o mnemônico-texto e o `<svg>` sanitizado inline ao fim do verso, com o MESMO mecanismo em `csv.ts` (`versoDaQuestao`) e `ankiconnect.ts` (`versoHtml`), preservando byte-identidade quando os campos estão ausentes (PIPE-03/D-10). Wirar a rota `/generate` (api.ts) para propagar os defaults D-12 (mnemônico ON, imagem OFF). Estender o guard CLI-free e o smoke gated (D-14) para cobrir os novos toggles e o passo de SVG real. Provar o embed com testes determinísticos em arquivo dedicado.

Purpose: o valor da fase só chega ao usuário quando o SVG renderiza no card. Este plano é o "último metro" — inline, sem escapeHtml (Pitfall 2), com gate de byte-identidade que mantém PIPE-03 trivialmente verdadeiro.
Output: exporters estendidos; api.ts com defaults D-12; guard/smoke estendidos; testes de embed verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-mnem-nicos-imagem-svg/04-CONTEXT.md
@.planning/phases/04-mnem-nicos-imagem-svg/04-RESEARCH.md
@.planning/phases/04-mnem-nicos-imagem-svg/04-PATTERNS.md
@.planning/phases/04-mnem-nicos-imagem-svg/04-02-SUMMARY.md

<interfaces>
<!-- Contratos reais do codebase. NÃO explorar. -->

csv.ts (estado atual):
  function versoDaQuestao(q: Questao): string   // monta q.resposta + metadata (gabarito/alternativas/banca/ano); retorna verso
  gate de byte-identidade já usado: `const temDeck = questoes.some(q => q.deck)` → coluna Deck só quando presente

ankiconnect.ts (estado atual):
  function versoHtml(q: Questao, fonte?: string): string   // back = escapeHtml(q.resposta)... alternativas/gabarito/fonte; retorna back. ESTE PLANO a torna EXPORTADA (export aditivo) para teste positivo de IMG-03.
  function escapeHtml(s: string): string   // & < > → entidades. NUNCA aplicar ao SVG (Pitfall 2)
  modelName: 'Basic'   // D-11: NÃO mudar; embed é inline no campo Back

api.ts (estado atual, ~linhas 130-164):
  genOptions: GenerateOptions = { maxPerChunk, incluirExtraidas, incluirCriadas, tags, model, classificar, cardBuilder }
  enrichOpts = { classificar: options?.classificar ?? true, cardBuilder: options?.cardBuilder ?? false }
  if (deveRodarEnrich(enrichOpts)) questoes = await enrichAll(questoes, enrichOpts, e => jobStore.emit(job, {type:'enrich-progress', data:e}))

guard-enrich.ts: cenários com `deveRodarEnrich(opts) !== false/true` + `fail(msg)`; flag `--assert` sai 0.
smoke-enrich.ts: passos CLI-free (loadPrompt, parse*) + flag `--assert-args` sai 0 antes do passo gated.

Ordem de montagem do verso (RESEARCH Padrão 3): resposta + metadata + mnemônico-texto + SVG inline.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: embed mnemônico+SVG nos dois exporters + exportar versoHtml + testes de embed dedicados</name>
  <files>ankinator-app/server/src/core/exporters/csv.ts, ankinator-app/server/src/core/exporters/ankiconnect.ts, ankinator-app/server/src/core/exporters/exporters-embed.test.ts</files>
  <read_first>
    - ankinator-app/server/src/core/exporters/csv.ts (linhas 37-49: versoDaQuestao; 57-66: gate temDeck — padrão de byte-identidade)
    - ankinator-app/server/src/core/exporters/ankiconnect.ts (linhas 77-92: versoHtml + escapeHtml — confirmar a assinatura `function versoHtml(q, fonte?)` para prefixar `export`; linhas 113-122: notes modelName 'Basic')
    - .planning/phases/04-mnem-nicos-imagem-svg/04-PATTERNS.md (linhas 390-486: extensões exatas com texto-alvo; linhas 819-826: gate byte-identidade)
    - .planning/phases/04-mnem-nicos-imagem-svg/04-RESEARCH.md (Padrão 3 linhas 324-368; Pitfall 2 linhas 488-492)
  </read_first>
  <behavior>
    - csv: card com q.mnemonico='M' e q.mnemonicoSvg='<svg>...</svg>' → o campo verso contém 'Mnemônico: M' e a substring '<svg' (não escapada).
    - csv: card sem mnemonico/mnemonicoSvg → `toAnkiCsv([card])` byte-idêntico ao baseline da Phase 3 (sem nenhuma linha de mnemônico).
    - ankiconnect (versoHtml POSITIVO — IMG-03): versoHtml de card com q.mnemonicoSvg contém a substring literal '<svg' (NÃO '&lt;svg').
    - ankiconnect (versoHtml POSITIVO — escape do texto): versoHtml de card cujo q.mnemonico contém um '<' literal → o verso contém a forma escapada '&lt;' do texto do mnemônico (escapeHtml aplicado só ao texto), e NÃO o '<' cru daquele texto.
    - ankiconnect: card sem campos → versoHtml byte-idêntico ao baseline.
  </behavior>
  <action>
    Em `csv.ts`, no fim de `versoDaQuestao`, ANTES de `return verso`, adicionar dois gates por campo opcional: `if (q.mnemonico) { verso += '\n\n💡 Mnemônico: ' + q.mnemonico; }` e `if (q.mnemonicoSvg) { verso += '\n\n' + q.mnemonicoSvg; }` — o SVG entra cru (já sanitizado no Plano 02), NUNCA escapado. Em `ankiconnect.ts`: (1) tornar a função `versoHtml` EXPORTADA — mudança ADITIVA, prefixar a declaração existente com `export` (`export function versoHtml(q: Questao, fonte?: string): string`) para permitir o teste positivo determinístico de IMG-03; NÃO alterar a assinatura nem o corpo já existente além do embed abaixo. (2) no fim de `versoHtml`, ANTES de `return back`, adicionar: `if (q.mnemonico) { back += '<br><br><b>💡 Mnemônico:</b> ' + escapeHtml(q.mnemonico); }` (texto escapado) e `if (q.mnemonicoSvg) { back += '<br><br>' + q.mnemonicoSvg; }` (SVG NÃO escapado — Pitfall 2). NÃO alterar `modelName: 'Basic'` (D-11). Criar `exporters-embed.test.ts` (arquivo dedicado para não colidir com enrich.test.ts) importando `{ toAnkiCsv } from './csv.js'` E `{ versoHtml } from './ankiconnect.js'`. Os testes cobrem, no mínimo: (CSV) SVG presente → '<svg' no verso; CSV ausente → byte-identidade (capturar baseline string e comparar); (versoHtml POSITIVO) card com q.mnemonicoSvg='<svg ...></svg>' → o retorno de `versoHtml` contém a substring literal `<svg` e NÃO contém `&lt;svg`; (versoHtml POSITIVO) card cujo q.mnemonico contém um `<` (ex.: `'a < b'`) → o retorno contém `a &lt; b` (texto escapado) e NÃO o `<` cru desse texto; (versoHtml byte-identidade) card sem campos → retorno idêntico ao baseline. Comentar com D-09/D-10/D-11 e Pitfall 2.
  </action>
  <verify>
    <automated>cd ankinator-app/server && npm test -- exporters-embed</automated>
  </verify>
  <acceptance_criteria>
    - `csv.ts` contém `q.mnemonicoSvg` e `q.mnemonico`; o SVG é concatenado SEM chamada a escapeHtml.
    - `ankiconnect.ts` contém `export function versoHtml` (função tornada exportada — mudança aditiva), `q.mnemonicoSvg` concatenado SEM escapeHtml e `q.mnemonico` COM escapeHtml; `modelName: 'Basic'` intacto.
    - `exporters-embed.test.ts` existe e importa `versoHtml` de `./ankiconnect.js`; `npm test -- exporters-embed` sai 0.
    - Existe um teste de byte-identidade (verso de card sem campos == baseline) e um teste de '<svg' presente no CSV.
    - TESTE POSITIVO de versoHtml (IMG-03): existe um `it` que afirma que `versoHtml(cardComSvg)` contém a substring literal `<svg` (e NÃO `&lt;svg`); e um `it` que afirma que `versoHtml(cardComMnemonicoContendo'<')` produz `&lt;` (texto escapado). `cd ankinator-app/server && npm test -- exporters-embed` sai 0 com esses asserts.
    - `grep -c "escapeHtml(q.mnemonicoSvg" ankinator-app/server/src/core/exporters/ankiconnect.ts` retorna 0 (SVG nunca escapado).
  </acceptance_criteria>
  <done>SVG e mnemônico-texto embutidos inline nos dois exporters com o mesmo mecanismo; versoHtml exportado e coberto por teste positivo (SVG cru + texto escapado); byte-identidade preservada; SVG nunca escapado.</done>
</task>

<task type="auto">
  <name>Task 2: wiring da rota /generate (defaults D-12) + guard + smoke estendidos</name>
  <files>ankinator-app/server/src/api.ts, ankinator-app/server/src/scripts/guard-enrich.ts, ankinator-app/server/src/scripts/smoke-enrich.ts</files>
  <read_first>
    - ankinator-app/server/src/api.ts (linhas 130-164: genOptions + enrichOpts + chamada enrichAll)
    - ankinator-app/server/src/scripts/guard-enrich.ts (linhas 29-51: cenários + fail + flag --assert)
    - ankinator-app/server/src/scripts/smoke-enrich.ts (linhas 24-58: passos CLI-free + flag --assert-args; linha 60+: passo gated)
    - .planning/phases/04-mnem-nicos-imagem-svg/04-PATTERNS.md (linhas 508-571: extensões de guard e smoke)
    - .planning/phases/04-mnem-nicos-imagem-svg/04-RESEARCH.md (Exemplo 3 linhas 606-620; § Validation Architecture Camadas 2-3 linhas 708-752)
  </read_first>
  <action>
    Em `api.ts`: adicionar `mnemonico: options?.mnemonico` e `imagem: options?.imagem` ao objeto `genOptions` (consistência de tipo — exige que `GenerateOptions` do server tenha esses campos; ver Task 3). No objeto `enrichOpts`, adicionar `mnemonico: options?.mnemonico ?? true` (default ON, D-12) e `imagem: options?.imagem ?? false` (default OFF, D-12). NÃO mudar a lógica de `deveRodarEnrich`/`enrichAll` (já ampliados no Plano 02). Em `guard-enrich.ts`: adicionar 3 cenários espelhando o padrão existente — Cenário 3: `deveRodarEnrich({classificar:false,cardBuilder:false,mnemonico:true,imagem:false})` deve ser true (senão `fail`); Cenário 4: `{...,mnemonico:false,imagem:true}` deve ser true; Cenário 5: os 4 toggles false deve ser false (byte-identidade PIPE-03). Em `smoke-enrich.ts`: adicionar passos CLI-free ANTES do `--assert-args` exit — `loadPrompt('mnemonic')` + `loadPrompt('mnemonic-image')` (valida cross-mode), `parseMnemonicosJson` com fixture JSON válido + fixture com cercas (importar de enrich.js), e `sanitizarSvg` com SVG geométrico limpo (→ não-null) e com `<svg><script>` (→ null) (importar de sanitize-svg.js). Após o exit `--assert-args`, adicionar (no caminho gated, sem flag) um passo que roda `enrichAll` com 1 card de fixture e `{mnemonico:true, imagem:true}`, e imprime instrução para o Passo 5 humano (exportar via AnkiConnect e conferir render no Anki desktop). Comentar com D-12/D-14.
  </action>
  <verify>
    <automated>cd ankinator-app/server && npx tsx src/scripts/guard-enrich.ts --assert && npx tsx src/scripts/smoke-enrich.ts --assert-args</automated>
  </verify>
  <acceptance_criteria>
    - `guard-enrich.ts --assert` sai 0 e cobre os cenários mnemonico/imagem (grep por `mnemonico` e `imagem` no arquivo retorna ≥1 cada).
    - `smoke-enrich.ts --assert-args` sai 0 e exercita `loadPrompt('mnemonic')`, `parseMnemonicosJson` e `sanitizarSvg` (todos CLI-free).
    - `api.ts` contém `mnemonico: options?.mnemonico ?? true` e `imagem: options?.imagem ?? false`.
    - Nenhuma flag de watch nos comandos (CI-friendly).
  </acceptance_criteria>
  <done>Defaults D-12 propagados; guard prova PIPE-03 com 4 toggles; smoke CLI-free valida prompts+parser+sanitização.</done>
</task>

<task type="auto">
  <name>Task 3: GenerateOptions/EnrichOpts do server alinhados (mnemonico/imagem)</name>
  <files>ankinator-app/server/src/core/types.ts</files>
  <read_first>
    - ankinator-app/server/src/core/types.ts (linhas 103-119: GenerateOptions — adicionar campos)
    - ankinator-app/server/src/core/specialists/enrich.ts (EnrichOpts já ampliado no Plano 02 — referência)
  </read_first>
  <action>
    Em `ankinator-app/server/src/core/types.ts`, adicionar a `GenerateOptions` os campos `mnemonico?: boolean;` (com comentário "Rodar especialista de mnemônico (batch). Default: true.") e `imagem?: boolean;` (comentário "Rodar especialista de imagem SVG (por-card). Default: false."), espelhando o estilo dos campos `classificar`/`cardBuilder` existentes. NÃO tocar o tipo `Questao` (D-11 — `mnemonico?`/`mnemonicoSvg?` já existem). Esta mudança torna o `genOptions` da Task 2 type-safe.
  </action>
  <verify>
    <automated>cd ankinator-app/server && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `types.ts` `GenerateOptions` contém `mnemonico?: boolean` e `imagem?: boolean`.
    - `Questao` permanece com exatamente os campos atuais (nenhum campo novo — D-11).
    - `npm run build` (server) sai 0.
  </acceptance_criteria>
  <done>GenerateOptions do server inclui os novos toggles; build verde; tipo Questao intacto.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Smoke gated — render do SVG real no Anki desktop (IMG-03)</name>
  <read_first>
    - .planning/phases/04-mnem-nicos-imagem-svg/04-RESEARCH.md (§ Render de <svg> Inline no Anki, linhas 445-473: risco do Anki 25.02.x; fallbacks que NÃO devem ser implementados agora)
    - .planning/phases/04-mnem-nicos-imagem-svg/04-VALIDATION.md (§ Manual-Only Verifications)
  </read_first>
  <action>
    PAUSAR para o sign-off humano do render real. Orientar o humano a rodar o smoke gated completo (`npx tsx src/scripts/smoke-enrich.ts`), exportar o card via AnkiConnect/CSV e confirmar visualmente no Anki desktop que o `<svg>` renderiza como imagem no verso (não como texto cru nem em branco). Aguardar "approved"; se falhar, registrar como gatilho de revisão de D-09 sem implementar fallback nesta fase.
  </action>
  <what-built>
    O pipeline completo: enrichAll gera mnemônico + SVG sanitizado, e os exporters embutem o `<svg>` inline no verso. Tudo automatizado nas tasks anteriores. Este checkpoint é o ÚNICO gate confiável para o render real (Research flag 2: Anki 25.02.x introduziu DOMPurify interno que pode remover atributos SVG).
  </what-built>
  <how-to-verify>
    1. Com `claude` (assinatura) e o Anki desktop abertos + add-on AnkiConnect instalado, rodar o smoke gated completo: `cd ankinator-app/server && npx tsx src/scripts/smoke-enrich.ts` (gera mnemônico + SVG real para 1 card de fixture).
    2. Exportar esse card via AnkiConnect (ou importar o CSV gerado) para um deck de teste.
    3. Abrir o card no Anki desktop e confirmar: o mnemônico-texto aparece no verso E a imagem SVG renderiza (não aparece como texto `<svg...>` nem em branco).
    4. Se possível, conferir também no AnkiDroid/AnkiMobile.
  </how-to-verify>
  <acceptance_criteria>
    - Humano confirma que o SVG RENDERIZA como imagem no verso do card no Anki desktop.
    - Se NÃO renderizar: reportar como gatilho de revisão de D-09 (fallback data-URI/media file — NÃO implementar nesta fase; abrir item para replanejamento).
  </acceptance_criteria>
  <resume-signal>Digite "approved" se o SVG renderiza no Anki, ou descreva o que apareceu (texto cru / branco / erro).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| q.mnemonicoSvg → campo Back/Verso (Anki) | o SVG (já sanitizado no Plano 02) é embutido inline; escapá-lo quebraria o render, mas não-sanitizá-lo seria XSS |
| options do cliente → enrichOpts | toggles do POST /generate controlam quais estágios rodam |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering | versoHtml/versoDaQuestao embute q.mnemonicoSvg | mitigate | O SVG já foi sanitizado fail-closed no Plano 02 (só campos sanitizados chegam aqui); embutido cru por design (escapá-lo quebraria o render — Pitfall 2). A sanitização é o controle; o embed confia nela. |
| T-04-11 | Tampering | escapeHtml acidental no SVG | mitigate | Teste positivo afirma '<svg' literal (não '&lt;svg') no retorno de versoHtml; teste afirma `&lt;` para o texto do mnemônico; grep proíbe `escapeHtml(q.mnemonicoSvg`. |
| T-04-12 | Information Disclosure | byte-identidade PIPE-03 | accept | Gate `if (q.mnemonicoSvg)` garante saída idêntica quando ausente; sem risco — apenas não-regressão. |
| T-04-SC | Tampering | (herdado) supply-chain do pacote | mitigate | Coberto pelo checkpoint do Plano 01; nenhum install novo aqui. |
</threat_model>

<verification>
- `cd ankinator-app/server && npm test -- exporters-embed` → exit 0 (inclui teste positivo de versoHtml: SVG cru + texto escapado).
- `cd ankinator-app/server && npx tsx src/scripts/guard-enrich.ts --assert` → exit 0.
- `cd ankinator-app/server && npx tsx src/scripts/smoke-enrich.ts --assert-args` → exit 0.
- `cd ankinator-app/server && npm run build` → tsc verde.
- Checkpoint humano: SVG renderiza no Anki (IMG-03).
</verification>

<success_criteria>
- mnemônico-texto + SVG inline anexados ao verso nos dois exporters com o mesmo mecanismo; SVG nunca escapado.
- versoHtml exportado e coberto por teste positivo determinístico (SVG cru presente + texto do mnemônico escapado) — IMG-03 verificável sem CLI.
- byte-identidade PIPE-03 quando campos ausentes.
- defaults D-12 propagados na rota /generate.
- guard/smoke estendidos verdes; render confirmado no Anki (checkpoint).
</success_criteria>

<output>
Create `.planning/phases/04-mnem-nicos-imagem-svg/04-03-SUMMARY.md` when done.
</output>
</output>
