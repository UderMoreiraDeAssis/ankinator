---
phase: 04-mnem-nicos-imagem-svg
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ankinator-app/server/package.json
  - ankinator-app/server/src/core/specialists/prompts/mnemonic.md
  - ankinator-app/server/src/core/specialists/prompts/mnemonic-image.md
  - ankinator-app/server/src/core/specialists/sanitize-svg.ts
  - ankinator-app/server/src/core/specialists/sanitize-svg.test.ts
autonomous: false
requirements: [MNEM-02, IMG-02]
user_setup: []

must_haves:
  truths:
    - "O SVG gerado é sanitizado por uma allowlist geométrica estrita antes de qualquer embed; todos os vetores adversariais F-01..F-12 são removidos de forma determinística (sem CLI)."
    - "Falha de sanitização retorna null (fail-closed), nunca markup parcial."
    - "O especialista mnemônico produz JSON batch {mnemonicos:[{id,mnemonico,tecnica?}]} casável por id."
  artifacts:
    - path: "ankinator-app/server/src/core/specialists/sanitize-svg.ts"
      provides: "Função sanitizarSvg(svgRaw) com allowlist geométrica estrita via ALLOWED_TAGS/ALLOWED_ATTR (D-06/D-07), fail-closed (D-08)"
      exports: ["sanitizarSvg"]
      contains: "ALLOWED_TAGS"
    - path: "ankinator-app/server/src/core/specialists/sanitize-svg.test.ts"
      provides: "Fixtures adversariais F-01..F-12 (cobertura completa dos vetores SVG XSS)"
      min_lines: 60
    - path: "ankinator-app/server/src/core/specialists/prompts/mnemonic.md"
      provides: "Seção ## Saída instruindo formato JSON batch por id"
      contains: "mnemonicos"
    - path: "ankinator-app/server/src/core/specialists/prompts/mnemonic-image.md"
      provides: "Regras de saída SVG (sem id/version no raiz; começa com <svg)"
      contains: "<svg"
  key_links:
    - from: "ankinator-app/server/src/core/specialists/sanitize-svg.ts"
      to: "isomorphic-dompurify"
      via: "import DOMPurify from 'isomorphic-dompurify'"
      pattern: "from 'isomorphic-dompurify'"
    - from: "ankinator-app/server/src/core/specialists/sanitize-svg.test.ts"
      to: "ankinator-app/server/src/core/specialists/sanitize-svg.ts"
      via: "import { sanitizarSvg } from './sanitize-svg.js'"
      pattern: "from './sanitize-svg.js'"
---

<objective>
Estabelecer a fundação de segurança da fase: instalar `isomorphic-dompurify`, criar o módulo de sanitização SVG `sanitize-svg.ts` com a allowlist geométrica estrita (D-06/D-07), prová-lo determinísticamente com os 12 fixtures adversariais F-01..F-12 (D-14, sem CLI/quota), e refinar os dois prompts canônicos (`mnemonic.md` para emitir JSON batch — pré-requisito do parser do Plano 02; `mnemonic-image.md` para evitar atributos que o Anki 25.02.x remove).

Purpose: a sanitização é um controle de segurança de baixa reversibilidade (o SVG renderiza em contexto que executa JS no Anki/QtWebEngine). Esta é a peça mais crítica da fase; toda a defesa em camadas (D-06→D-07→D-08→D-14) nasce aqui. O refino do `mnemonic.md` precisa existir ANTES do parser do Plano 02 (Pitfall 5 do RESEARCH).
Output: `isomorphic-dompurify` instalado; `sanitize-svg.ts` + `sanitize-svg.test.ts`; prompts refinados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-mnem-nicos-imagem-svg/04-CONTEXT.md
@.planning/phases/04-mnem-nicos-imagem-svg/04-RESEARCH.md
@.planning/phases/04-mnem-nicos-imagem-svg/04-PATTERNS.md

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do codebase. NÃO explorar. -->

Padrão fail-closed a replicar (de generation.ts / enrich.ts): `try { ... } catch { return null; }`.

Configuração DOMPurify obrigatória (RESEARCH.md Padrão 2). NUNCA combinar USE_PROFILES com ALLOWED_TAGS (USE_PROFILES sobrescreve ALLOWED_TAGS silenciosamente — Pitfall 1):
  DOMPurify.sanitize(svgRaw, { ALLOWED_TAGS: SVG_TAGS, ALLOWED_ATTR: SVG_ATTRS })

SVG_TAGS (allowlist geométrica estrita D-07): svg, g, defs, path, rect, circle, ellipse, line, polyline, polygon, text, tspan, linearGradient, radialGradient, stop

SVG_ATTRS (subset seguro — geometria/apresentação/texto/gradiente; ver RESEARCH.md Padrão 2 linhas 273-294 para a lista completa): viewBox, xmlns, width, height, x, y, x1, y1, x2, y2, cx, cy, r, rx, ry, d, points, transform, fill, fill-opacity, fill-rule, stroke, stroke-width, stroke-dasharray, stroke-linecap, stroke-linejoin, stroke-opacity, opacity, color, font-family, font-size, font-weight, text-anchor, dominant-baseline, gradientUnits, gradientTransform, offset, stop-color, stop-opacity, fx, fy, id, preserveAspectRatio, clip-path, mask
  (NOTA: 'style' NUNCA entra em ALLOWED_ATTR — F-09 depende disso. 'id' é permitido para referência interna de gradiente, NÃO há xlink:href externo possível pois <use>/<image> estão fora de SVG_TAGS.)

Regra ESM/NodeNext (Pitfall 7):
  - módulo npm: `import DOMPurify from 'isomorphic-dompurify'` (SEM extensão)
  - módulo local: `import { sanitizarSvg } from './sanitize-svg.js'` (COM .js)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verificação de legitimidade — isomorphic-dompurify (gate de pacote)</name>
  <read_first>
    - .planning/phases/04-mnem-nicos-imagem-svg/04-RESEARCH.md (§ Auditoria de Legitimidade dos Pacotes — slopcheck indisponível na pesquisa; verificação formal delegada ao planner)
  </read_first>
  <action>
    PAUSAR antes de qualquer `npm install`. Apresentar ao humano os dados de legitimidade de `isomorphic-dompurify@3.15.0` (mantenedor kkomelin, repo oficial, ausência de postinstall) e aguardar aprovação explícita; não prosseguir para a Task 2 até o humano digitar "approved" (gate de supply-chain T-04-SC).
  </action>
  <what-built>
    Nada ainda — este é o gate ANTES do `npm install`. O RESEARCH.md registra que `slopcheck` não pôde rodar na sessão de pesquisa; portanto o pacote entra como verificação humana antes da instalação (threat T-04-SC).
  </what-built>
  <how-to-verify>
    1. Abra https://www.npmjs.com/package/isomorphic-dompurify e confirme: mantenedor `kkomelin`, repositório `github.com/kkomelin/isomorphic-dompurify`, sem script `postinstall`, versão 3.15.0 publicada.
    2. Confirme que as deps transitivas `dompurify` (cure53) e `jsdom` (jsdom/jsdom) são as oficiais.
    3. Aprove se tudo confere.
  </how-to-verify>
  <acceptance_criteria>
    - Humano confirma "approved" para `isomorphic-dompurify@3.15.0` ANTES de qualquer `npm install`.
    - Se reprovado: parar a fase e replanejar com `dompurify` + `jsdom` explícitos ou sanitização alternativa.
  </acceptance_criteria>
  <resume-signal>Digite "approved" para liberar a instalação, ou descreva o problema.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Instalar isomorphic-dompurify + criar sanitize-svg.ts + fixtures F-01..F-12</name>
  <files>ankinator-app/server/package.json, ankinator-app/server/src/core/specialists/sanitize-svg.ts, ankinator-app/server/src/core/specialists/sanitize-svg.test.ts</files>
  <read_first>
    - ankinator-app/server/src/core/specialists/prompt-loader.ts (linhas 30-32: padrão de import ESM correto, para confirmar estilo NodeNext)
    - ankinator-app/server/src/core/generation.ts (padrão fail-closed: catch → isolamento; ~linhas 84-96 citadas no PATTERNS)
    - .planning/phases/04-mnem-nicos-imagem-svg/04-RESEARCH.md (Padrão 2, linhas 255-321: config concreta de sanitizarSvg; e § Cobertura Adversarial, tabela F-01..F-12 linhas 391-441)
    - .planning/phases/04-mnem-nicos-imagem-svg/04-PATTERNS.md (seção sanitize-svg.ts, linhas 196-232; e fixtures linhas 365-386)
  </read_first>
  <behavior>
    - F-01: `sanitizarSvg('<svg><script>alert(1)</script></svg>')` → resultado não contém `<script>` nem `alert`.
    - F-02: `sanitizarSvg('<svg onload="alert(1)"><rect/></svg>')` → não contém `onload`.
    - F-03: `sanitizarSvg('<svg><rect onclick="evil()"/></svg>')` → não contém `onclick`; `<rect` mantido.
    - F-04: `<a href="javascript:...">` → `<a>` fora da allowlist → removida (sem `javascript:` no output).
    - F-05: `<use xlink:href="http://evil.com/...">` → `<use>` removida.
    - F-06: `<foreignObject>` → removida.
    - F-07: `<image href="http://evil.com/...">` → removida.
    - F-08: `<animate>` / `<set>` → removida.
    - F-09: `<rect style="fill:url(http://evil.com/x)"/>` → atributo `style` removido (style fora de ALLOWED_ATTR).
    - F-10: SVG geométrico limpo `<svg viewBox="0 0 100 100"><rect x="10" y="10" width="80" height="80" fill="blue"/></svg>` → saída não-nula, contém `<rect`.
    - F-11: `sanitizarSvg('')` → null; `sanitizarSvg('<div>hello</div>')` → null.
    - F-12: entrada com cercas ```svg → o RESEARCH define que o parser do especialista remove cercas ANTES de sanitizar; documentar como teste do contrato (cercas não chegam à sanitizarSvg) — pode ser `it` que afirma que sanitizarSvg recebe markup já sem cercas (sem cerca embutida no fixture).
  </behavior>
  <action>
    Instalar a dependência: `npm install isomorphic-dompurify --workspace @ankinator/server` (versão 3.15.0 conforme RESEARCH; verificar que aparece em ankinator-app/server/package.json dependencies). Criar `sanitize-svg.ts` exportando `sanitizarSvg(svgRaw: string): string | null`. Importar `DOMPurify` de `'isomorphic-dompurify'` (SEM extensão — exports map resolve, Pitfall 7). Declarar as constantes `SVG_TAGS` (allowlist geométrica estrita de D-07) e `SVG_ATTRS` (subset seguro) com os valores EXATOS do bloco <interfaces> deste plano. Chamar `DOMPurify.sanitize(svgRaw, { ALLOWED_TAGS: SVG_TAGS, ALLOWED_ATTR: SVG_ATTRS })` — APENAS ALLOWED_TAGS/ALLOWED_ATTR, NUNCA USE_PROFILES (Pitfall 1). Aplicar gate fail-closed (D-08, Pitfall 4): `trim()` o resultado; se NÃO começar com `<svg`, retornar `null`; envolver tudo em `try { ... } catch { return null; }`. Criar `sanitize-svg.test.ts` importando `{ sanitizarSvg } from './sanitize-svg.js'` (COM .js) com um `it` por fixture F-01..F-12 conforme <behavior>. Os testes rodam DOMPurify em Node puro — NÃO precisam de `vi.mock`. Comentar cada constante e o gate referenciando D-06/D-07/D-08 e o Pitfall correspondente (convenção PT do projeto).
  </action>
  <verify>
    <automated>cd ankinator-app/server && npm test -- sanitize-svg</automated>
  </verify>
  <acceptance_criteria>
    - `ankinator-app/server/package.json` contém `"isomorphic-dompurify"` em `dependencies`.
    - `sanitize-svg.ts` contém `export function sanitizarSvg`, a string `ALLOWED_TAGS:` e a string `ALLOWED_ATTR:`, e NÃO contém `USE_PROFILES`.
    - `sanitize-svg.ts` contém o gate `startsWith('<svg')` e `return null`.
    - `npm test -- sanitize-svg` (em ankinator-app/server) sai 0 com ≥12 testes passando (F-01..F-12).
    - `grep -c "it(" sanitize-svg.test.ts` ≥ 12.
  </acceptance_criteria>
  <done>Sanitização determinística cobre todos os 12 vetores adversariais; fail-closed retorna null; dependência instalada e auditada.</done>
</task>

<task type="auto">
  <name>Task 3: Refinar mnemonic.md (saída JSON batch) e mnemonic-image.md (regras Anki 25.02.x)</name>
  <files>ankinator-app/server/src/core/specialists/prompts/mnemonic.md, ankinator-app/server/src/core/specialists/prompts/mnemonic-image.md</files>
  <read_first>
    - ankinator-app/server/src/core/specialists/prompts/mnemonic.md (estado atual: seção `## Saída` descreve texto livre — Pitfall 5; D-01 "Quando NÃO gerar" e D-03 "## Técnicas" já presentes — NÃO duplicar)
    - ankinator-app/server/src/core/specialists/prompts/mnemonic-image.md (estado atual: `## Restrições de segurança` e `## Saída` já presentes)
    - .planning/phases/04-mnem-nicos-imagem-svg/04-PATTERNS.md (linhas 266-309: pontos de intervenção exatos com o texto-alvo)
    - .planning/phases/04-mnem-nicos-imagem-svg/04-RESEARCH.md (Pitfall 5 linhas 506-512; Pergunta Aberta 2 linhas 658-660; refino do mnemonic-image)
  </read_first>
  <action>
    Em `mnemonic.md`, SUBSTITUIR o corpo da seção `## Saída` (hoje "Devolva o texto do mnemônico...") por instrução de formato JSON batch: o especialista deve devolver APENAS JSON, sem texto antes/depois e sem cercas, no formato exato `{"mnemonicos":[{"id":"<id-do-card>","mnemonico":"<texto>","tecnica":"<acrônimo|história|loci|rima>"}]}`; para cards conceituais/de raciocínio, NÃO incluir o card na lista (omissão = sem mnemônico, casa com D-01); manter o mapeamento item→gatilho DENTRO do campo `mnemonico`. NÃO tocar as seções `## Quando NÃO gerar` (D-01) nem `## Técnicas` (D-03) — fonte única, refinar não duplicar. Em `mnemonic-image.md`, AMPLIAR a seção `## Saída` com 3 regras adicionais (sem remover o conteúdo existente sobre `<svg>` autocontido), redigidas de forma que cada regra seja uma frase de NEGAÇÃO/EXIGÊNCIA case-grepável: (a) "sem texto antes ou depois e sem cercas de código"; (b) uma regra que diga explicitamente que o elemento `<svg>` raiz NÃO deve ter os atributos `id` nem `version` — usar uma redação que case o padrão `não use os atributos id ou version` ou `sem id ou version no <svg> raiz` (Anki 25.02.x remove esses — A1 da pesquisa); (c) "o SVG deve começar exatamente com `<svg` e terminar com `</svg>`". NÃO tocar `## Restrições de segurança` (já cobre script/URL externa/foreignObject).
  </action>
  <verify>
    <automated>grep -c 'mnemonicos' ankinator-app/server/src/core/specialists/prompts/mnemonic.md && grep -v '^#' ankinator-app/server/src/core/specialists/prompts/mnemonic-image.md | grep -ciE '(sem|não|nao)[^.]*\b(id|version)\b' </automated>
  </verify>
  <acceptance_criteria>
    - `mnemonic.md` seção `## Saída` contém a string literal `{"mnemonicos":[` e a instrução de omitir cards conceituais.
    - `mnemonic.md` ainda contém as seções `## Quando NÃO gerar` e `## Técnicas` (não removidas).
    - `mnemonic-image.md` seção `## Saída` contém uma instrução de NEGAÇÃO explícita para os atributos `id`/`version` no `<svg>` raiz (frase do tipo "sem/não use id ou version"), e a instrução de começar com `<svg`. NÃO basta a palavra "version" aparecer em qualquer contexto — a regra precisa ser uma negação dos atributos no raiz.
    - `grep -c 'mnemonicos' mnemonic.md` ≥ 1.
    - `grep -v '^#' mnemonic-image.md | grep -ciE '(sem|não|nao)[^.]*\b(id|version)\b'` ≥ 1 (a instrução de exclusão de id/version existe fora de comentários/headers, evitando o falso-positivo de "version" solto).
  </acceptance_criteria>
  <done>mnemonic.md emite JSON batch casável por id (pré-requisito do parser do Plano 02); mnemonic-image.md evita atributos removidos pelo Anki 25.02.x, com a regra de exclusão de id/version verificada por grep específico (não por ocorrência solta de "version").</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PDF não-confiável → prompt do Claude | conteúdo do PDF pode tentar injeção indireta para o Claude emitir markup perigoso no SVG |
| SVG gerado → campo de card (Anki/QtWebEngine) | o SVG renderiza em contexto que EXECUTA JS; XSS aqui é severidade HIGH |
| pacote npm novo → árvore de deps | supply-chain do `isomorphic-dompurify` e transitivas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `sanitizarSvg` (sanitize-svg.ts) — XSS via `<script>`/`on*`/`javascript:` | mitigate | Allowlist geométrica estrita via ALLOWED_TAGS/ALLOWED_ATTR (D-07); provado por F-01..F-03 determinísticos. **BLOCKING**: bypass = HIGH. |
| T-04-02 | Tampering | `sanitizarSvg` — `<use xlink:href>` / `<foreignObject>` / `<animate>` | mitigate | Tags fora de SVG_TAGS removidas por omissão; F-05/F-06/F-08. **BLOCKING**. |
| T-04-03 | Information Disclosure | `sanitizarSvg` — `<image href>` externo / CSS `url()` exfil | mitigate | `<image>` fora de SVG_TAGS; `style` fora de ALLOWED_ATTR; F-07/F-09. **BLOCKING**. |
| T-04-04 | Tampering | gate fail-closed | mitigate | Retorna `null` se output não começa com `<svg` (Pitfall 4); nunca embute markup parcial (D-08); F-11. |
| T-04-05 | Spoofing/Tampering | prompt injection via conteúdo do PDF no mnemônico | mitigate | Defesa em profundidade: o markup perigoso que o Claude eventualmente emita é removido na sanitização (T-04-01..03); o prompt mnemonic-image.md reforça "sem script/URL externa". |
| T-04-SC | Tampering | `npm install isomorphic-dompurify` | mitigate | Checkpoint humano (Task 1) verifica npmjs.com antes de instalar; slopcheck indisponível na pesquisa → verificação manual obrigatória. |
</threat_model>

<verification>
- `cd ankinator-app/server && npm test -- sanitize-svg` → exit 0, ≥12 testes verdes (F-01..F-12).
- `grep -L USE_PROFILES ankinator-app/server/src/core/specialists/sanitize-svg.ts` retorna o arquivo (confirma ausência de USE_PROFILES).
- `cd ankinator-app/server && npm run build` → tsc verde (import ESM correto, Pitfall 7).
</verification>

<success_criteria>
- isomorphic-dompurify instalado e auditado (checkpoint humano aprovado).
- sanitizarSvg remove todos os 12 vetores adversariais de forma determinística; fail-closed retorna null.
- mnemonic.md emite JSON batch; mnemonic-image.md evita id/version no raiz (regra verificada por grep específico).
- build do server verde.
</success_criteria>

<output>
Create `.planning/phases/04-mnem-nicos-imagem-svg/04-01-SUMMARY.md` when done.
</output>
</output>
