---
phase: 04-mnem-nicos-imagem-svg
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - ankinator-app/web/src/types.ts
  - ankinator-app/web/src/components/StructurePanel.tsx
  - ankinator-app/web/src/components/CardTable.tsx
  - ankinator-app/web/src/components/ProgressPanel.tsx
  - ankinator-app/web/src/App.tsx
autonomous: false
requirements: [MNEM-01, IMG-01]

must_haves:
  truths:
    - "O bloco 'Modo educativo' do StructurePanel tem 2 toggles novos: Mnemônico (default LIGADO) e Imagem de mnemônico (default DESLIGADO), espelhando D-12."
    - "A CardTable mostra badges read-only 📝 (quando q.mnemonico) e 🖼️ (quando q.mnemonicoSvg); o SVG NUNCA é renderizado no browser (D-13)."
    - "O ProgressPanel exibe rótulos para os estágios 'gerando-mnemonico' e 'gerando-imagem' vindos do enrich-progress."
    - "Os tipos web GenerateOptions e EnrichProgress espelham exatamente o server (sem tocar o tipo Questao)."
    - "Verificação da UI = build verde (tsc/tsc -b) + checkpoint humano visual (D-13); SEM testes de componente React por decisão de superfície mínima — nenhuma cobertura comportamental automatizada da UI é prometida."
  artifacts:
    - path: "ankinator-app/web/src/types.ts"
      provides: "GenerateOptions += mnemonico?/imagem?; EnrichProgress.estagio += novos literais"
      contains: "gerando-mnemonico"
    - path: "ankinator-app/web/src/components/StructurePanel.tsx"
      provides: "2 toggles no bloco Modo educativo (default ON/OFF)"
      contains: "Mnemônico"
    - path: "ankinator-app/web/src/components/CardTable.tsx"
      provides: "badges 📝/🖼️ read-only sem render de SVG"
      contains: "mnemonicoSvg"
    - path: "ankinator-app/web/src/App.tsx"
      provides: "defaults mnemonico:true / imagem:false no useState de options"
      contains: "mnemonico"
  key_links:
    - from: "ankinator-app/web/src/components/StructurePanel.tsx"
      to: "setOptions"
      via: "onChange dos toggles mnemônico/imagem"
      pattern: "mnemonico|imagem"
    - from: "ankinator-app/web/src/components/CardTable.tsx"
      to: "c.mnemonico / c.mnemonicoSvg"
      via: "badge condicional read-only"
      pattern: "c.mnemonicoSvg"
---

<objective>
Entregar a superfície de UI mínima e já especificada (D-12/D-13, sem UI-SPEC por decisão do usuário): 2 toggles no bloco "Modo educativo" do `StructurePanel` (Mnemônico default ON / Imagem default OFF), badges read-only 📝/🖼️ na `CardTable` (SEM renderizar SVG), rótulos para os novos estágios no `ProgressPanel`, e os espelhos de tipo (`GenerateOptions`/`EnrichProgress`) consistentes com o server. Ajustar os defaults no `App.tsx`.

Purpose: dar ao usuário o controle (toggles) e o feedback (badges + progresso) do modo educativo de mnemônico/imagem, mantendo a defesa em camadas D-13 (não abrir a superfície de XSS de renderizar SVG no React).
Output: 5 arquivos web estendidos; build web verde.

NOTA — Estratégia de verificação da UI (D-13, decisão travada): a verificação deste plano é (1) `tsc --noEmit` / `tsc -b` (tipos consistentes com o server) + (2) `npm run build` (vite/tsc verde) + (3) checkpoint humano visual (Task 3). NÃO criamos testes de componente React (React Testing Library / @testing-library/react) — por design, a superfície da UI é MÍNIMA: badges read-only e toggles, SEM render de SVG (D-13). A ausência de testes unitários de componente é deliberada (decisão de superfície mínima), não uma lacuna de cobertura; este plano NÃO promete cobertura comportamental automatizada da UI. A cobertura comportamental do modo educativo vive no server (Planos 02 e 03, testes determinísticos de enrich/exporters).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-mnem-nicos-imagem-svg/04-CONTEXT.md
@.planning/phases/04-mnem-nicos-imagem-svg/04-PATTERNS.md

<interfaces>
<!-- Contratos reais do codebase web. NÃO explorar. -->

web/src/types.ts (estado atual):
  GenerateOptions { maxPerChunk?; incluirExtraidas?; incluirCriadas?; tags?; classificar?; cardBuilder? }   // + mnemonico?, imagem?
  EnrichProgress { estagio: 'classificando' | 'reescrevendo'; index; total; erro? }   // + 'gerando-mnemonico' | 'gerando-imagem'
  Questao { ...; mnemonico?: string; mnemonicoSvg?: string }   // JÁ existe — NÃO tocar (D-11)

StructurePanel.tsx: bloco "Modo educativo" (toggles classificar `options.classificar !== false`, cardBuilder `options.cardBuilder === true`). Padrão de label+checkbox `setOptions({ ...options, X: e.target.checked })`.

CardTable.tsx: badges read-only `{c.deck && <span ...>{c.deck}</span>}` e `{c.tags?.length ? <span>...</span> : null}`. Padrão `{c.campo && <span>...</span>}`. NUNCA dangerouslySetInnerHTML (D-13).

ProgressPanel.tsx: `enrichProgress.estagio === 'classificando' ? '...' : 'Reescrevendo card N/total…'` — ampliar o ternário (~linhas 51-60).

App.tsx: `useState<GenerateOptions>({ ..., classificar: true, cardBuilder: false })` — adicionar mnemonico/imagem. Listener `enrich-progress` JÁ é genérico (nenhuma mudança).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: espelhar tipos (GenerateOptions/EnrichProgress) + defaults no App.tsx</name>
  <files>ankinator-app/web/src/types.ts, ankinator-app/web/src/App.tsx</files>
  <read_first>
    - ankinator-app/web/src/types.ts (linhas 51-68: GenerateOptions + EnrichProgress; linhas 28-41: Questao — NÃO tocar)
    - ankinator-app/web/src/App.tsx (linhas 20-26: useState de options; linhas 86-89: listener enrich-progress — já genérico)
    - .planning/phases/04-mnem-nicos-imagem-svg/04-PATTERNS.md (linhas 574-616: extensão dos tipos; linhas 760-779: App.tsx defaults)
  </read_first>
  <action>
    Em `web/src/types.ts`: adicionar a `GenerateOptions` os campos `mnemonico?: boolean;` e `imagem?: boolean;` (com comentários "Phase 4: default true/false no App.tsx — D-12"), espelhando o estilo de `classificar`/`cardBuilder`. Ampliar a union `EnrichProgress['estagio']` para `'classificando' | 'reescrevendo' | 'gerando-mnemonico' | 'gerando-imagem'` (espelho EXATO do server). NÃO tocar `Questao` (D-11 — campos já existem). Em `App.tsx`, no objeto inicial de `useState<GenerateOptions>` (que já tem `classificar: true, cardBuilder: false`), adicionar `mnemonico: true,` (default ON, D-12) e `imagem: false,` (default OFF, D-12). NÃO alterar o listener `enrich-progress` (já genérico).
  </action>
  <verify>
    <automated>grep -c 'gerando-mnemonico' ankinator-app/web/src/types.ts && grep -c 'mnemonico: true' ankinator-app/web/src/App.tsx && cd ankinator-app/web && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/types.ts` `GenerateOptions` contém `mnemonico?: boolean` e `imagem?: boolean`; `EnrichProgress.estagio` contém `'gerando-mnemonico'` e `'gerando-imagem'`.
    - `web/src/types.ts` `Questao` inalterado (sem campo novo).
    - `App.tsx` useState de options contém `mnemonico: true` e `imagem: false`.
    - `cd ankinator-app/web && npx tsc --noEmit` sai 0 (a ampliação de `EnrichProgress.estagio` e `GenerateOptions` não introduz erro de tipo ANTES do build da Task 2).
  </acceptance_criteria>
  <done>Tipos web espelham o server; defaults D-12 aplicados no App; tsc sem erros de tipo.</done>
</task>

<task type="auto">
  <name>Task 2: toggles no StructurePanel + badges na CardTable + rótulos no ProgressPanel</name>
  <files>ankinator-app/web/src/components/StructurePanel.tsx, ankinator-app/web/src/components/CardTable.tsx, ankinator-app/web/src/components/ProgressPanel.tsx</files>
  <read_first>
    - ankinator-app/web/src/components/StructurePanel.tsx (linhas 130-154: bloco "Modo educativo" — adicionar 2 labels após "Card educativo")
    - ankinator-app/web/src/components/CardTable.tsx (linhas 70-81: badges deck/tags read-only — adicionar 2 badges após)
    - ankinator-app/web/src/components/ProgressPanel.tsx (linhas ~51-60: ternário de `estagio` que hoje trata 'classificando'/'reescrevendo' — ampliar para os novos literais)
    - .planning/phases/04-mnem-nicos-imagem-svg/04-PATTERNS.md (linhas 618-756: extensões exatas de cada componente com JSX-alvo)
  </read_first>
  <action>
    Em `StructurePanel.tsx`, dentro do bloco "Modo educativo", APÓS o label "Card educativo", adicionar 2 labels com o MESMO padrão JSX (label flex + checkbox): (1) "Mnemônico" com `checked={options.mnemonico !== false}` (default ON, D-12) e `onChange={(e) => setOptions({ ...options, mnemonico: e.target.checked })}`; (2) "Imagem de mnemônico" com `checked={options.imagem === true}` (default OFF, D-12) e `onChange={(e) => setOptions({ ...options, imagem: e.target.checked })}`. Em `CardTable.tsx`, APÓS os badges de deck/tags (dentro do mesmo container de badges), adicionar 2 badges read-only com o padrão `{c.campo && <span ...>...</span>}`: (1) `{c.mnemonico && <span title={c.mnemonico.slice(0,60)} ...>📝 mnemônico</span>}`; (2) `{c.mnemonicoSvg && <span ...>🖼️ SVG</span>}`. NUNCA usar `dangerouslySetInnerHTML` nem renderizar o SVG (D-13). Em `ProgressPanel.tsx`, ampliar o ternário de `enrichProgress.estagio` (~linhas 51-60) para incluir os casos `'gerando-mnemonico'` → 'Gerando mnemônicos…' e `'gerando-imagem'` → `Gerando imagem ${enrichProgress.index + 1}/${enrichProgress.total}…`.
  </action>
  <verify>
    <automated>cd ankinator-app/web && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `StructurePanel.tsx` contém `options.mnemonico !== false` e `options.imagem === true` (defaults D-12 corretos).
    - `CardTable.tsx` contém `c.mnemonico &&` e `c.mnemonicoSvg &&`; NÃO contém `dangerouslySetInnerHTML` (D-13).
    - `ProgressPanel.tsx` contém `'gerando-mnemonico'` e `'gerando-imagem'`.
    - `cd ankinator-app/web && npm run build` sai 0.
  </acceptance_criteria>
  <done>Toggles ON/OFF, badges read-only sem render de SVG, rótulos de progresso para os novos estágios; build web verde.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação visual do modo educativo (toggles + badges + progresso)</name>
  <read_first>
    - .planning/phases/04-mnem-nicos-imagem-svg/04-CONTEXT.md (D-12: defaults; D-13: badges sem render)
  </read_first>
  <action>
    PAUSAR para verificação visual humana. Orientar o humano a rodar o app (`npm run dev`), conferir os defaults dos toggles (mnemônico ON / imagem OFF), os badges read-only 📝/🖼️ na CardTable (sem render de SVG) e os rótulos de progresso dos novos estágios. Aguardar "approved" ou a descrição do que estiver incorreto. Este checkpoint é a verificação comportamental da UI (D-13 — sem testes de componente React por decisão de superfície mínima).
  </action>
  <what-built>
    UI do modo educativo de mnemônico/imagem: 2 toggles no StructurePanel (mnemônico ON / imagem OFF por padrão), badges 📝/🖼️ read-only na CardTable, rótulos de progresso dos novos estágios. Tudo automatizado nas tasks anteriores. A verificação automatizada deste plano é só build/tipos (D-13); o comportamento visual é confirmado aqui pelo humano.
  </what-built>
  <how-to-verify>
    1. Rodar o app web (`cd ankinator-app/web && npm run dev`) e o server.
    2. Na tela de estrutura, confirmar no bloco "Modo educativo": toggle "Mnemônico" LIGADO por padrão; toggle "Imagem de mnemônico" DESLIGADO por padrão.
    3. Gerar questões; na tela de revisão, confirmar que cards com mnemônico mostram o badge 📝 e cards com SVG mostram 🖼️ — e que o SVG NÃO é renderizado como imagem na tabela (só o badge).
    4. Durante a geração, confirmar que o progresso mostra "Gerando mnemônicos…" e "Gerando imagem N/total…".
  </how-to-verify>
  <acceptance_criteria>
    - Humano confirma defaults corretos (mnemônico ON, imagem OFF), badges read-only visíveis, SVG NÃO renderizado na tabela, rótulos de progresso corretos.
  </acceptance_criteria>
  <resume-signal>Digite "approved" ou descreva o que está incorreto.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| q.mnemonicoSvg → DOM do browser (React) | renderizar o SVG no app web abriria uma superfície de XSS via dangerouslySetInnerHTML DIFERENTE do Anki |
| options do usuário → POST /generate | toggles controlam quais estágios caros rodam (quota) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Tampering | CardTable renderizando SVG | mitigate | D-13: apenas badges read-only; NUNCA `dangerouslySetInnerHTML` — não abrir a 2ª superfície de sanitização no React. Teste/grep proíbe dangerouslySetInnerHTML. |
| T-04-14 | Elevation/Quota | toggle imagem default ON | mitigate | D-12: imagem default OFF (`options.imagem === true`) — o estágio mais caro é opt-in consciente; guarda de quota. |
</threat_model>

<verification>
- `cd ankinator-app/web && npx tsc --noEmit` → tsc sem erros de tipo (Task 1, antes do build).
- `cd ankinator-app/web && npm run build` → vite/tsc verde.
- `grep -c dangerouslySetInnerHTML ankinator-app/web/src/components/CardTable.tsx` → 0 (D-13).
- Checkpoint humano: defaults + badges + progresso corretos (verificação comportamental da UI — D-13, sem testes de componente).
</verification>

<success_criteria>
- toggles mnemônico (ON) / imagem (OFF) no Modo educativo.
- badges 📝/🖼️ read-only; SVG não renderizado no front.
- rótulos de progresso para os novos estágios.
- tipos web espelham o server; tsc sem erros; build web verde.
- Verificação da UI = tipos + build + checkpoint humano (D-13); sem cobertura de componente React por design.
</success_criteria>

<output>
Create `.planning/phases/04-mnem-nicos-imagem-svg/04-04-SUMMARY.md` when done.
</output>
</output>
