---
phase: 01-andaime-dos-especialistas
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/01-andaime-dos-especialistas/baseline-cli.txt
autonomous: true
requirements: [SPEC-01]
must_haves:
  truths:
    - "Existe um baseline capturado do smoke-cli.ts ANTES de qualquer refatoração do CliProvider, para comparar comportamento depois."
    - "As dependências dos dois workspaces estão alinhadas ao lockfile (npm ci), removendo a divergência de versão do tsc antes de medir build."
    - "Ao final desta wave, src/ compila verde (npm run build sai 0) — nenhum import pendente foi introduzido; o smoke-runner so e criado na Wave 1 (Plano 02), apos runner.ts/prompt-loader.ts existirem."
  artifacts:
    - path: ".planning/phases/01-andaime-dos-especialistas/baseline-cli.txt"
      provides: "Saida de referencia do CliProvider atual (pre-refator) para regressao SPEC-01"
  key_links:
    - from: "ankinator-app/server (workspace)"
      to: "package-lock.json (lockfile)"
      via: "npm ci alinha tsc ao lockfile antes de medir build"
      pattern: "npm ci"
---

<objective>
Wave 0 de infraestrutura de validacao — APENAS operacoes build-safe (nao introduz nenhum arquivo `.ts` em `src/` que importe modulos ainda inexistentes, para que `npm run build` saia 0 em TODO limite de tarefa). Esta fase e puro andaime e NAO tem framework de teste — a convencao do projeto e smoke script `.ts` via `tsx` + gate `npm run build`. Antes de refatorar o `CliProvider` (Plano 02), precisamos de (a) um baseline do comportamento atual do CLI para provar "sem regressao" depois, e (b) alinhar as dependencias ao lockfile (tsc instalado 6.0.3 vs `^5.9.3` declarado).

Purpose: Fechar os Wave 0 Gaps do 01-VALIDATION.md que sao build-safe — sem baseline, a regressao SPEC-01 nao e demonstravel; sem npm ci, o gate de build mede uma versao de tsc nao-deterministica. O esqueleto do `smoke-runner.ts` foi MOVIDO para o Plano 02 (Wave 1): ele importa `runClaudeCli`/`loadPrompt` que so existem apos o Plano 02; cria-lo aqui deixaria `src/` com import pendente (TS2307) entre Wave 0 e Wave 1 — porque `tsconfig.json` tem `include: src/**/*.ts` e NAO tem `exclude`, o `tsc` compilaria o smoke-runner e falharia. Por isso a Wave 0 fica limitada a `npm ci` + baseline.
Output: baseline-cli.txt, lockfile aplicado. `src/` permanece compilavel (build verde).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-andaime-dos-especialistas/01-VALIDATION.md
@.planning/phases/01-andaime-dos-especialistas/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alinhar dependencias ao lockfile (npm ci) e capturar baseline do CliProvider</name>
  <read_first>
    - ankinator-app/server/src/scripts/smoke-cli.ts (molde; ja existe — uso: `tsx src/scripts/smoke-cli.ts <pdf>`)
    - ankinator-app/server/package.json (scripts: build/start/smoke)
    - ankinator-app/server/tsconfig.json (CONFIRMADO: `include: ["src/**/*.ts"]`, sem `exclude` — qualquer .ts em src/ entra no build; por isso o smoke-runner so pode ser criado depois que seus imports existirem)
    - .planning/phases/01-andaime-dos-especialistas/01-RESEARCH.md secao "Open Questions" item 1 (divergencia tsc 6.0.3 vs ^5.9.3) e secao "Wave 0 Gaps"
  </read_first>
  <action>
    Rodar `npm ci` em `ankinator-app/server` e `ankinator-app/web` para travar as versoes no lockfile (resolve a divergencia tsc 6.0.3 vs ^5.9.3 antes de qualquer medicao de build, conforme RESEARCH Open Question 1).
    Em seguida capturar o baseline do CliProvider ATUAL (antes de qualquer refatoracao — pre-requisito de SPEC-01 "sem regressao"): localizar um PDF curto de teste no repo (procurar com `find . -name '*.pdf' -not -path '*/node_modules/*'`); se nenhum existir, registrar no SUMMARY que o baseline foi capturado de forma simbolica E que, por nao haver fixture, a guarda de regressao SPEC-01 sera coberta por DOIS mecanismos no Plano 04: (1) a assertion deterministica de args/cwd/env (CLI-free, Plano 02 cria o helper; Plano 04 a roda) e (2) o checkpoint humano HARD-BLOCK que so libera com um PDF fornecido pelo usuario. Se um PDF existir, rodar `cd ankinator-app/server && npx tsx src/scripts/smoke-cli.ts <pdf>` e gravar a saida em `.planning/phases/01-andaime-dos-especialistas/baseline-cli.txt` (contagem de questoes + forma `[tipo] Q:/A:` e o que importa comparar, nao o texto exato — a geracao do LLM nao e deterministica; documentar isso no topo do arquivo de baseline).
    NAO criar `smoke-runner.ts` neste plano (ele foi movido para o Plano 02). NAO refatorar nada do CliProvider neste plano — apenas capturar o estado atual.
  </action>
  <verify>
    <automated>cd ankinator-app/server && npm run build 2>&1 | tail -3; echo "build exit: ${PIPESTATUS[0]:-$?}"; test -f ../../.planning/phases/01-andaime-dos-especialistas/baseline-cli.txt && echo "baseline ok"; test ! -f src/scripts/smoke-runner.ts && echo "smoke-runner ausente na Wave 0 (correto)"</automated>
  </verify>
  <acceptance_criteria>
    - `npm ci` executado com sucesso em ankinator-app/server e ankinator-app/web (sem erro de resolucao).
    - `cd ankinator-app/server && npm run build` sai com codigo 0 (compilacao verde, agora com versao de tsc do lockfile) — confirma que a Wave 0 NAO deixou import pendente em src/.
    - `src/scripts/smoke-runner.ts` NAO existe ao fim deste plano (foi movido para o Plano 02): `test ! -f ankinator-app/server/src/scripts/smoke-runner.ts`.
    - `.planning/phases/01-andaime-dos-especialistas/baseline-cli.txt` existe; se um PDF de teste foi encontrado, contem a saida do smoke-cli (contagem + forma das questoes); se nao, contem a nota de baseline simbolico + plano de comparacao (assertion deterministica + checkpoint hard-block no Plano 04).
    - cli-provider.ts NAO foi modificado neste plano: `git diff --name-only ankinator-app/server/src/core/providers/cli-provider.ts` retorna vazio.
  </acceptance_criteria>
  <done>Lockfile aplicado nos dois workspaces; build server verde (src/ sem import pendente); baseline do CliProvider gravado; smoke-runner NAO criado aqui; cli-provider.ts intacto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| processo Node -> `claude` CLI (spawn) | Unico ponto que cruza fronteira de processo; reusa o mecanismo existente, sem mudanca nesta fase. Apenas exercitado se um PDF de baseline existir. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | smoke-cli.ts (spawn via CliProvider existente) | accept | App local single-user; o smoke usa o spawn existente (array de args + prompt por stdin); sem input externo nesta fase; nenhum codigo novo. |
| T-01-SC | Tampering | npm ci (sem novos pacotes) | accept | Nenhum pacote novo instalado; `npm ci` apenas aplica o lockfile existente. RESEARCH "Package Legitimacy Audit": N/A — sem `npm install`. |

Nota: nenhuma nova superficie de ataque. Nenhum arquivo de codigo novo nesta wave (so dados de baseline). Sanitizacao de SVG e diferida para a Fase 4 (IMG-02).
</threat_model>

<verification>
- `cd ankinator-app/server && npm run build` sai 0 apos `npm ci` (src/ sem import pendente — base do blocker fechado).
- `smoke-runner.ts` NAO existe nesta wave (movido para o Plano 02).
- `baseline-cli.txt` existe.
- cli-provider.ts intacto (diff vazio).
</verification>

<success_criteria>
- Lockfile aplicado nos dois workspaces; build server verde com a versao de tsc do lockfile.
- Baseline do comportamento atual do CliProvider gravado (real ou simbolico, documentado).
- Wave 0 e 100% build-safe: nenhum arquivo `.ts` novo em `src/`; `npm run build` sai 0 em todo limite de tarefa.
</success_criteria>

<output>
Create `.planning/phases/01-andaime-dos-especialistas/01-01-SUMMARY.md` when done
</output>
</content>
</invoke>
