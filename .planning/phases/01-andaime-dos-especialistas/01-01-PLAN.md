---
phase: 01-andaime-dos-especialistas
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - ankinator-app/server/src/scripts/smoke-runner.ts
  - .planning/phases/01-andaime-dos-especialistas/baseline-cli.txt
autonomous: true
requirements: [SPEC-01]
must_haves:
  truths:
    - "Existe um baseline capturado do smoke-cli.ts ANTES de qualquer refatoração do CliProvider, para comparar comportamento depois."
    - "Existe um smoke-runner.ts que (após Wave 1) exercita runClaudeCli isolado e loadPrompt dos 5 nomes canônicos."
    - "As dependências dos dois workspaces estão alinhadas ao lockfile (npm ci), removendo a divergência de versão do tsc antes de medir build."
  artifacts:
    - path: "ankinator-app/server/src/scripts/smoke-runner.ts"
      provides: "Smoke script que valida runner + loadPrompt cross-mode (tsx e node dist)"
      contains: "runClaudeCli"
    - path: ".planning/phases/01-andaime-dos-especialistas/baseline-cli.txt"
      provides: "Saída de referência do CliProvider atual (pre-refator) para regressao SPEC-01"
  key_links:
    - from: "ankinator-app/server/src/scripts/smoke-runner.ts"
      to: "ankinator-app/server/src/core/specialists/runner.ts"
      via: "import com extensao .js (NodeNext)"
      pattern: "specialists/runner.js"
---

<objective>
Wave 0 de infraestrutura de validacao. Esta fase e puro andaime e NAO tem framework de teste — a convencao do projeto e smoke script `.ts` via `tsx` + gate `npm run build`. Antes de refatorar o `CliProvider` (Plano 02), precisamos de (a) um baseline do comportamento atual do CLI para provar "sem regressao" depois, (b) o esqueleto do `smoke-runner.ts` que validara o runner extraido e o `loadPrompt`, e (c) alinhar as dependencias ao lockfile (tsc instalado 6.0.3 vs `^5.9.3` declarado).

Purpose: Fechar os Wave 0 Gaps do 01-VALIDATION.md — sem baseline, a regressao SPEC-01 nao e demonstravel; sem npm ci, o gate de build mede uma versao de tsc nao-deterministica.
Output: `smoke-runner.ts` (esqueleto que importa os modulos da Wave 1, ainda nao existentes — por isso este plano NAO roda o smoke-runner, apenas o cria), baseline-cli.txt, lockfile aplicado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-andaime-dos-especialistas/01-VALIDATION.md
@.planning/phases/01-andaime-dos-especialistas/01-PATTERNS.md

<interfaces>
<!-- Contratos que os modulos da Wave 1 (Plano 02) vao expor; o smoke-runner e escrito contra eles. -->
<!-- O smoke-runner importa estes simbolos; eles SO existirao apos o Plano 02. Logo, este plano NAO executa o smoke-runner. -->

De ankinator-app/server/src/core/specialists/runner.ts (criado no Plano 02):
  export function runClaudeCli(input: { systemPrompt: string; userMessage: string; model?: string }): Promise<string>

De ankinator-app/server/src/core/specialists/prompt-loader.ts (criado no Plano 02):
  export function loadPrompt(nome: string): string
  // nomes canonicos validos (D-07): anki-orchestrator, deck-classifier, card-builder, mnemonic, mnemonic-image
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alinhar dependencias ao lockfile (npm ci) e capturar baseline do CliProvider</name>
  <read_first>
    - ankinator-app/server/src/scripts/smoke-cli.ts (molde; ja existe — uso: `tsx src/scripts/smoke-cli.ts <pdf>`)
    - ankinator-app/server/package.json (scripts: build/start/smoke)
    - .planning/phases/01-andaime-dos-especialistas/01-RESEARCH.md secao "Open Questions" item 1 (divergencia tsc 6.0.3 vs ^5.9.3) e secao "Wave 0 Gaps"
  </read_first>
  <action>
    Rodar `npm ci` em `ankinator-app/server` e `ankinator-app/web` para travar as versoes no lockfile (resolve a divergencia tsc 6.0.3 vs ^5.9.3 antes de qualquer medicao de build, conforme RESEARCH Open Question 1).
    Em seguida capturar o baseline do CliProvider ATUAL (antes de qualquer refatoracao — pre-requisito de SPEC-01 "sem regressao"): localizar um PDF curto de teste no repo (procurar com `find . -name '*.pdf' -not -path '*/node_modules/*'`); se nenhum existir, registrar no SUMMARY que o baseline foi capturado de forma simbolica e que a comparacao de regressao SPEC-01 sera feita manualmente no checkpoint do Plano 04 com um PDF fornecido pelo usuario. Se um PDF existir, rodar `cd ankinator-app/server && npx tsx src/scripts/smoke-cli.ts <pdf>` e gravar a saida em `.planning/phases/01-andaime-dos-especialistas/baseline-cli.txt` (contagem de questoes + forma `[tipo] Q:/A:` e o que importa comparar, nao o texto exato — a geracao do LLM nao e deterministica; documentar isso no topo do arquivo de baseline).
    NAO refatorar nada do CliProvider neste plano — apenas capturar o estado atual.
  </action>
  <verify>
    <automated>cd ankinator-app/server && npm run build 2>&1 | tail -3; echo "build exit: ${PIPESTATUS[0]:-$?}"; test -f ../../.planning/phases/01-andaime-dos-especialistas/baseline-cli.txt && echo "baseline ok"</automated>
  </verify>
  <acceptance_criteria>
    - `npm ci` executado com sucesso em ankinator-app/server e ankinator-app/web (sem erro de resolucao).
    - `cd ankinator-app/server && npm run build` sai com codigo 0 (compilacao verde, agora com versao de tsc do lockfile).
    - `.planning/phases/01-andaime-dos-especialistas/baseline-cli.txt` existe; se um PDF de teste foi encontrado, contem a saida do smoke-cli (contagem + forma das questoes); se nao, contem a nota de baseline simbolico + plano de comparacao manual.
    - cli-provider.ts NAO foi modificado neste plano: `git diff --name-only ankinator-app/server/src/core/providers/cli-provider.ts` retorna vazio.
  </acceptance_criteria>
  <done>Lockfile aplicado nos dois workspaces; build server verde; baseline do CliProvider gravado; cli-provider.ts intacto.</done>
</task>

<task type="auto">
  <name>Task 2: Criar o esqueleto do smoke-runner.ts (molde smoke-cli.ts)</name>
  <read_first>
    - ankinator-app/server/src/scripts/smoke-cli.ts (molde exato: import com `.js`, argv guard, timing, logs PT com console.log, top-level await ESM)
    - .planning/phases/01-andaime-dos-especialistas/01-PATTERNS.md secao "smoke-runner.ts" (estrutura a copiar) e secao "Smoke script como teste"
  </read_first>
  <action>
    Criar `ankinator-app/server/src/scripts/smoke-runner.ts` seguindo o molde de `smoke-cli.ts` (mesmas convencoes: imports relativos com extensao `.js`, top-level `await` ESM, logs em PT, timing com `Date.now()`).
    O script deve: (1) iterar os 5 nomes canonicos (`anki-orchestrator`, `deck-classifier`, `card-builder`, `mnemonic`, `mnemonic-image`) chamando `loadPrompt(nome)` e logando nome + tamanho em chars (valida o Pitfall 1 cross-mode); (2) chamar `runClaudeCli({ systemPrompt: 'Responda apenas "ok".', userMessage: 'ping' })` e logar o texto retornado + tempo decorrido.
    Importar `runClaudeCli` de `../core/specialists/runner.js` e `loadPrompt` de `../core/specialists/prompt-loader.js` (extensao `.js` obrigatoria por NodeNext, mesmo apontando para `.ts`).
    IMPORTANTE: estes modulos so existem apos o Plano 02. Portanto este plano NAO executa o smoke-runner (ele falharia ao importar modulos inexistentes). O smoke-runner e validado no Plano 04 (Wave 2), em modo `tsx` (src) E `node dist` (prod), para fechar o Pitfall 1. Registrar isso como comentario no topo do arquivo.
  </action>
  <verify>
    <automated>test -f ankinator-app/server/src/scripts/smoke-runner.ts && grep -q 'runClaudeCli' ankinator-app/server/src/scripts/smoke-runner.ts && grep -q 'loadPrompt' ankinator-app/server/src/scripts/smoke-runner.ts && grep -c 'specialists/runner.js' ankinator-app/server/src/scripts/smoke-runner.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ankinator-app/server/src/scripts/smoke-runner.ts` existe.
    - Contem chamada a `loadPrompt` iterando os 5 nomes canonicos e chamada a `runClaudeCli`.
    - Importa `runClaudeCli` de `../core/specialists/runner.js` e `loadPrompt` de `../core/specialists/prompt-loader.js` (extensao `.js`).
    - Comentario no topo registra que o script so e executavel apos o Plano 02 e que o Plano 04 o roda em modo tsx E node dist.
  </acceptance_criteria>
  <done>smoke-runner.ts criado contra os contratos da Wave 1, com extensoes .js corretas; nao executado (modulos da Wave 1 ainda nao existem).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| processo Node -> `claude` CLI (spawn) | Unico ponto que cruza fronteira de processo; reusa o mecanismo existente, sem mudanca nesta fase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | smoke-runner.ts (spawn via runner futuro) | accept | App local single-user; smoke usa spawn com array de args + prompt por stdin (heranca do CliProvider); sem input externo nesta fase. |
| T-01-SC | Tampering | npm ci (sem novos pacotes) | accept | Nenhum pacote novo instalado; `npm ci` apenas aplica o lockfile existente. RESEARCH "Package Legitimacy Audit": N/A — sem `npm install`. |

Nota: nenhuma nova superficie de ataque. O runner (Plano 02) reusa o spawn do CliProvider (array de args, prompt por stdin — sem shell string). Sanitizacao de SVG e diferida para a Fase 4 (IMG-02).
</threat_model>

<verification>
- `cd ankinator-app/server && npm run build` sai 0 apos `npm ci`.
- `smoke-runner.ts` existe e referencia os contratos `runClaudeCli`/`loadPrompt` com extensao `.js`.
- `baseline-cli.txt` existe.
- cli-provider.ts intacto (diff vazio).
</verification>

<success_criteria>
- Lockfile aplicado nos dois workspaces; build server verde com a versao de tsc do lockfile.
- Baseline do comportamento atual do CliProvider gravado (real ou simbolico, documentado).
- Esqueleto do smoke-runner.ts pronto para a Wave 1, escrito contra os contratos definidos no `<interfaces>`.
</success_criteria>

<output>
Create `.planning/phases/01-andaime-dos-especialistas/01-01-SUMMARY.md` when done
</output>
