---
phase: 01-andaime-dos-especialistas
plan: 02
type: execute
wave: 1
depends_on: [01-01]
files_modified:
  - ankinator-app/server/src/core/specialists/runner.ts
  - ankinator-app/server/src/core/specialists/prompt-loader.ts
  - ankinator-app/server/src/core/specialists/prompts/anki-orchestrator.md
  - ankinator-app/server/src/core/specialists/prompts/deck-classifier.md
  - ankinator-app/server/src/core/specialists/prompts/card-builder.md
  - ankinator-app/server/src/core/specialists/prompts/mnemonic.md
  - ankinator-app/server/src/core/specialists/prompts/mnemonic-image.md
  - ankinator-app/server/src/core/providers/cli-provider.ts
  - ankinator-app/server/src/scripts/smoke-runner.ts
autonomous: true
requirements: [SPEC-01, SPEC-02]
must_haves:
  truths:
    - "Existe runClaudeCli({systemPrompt,userMessage,model?}) que envia um prompt de especialista ao claude CLI (assinatura) e retorna a saida (texto do envelope JSON), reusando o spawn do CliProvider."
    - "Existe um helper PURO buildSpawnArgs({systemPrompt,userMessage,model}) que devolve { bin, args, cwd, env } sem spawnar — permite assertion deterministica (CLI-free) de nao-regressao (args/cwd/env identicos ao CliProvider.invoke() pre-refator)."
    - "O CliProvider atual delega para runClaudeCli passando exatamente SYSTEM_FIDELITY, sem mudanca de comportamento observavel."
    - "Cada especialista tem UM .md canonico unico em prompts/; loadPrompt(nome) o le em runtime resolvendo SEMPRE de src/ (nunca de dist/), em modo tsx e node dist."
    - "O smoke-runner.ts e criado APOS runner.ts e prompt-loader.ts existirem (mesmo plano), de modo que src/ nunca tem import pendente em nenhum limite de tarefa (build verde sempre)."
  artifacts:
    - path: "ankinator-app/server/src/core/specialists/runner.ts"
      provides: "Funcao reutilizavel runClaudeCli extraida do CliProvider.invoke() + helper puro buildSpawnArgs (introspecao de args/cwd/env)"
      exports: ["runClaudeCli", "RunClaudeCliInput", "buildSpawnArgs", "SpawnPlan"]
    - path: "ankinator-app/server/src/core/specialists/prompt-loader.ts"
      provides: "loadPrompt(nome) com cache e allowlist, resolvendo .md em src/"
      exports: ["loadPrompt"]
    - path: "ankinator-app/server/src/scripts/smoke-runner.ts"
      provides: "Smoke script que valida runner + loadPrompt cross-mode + assertion deterministica de args (executado no Plano 04)"
      contains: "runClaudeCli"
    - path: "ankinator-app/server/src/core/specialists/prompts/anki-orchestrator.md"
      provides: "Fonte unica do especialista orquestrador"
    - path: "ankinator-app/server/src/core/specialists/prompts/deck-classifier.md"
      provides: "Fonte unica do especialista classificador de deck/tags"
    - path: "ankinator-app/server/src/core/specialists/prompts/card-builder.md"
      provides: "Fonte unica do especialista construtor de card educativo"
    - path: "ankinator-app/server/src/core/specialists/prompts/mnemonic.md"
      provides: "Fonte unica do especialista de mnemonicos"
    - path: "ankinator-app/server/src/core/specialists/prompts/mnemonic-image.md"
      provides: "Fonte unica do especialista de imagem SVG de mnemonico"
  key_links:
    - from: "ankinator-app/server/src/core/providers/cli-provider.ts"
      to: "ankinator-app/server/src/core/specialists/runner.ts"
      via: "import runClaudeCli (.js) e delegacao no invoke()"
      pattern: "runClaudeCli\\(\\{ systemPrompt: SYSTEM_FIDELITY"
    - from: "ankinator-app/server/src/core/specialists/runner.ts"
      to: "ankinator-app/server/src/core/specialists/runner.ts (buildSpawnArgs)"
      via: "runClaudeCli usa buildSpawnArgs para montar args/cwd/env (fonte unica do plano de spawn)"
      pattern: "buildSpawnArgs\\("
    - from: "ankinator-app/server/src/core/specialists/prompt-loader.ts"
      to: "src/core/specialists/prompts/*.md"
      via: "readFileSync resolvido em src/ (nao dist/)"
      pattern: "readFileSync"
    - from: "ankinator-app/server/src/scripts/smoke-runner.ts"
      to: "ankinator-app/server/src/core/specialists/runner.ts"
      via: "import com extensao .js (NodeNext) — criado APOS runner.ts no mesmo plano"
      pattern: "specialists/runner.js"
---

<objective>
Coracao do andaime (SPEC-01 + SPEC-02). Extrair o mecanismo de spawn do `claude -p` do `CliProvider.invoke()` para uma funcao livre reutilizavel `runClaudeCli`, e fazer o `CliProvider` passar a delegar para ela SEM mudar comportamento (D-01/D-02/D-03). Para tornar a nao-regressao VERIFICAVEL sem spawnar o CLI (Pitfall 2), extrair tambem um helper PURO `buildSpawnArgs` que devolve `{ bin, args, cwd, env }` — `runClaudeCli` consome esse helper, e a assertion deterministica do Plano 04 o introspeciona. Criar o `loadPrompt(nome)` que le os 5 `.md` canonicos resolvendo sempre da arvore `src/` (Pitfall 1: o build `tsc` NAO copia `.md` para `dist/`). Criar os 5 `.md` canonicos (fonte unica do texto de cada especialista — D-04/D-07). Por fim, criar o `smoke-runner.ts` (MOVIDO da Wave 0 para ca por causa do blocker: ele importa `runClaudeCli`/`loadPrompt`, que so existem apos as Tasks 1-2 deste plano; cria-lo na Wave 0 deixaria `src/` com import pendente — TS2307 — porque o tsconfig inclui todo `src/**/*.ts` sem `exclude`).

Purpose: Esta e a fundacao que todas as fases 3-5 vao reusar. O risco central nao e construir algo novo — e introduzir regressao ao reusar (Pitfall 2). A refatoracao do CliProvider deve ser comportamentalmente transparente (mesmos args, cwd, env, timeout, parse de envelope, fallback de texto puro), e isso agora e ASSERTAVEL deterministicamente via `buildSpawnArgs`. ORDEM OBRIGATORIA das tasks: Task 1 (runner.ts) -> Task 2 (prompt-loader.ts + .md) -> Task 3 (smoke-runner.ts), para que `src/` nunca tenha import dangling em nenhum limite de tarefa (build verde sempre).
Output: modulo `specialists/` com runner (+ helper puro de args) + prompt-loader + 5 prompts; CliProvider delegando; smoke-runner.ts escrito contra os contratos ja existentes (nao executado aqui — executado no Plano 04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-andaime-dos-especialistas/01-CONTEXT.md
@.planning/phases/01-andaime-dos-especialistas/01-RESEARCH.md
@.planning/phases/01-andaime-dos-especialistas/01-PATTERNS.md

<interfaces>
<!-- Codigo EXATO a extrair, verificado linha a linha no codebase. -->

Origem do mecanismo (cli-provider.ts:17-26, :43-98) — extrair byte-a-byte:
  const CLI_BIN = process.env.ANKINATOR_CLAUDE_BIN?.trim() || 'claude';
  const CALL_TIMEOUT_MS = Number(process.env.ANKINATOR_CLI_TIMEOUT_MS) || 180_000;
  interface CliEnvelope { type?; subtype?; is_error?; result?; error? }
  args = ['-p','--output-format','json','--model', model, '--system-prompt', <systemPrompt>,
          '--exclude-dynamic-system-prompt-sections','--strict-mcp-config']
  spawn(CLI_BIN, args, { cwd: os.tmpdir(), stdio:['pipe','pipe','pipe'], env:{...process.env} })
  timeout -> child.kill('SIGKILL'); parse JSON.parse(stdout) as CliEnvelope; fallback catch -> resolve(stdout)
  child.stdin.write(userMessage); child.stdin.end();

Helper puro a extrair (introspecao para a assertion deterministica do Plano 04 — Warning 2):
  export interface SpawnPlan { bin: string; args: string[]; cwd: string; env: NodeJS.ProcessEnv }
  export function buildSpawnArgs({ systemPrompt, userMessage, model }: Required<Pick<RunClaudeCliInput,'systemPrompt'|'userMessage'|'model'>>): SpawnPlan
  // monta { bin: CLI_BIN, args: [...na ordem exata...], cwd: os.tmpdir(), env: { ...process.env } }
  // NAO spawna nada. runClaudeCli({...}) chama buildSpawnArgs(...) e entao spawn(plan.bin, plan.args, { cwd: plan.cwd, env: plan.env, stdio: [...] }).

Assinatura nova (D-02):
  runClaudeCli({ systemPrompt: string; userMessage: string; model?: string }): Promise<string>
  // retorna env.result (texto). NAO parseia questoes — isso fica em parseQuestoesJson.

Padrao de resolucao de path ESM (config.ts:5-8) — COPIAR esta forma, NUNCA __dirname:
  import path from 'node:path';
  import { fileURLToPath } from 'node:url';
  const here = path.dirname(fileURLToPath(import.meta.url));

CliProvider apos refator (cli-provider.ts) — invoke() vira delegacao de 1 linha:
  import { runClaudeCli } from '../specialists/runner.js';  // .js obrigatorio (NodeNext)
  private invoke(userMessage, model) { return runClaudeCli({ systemPrompt: SYSTEM_FIDELITY, userMessage, model }); }

smoke-runner.ts (Task 3 deste plano) importa os contratos das Tasks 1-2:
  import { runClaudeCli } from '../core/specialists/runner.js';
  import { loadPrompt } from '../core/specialists/prompt-loader.js';
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extrair runClaudeCli + helper puro buildSpawnArgs para specialists/runner.ts e refatorar CliProvider para delegar</name>
  <read_first>
    - ankinator-app/server/src/core/providers/cli-provider.ts (codigo a extrair: linhas 17-18 consts, 20-26 CliEnvelope, 43-98 invoke())
    - ankinator-app/server/src/core/prompts.ts (SYSTEM_FIDELITY linha 14+ — segue importado pelo CliProvider, passado como systemPrompt)
    - .planning/phases/01-andaime-dos-especialistas/01-PATTERNS.md secao "runner.ts" e "cli-provider.ts (MODIFY)" (invariantes de nao-regressao)
    - .planning/phases/01-andaime-dos-especialistas/01-RESEARCH.md "Pitfall 2" (regressao silenciosa no CliProvider) e "Code Examples -> runner.ts"
  </read_first>
  <action>
    Criar `ankinator-app/server/src/core/specialists/runner.ts` movendo PARA LA: as consts `CLI_BIN` e `CALL_TIMEOUT_MS` (cli-provider.ts:17-18), a interface `CliEnvelope` (cli-provider.ts:20-26) e o corpo do `invoke()` (cli-provider.ts:44-97) como funcao livre exportada `runClaudeCli({ systemPrompt, userMessage, model = 'sonnet' }): Promise<string>`. O unico ponto parametrizado e o `systemPrompt` (antes fixo em `SYSTEM_FIDELITY`, agora vem do input). Exportar tambem a interface `RunClaudeCliInput`. Imports: `spawn` de `node:child_process`, `os` de `node:os`.
    HELPER PURO (Warning 2 — assertion deterministica de nao-regressao): extrair a montagem do plano de spawn para uma funcao PURA exportada `buildSpawnArgs({ systemPrompt, userMessage, model }): SpawnPlan` que NAO spawna nada — ela apenas devolve `{ bin: CLI_BIN, args: [<array na ordem exata>], cwd: os.tmpdir(), env: { ...process.env } }`. Exportar a interface `SpawnPlan { bin: string; args: string[]; cwd: string; env: NodeJS.ProcessEnv }`. O `runClaudeCli` DEVE usar `buildSpawnArgs(...)` como fonte unica do plano (chama o helper e passa `plan.bin`/`plan.args`/`plan.cwd`/`plan.env` ao `spawn`), garantindo que o que a assertion introspeciona e exatamente o que roda. `userMessage` continua indo por stdin (NAO entra em `args`); inclui-lo em `buildSpawnArgs` so para a assertion ter o input completo e poder validar que ele NAO vaza para `args` (continua via stdin).
    INVARIANTES DE NAO-REGRESSAO (Pitfall 2) — manter byte-a-byte identicos ao CliProvider.invoke() PRE-REFATOR: o array `args` na ordem exata (`-p`, `--output-format`, `json`, `--model`, model, `--system-prompt`, systemPrompt, `--exclude-dynamic-system-prompt-sections`, `--strict-mcp-config`); `cwd: os.tmpdir()`; `env: { ...process.env }`; `stdio: ['pipe','pipe','pipe']`; o `setTimeout` com `child.kill('SIGKILL')` e a mesma mensagem PT; os handlers `stdout`/`stderr`/`error`/`close`; o parse `JSON.parse(stdout) as CliEnvelope` com `if (env.is_error)`; o `resolve(env.result ?? '')`; o fallback `catch { resolve(stdout); }`; e `child.stdin.write(userMessage); child.stdin.end()`. NAO montar comando como shell string (manter array de args + stdin — mitigacao de command injection herdada).
    Refatorar `ankinator-app/server/src/core/providers/cli-provider.ts`: remover as consts `CLI_BIN`/`CALL_TIMEOUT_MS`, a interface `CliEnvelope` e o imports `spawn`/`os` que ficaram sem uso; adicionar `import { runClaudeCli } from '../specialists/runner.js';`; substituir o corpo de `invoke()` por `return runClaudeCli({ systemPrompt: SYSTEM_FIDELITY, userMessage, model });`. NAO tocar em `generateForChunk()` (linhas 37-41) nem na importacao de `SYSTEM_FIDELITY` de `../prompts.js`.
  </action>
  <verify>
    <automated>cd ankinator-app/server && npm run build 2>&1 | tail -3; echo "build exit: ${PIPESTATUS[0]:-$?}"; grep -q 'export function runClaudeCli' src/core/specialists/runner.ts && grep -q 'export function buildSpawnArgs' src/core/specialists/runner.ts && grep -q "runClaudeCli({ systemPrompt: SYSTEM_FIDELITY" src/core/providers/cli-provider.ts && echo "delegation+helper ok"</automated>
  </verify>
  <acceptance_criteria>
    - `src/core/specialists/runner.ts` exporta `runClaudeCli`, `RunClaudeCliInput`, `buildSpawnArgs` e `SpawnPlan`; o array de args contem `--exclude-dynamic-system-prompt-sections` e `--strict-mcp-config`, `cwd: os.tmpdir()`, e o fallback `catch { resolve(stdout); }`.
    - `buildSpawnArgs` e PURO (nao chama `spawn`); `runClaudeCli` o consome como fonte unica do plano de spawn (`grep -c 'spawn(' src/core/specialists/runner.ts` retorna exatamente 1 — apenas dentro de runClaudeCli, nunca em buildSpawnArgs).
    - `cli-provider.ts` importa `runClaudeCli` de `../specialists/runner.js` e `invoke()` delega com `systemPrompt: SYSTEM_FIDELITY`.
    - `cli-provider.ts` NAO contem mais `spawn(` nem `interface CliEnvelope` (movidos para o runner): `grep -c 'spawn(' src/core/providers/cli-provider.ts` retorna 0.
    - `cd ankinator-app/server && npm run build` sai com codigo 0.
  </acceptance_criteria>
  <done>runClaudeCli extraido com invariantes preservados + helper puro buildSpawnArgs (introspectavel para a assertion); CliProvider delega passando SYSTEM_FIDELITY; build server verde.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar prompt-loader.ts (resolve em src/, allowlist, cache) e os 5 .md canonicos</name>
  <read_first>
    - ankinator-app/server/src/config.ts (linhas 5-8: padrao fileURLToPath(import.meta.url) — copiar esta forma)
    - ankinator-app/server/src/core/prompts.ts (SYSTEM_FIDELITY — tom/idioma PT, foco concurso publico; herdar o tom nos .md canonicos)
    - .planning/phases/01-andaime-dos-especialistas/01-PATTERNS.md secao "prompt-loader.ts" e "prompts/*.md"
    - .planning/phases/01-andaime-dos-especialistas/01-RESEARCH.md "Pitfall 1" (.md ausente em dist/), "Assumptions Log -> A1" (deploy precisa enviar a arvore src/ com os .md) e "Code Examples -> prompt-loader.ts"
    - CLAUDE.md (raiz do projeto: "texto normal -> questoes para estudar por questoes; criar questoes novas alem de aproveitar prontas")
  </read_first>
  <action>
    Criar `ankinator-app/server/src/core/specialists/prompt-loader.ts` exportando `loadPrompt(nome: string): string`. Imports: `readFileSync` de `node:fs`, `path` de `node:path`, `fileURLToPath` de `node:url`. Definir uma allowlist `NOMES` com os 5 nomes canonicos (D-07) e VALIDAR `nome` contra ela ANTES do `readFileSync` (mitigacao de path traversal — Security Domain). Usar `cache: Map<string,string>` (carrega 1x por nome).
    INVARIANTE CRITICO (Pitfall 1): resolver o `.md` SEMPRE da arvore `src/`, NUNCA de `dist/`, porque o build `tsc` puro nao copia `.md`. Tecnica (Claude's discretion entre as duas, ambas validas — escolher uma): (a) a partir de `path.dirname(fileURLToPath(import.meta.url))`, se o caminho contiver um segmento `dist`, troca-lo por `src` e compor `core/specialists/prompts`; OU (b) subir diretorios ate achar o `package.json` do server e compor `src/core/specialists/prompts`. Compor `path.join(base, 'prompts', nome + '.md')` e ler com `readFileSync(file, 'utf8')`. Documentar a tecnica escolhida em comentario.
    REGISTRO NO SUMMARY (Warning 3 — obrigatorio, nao so comentario no codigo): no `01-02-SUMMARY.md`, escrever (1) QUAL das duas tecnicas de resolucao foi escolhida e por que, e (2) o caveat da Assumption A1: o deploy DEVE enviar a arvore `src/` com os `.md` (o build `tsc` NAO copia `.md` para `dist/`); se algum dia houver deploy "dist-only", trocar para o Plano B (copiar `.md` para `dist/` no build). Esta nota vive no SUMMARY, alem do comentario no codigo.
    Criar os 5 `.md` canonicos em `ankinator-app/server/src/core/specialists/prompts/` (D-04/D-07): `anki-orchestrator.md`, `deck-classifier.md`, `card-builder.md`, `mnemonic.md`, `mnemonic-image.md`. Cada um e a FONTE UNICA do system prompt/conhecimento do especialista, em PT, no tom de `SYSTEM_FIDELITY` (foco concurso publico; aproveitar questoes prontas E criar novas). Conteudo por especialista: anki-orchestrator = decidir por-card quais estagios rodar + agregar com principios Anki; deck-classifier = gerar hierarquia `Materia::Assunto::Subtopico` + tags (banca/ano/nivel/tema); card-builder = reescrever para atomicidade (minimum information) + explicacao curta + fonte no verso; mnemonic = gerar mnemonicos (acronimo/historia/palacio) para cards de memorizacao; mnemonic-image = gerar SVG autocontido (sem script/URLs externas) ilustrando o mnemonico. IMPORTANTE: estes .md NAO sao chamados pelo pipeline nesta fase — sao so a fonte; a logica que os invoca e das fases 3-5. NAO criar nenhum .md com nome fora da allowlist.
  </action>
  <verify>
    <automated>cd ankinator-app/server && for n in anki-orchestrator deck-classifier card-builder mnemonic mnemonic-image; do test -f src/core/specialists/prompts/$n.md || { echo "MISSING $n.md"; exit 1; }; done; npm run build 2>&1 | tail -2; echo "build exit: ${PIPESTATUS[0]:-$?}"; grep -q 'export function loadPrompt' src/core/specialists/prompt-loader.ts && echo "loader ok"</automated>
  </verify>
  <acceptance_criteria>
    - Os 5 `.md` existem em `src/core/specialists/prompts/` com os nomes canonicos exatos (D-07), conteudo em PT, nao-vazios.
    - `prompt-loader.ts` exporta `loadPrompt`, valida `nome` contra a allowlist dos 5 nomes antes do `readFileSync`, e usa cache em Map.
    - A resolucao de path le de `src/` (referencia a `import.meta.url`/`fileURLToPath`); NAO usa `__dirname`; comentario documenta a tecnica dist->src ou busca por package.json.
    - O SUMMARY deste plano registra a tecnica de `loadPrompt` escolhida E o caveat A1 (deploy precisa enviar `src/` com os `.md`).
    - `cd ankinator-app/server && npm run build` sai com codigo 0.
    - Verificacao real do Pitfall 1 (cross-mode tsx vs node dist) e do conteudo dos .md fica para o Plano 04 (smoke-runner em ambos os modos).
  </acceptance_criteria>
  <done>loadPrompt criado (allowlist + cache + resolve em src/); 5 .md canonicos criados como fonte unica; build server verde; tecnica + caveat A1 a registrar no SUMMARY.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Criar o esqueleto do smoke-runner.ts (APOS runner.ts/prompt-loader.ts — fecha o blocker)</name>
  <read_first>
    - ankinator-app/server/src/scripts/smoke-cli.ts (molde exato: import com `.js`, argv guard, timing, logs PT com console.log, top-level await ESM)
    - ankinator-app/server/src/core/specialists/runner.ts (criado na Task 1 — exporta runClaudeCli + buildSpawnArgs)
    - ankinator-app/server/src/core/specialists/prompt-loader.ts (criado na Task 2 — exporta loadPrompt)
    - .planning/phases/01-andaime-dos-especialistas/01-PATTERNS.md secao "smoke-runner.ts" (estrutura a copiar) e secao "Smoke script como teste"
  </read_first>
  <action>
    PRE-CONDICAO (blocker): esta task SO roda apos as Tasks 1 e 2 deste plano (runner.ts + prompt-loader.ts ja existem). Assim, ao criar o smoke-runner.ts, seus imports JA resolvem e `src/` permanece compilavel — `npm run build` continua saindo 0. (Foi por isso que este arquivo saiu da Wave 0.)
    Criar `ankinator-app/server/src/scripts/smoke-runner.ts` seguindo o molde de `smoke-cli.ts` (mesmas convencoes: imports relativos com extensao `.js`, top-level `await` ESM, logs em PT, timing com `Date.now()`).
    O script deve:
      (1) iterar os 5 nomes canonicos (`anki-orchestrator`, `deck-classifier`, `card-builder`, `mnemonic`, `mnemonic-image`) chamando `loadPrompt(nome)` e logando nome + tamanho em chars (valida o Pitfall 1 cross-mode);
      (2) ASSERTION DETERMINISTICA CLI-FREE (Warning 2): chamar `buildSpawnArgs({ systemPrompt: SYSTEM_FIDELITY, userMessage: '<fixo>', model: 'sonnet' })` e imprimir/asserir que `plan.args` esta na ordem canonica exata (`-p`,`--output-format`,`json`,`--model`,`sonnet`,`--system-prompt`,<systemPrompt>,`--exclude-dynamic-system-prompt-sections`,`--strict-mcp-config`), que `plan.cwd === os.tmpdir()`, que `plan.env` deriva de `process.env`, e que o `userMessage` NAO aparece em `plan.args` (vai por stdin). Importar `SYSTEM_FIDELITY` de `../core/prompts.js` para usar o MESMO systemPrompt que o CliProvider usa — assim a assertion compara o conjunto pos-refator com o conjunto pre-refator do `CliProvider.invoke()` (que tambem usava SYSTEM_FIDELITY na 6a posicao). Em caso de divergencia, `process.exit(1)` com mensagem clara. ESTA assertion NAO spawna o CLI — e o guard automatizado de SPEC-01 (Pitfall 2);
      (3) chamar `runClaudeCli({ systemPrompt: 'Responda apenas "ok".', userMessage: 'ping' })` e logar o texto retornado + tempo decorrido (esta parte SIM exercita o CLI e so e rodada no Plano 04).
    Importar `runClaudeCli`/`buildSpawnArgs` de `../core/specialists/runner.js`, `loadPrompt` de `../core/specialists/prompt-loader.js` e `SYSTEM_FIDELITY` de `../core/prompts.js` (extensao `.js` obrigatoria por NodeNext). Importar `os` de `node:os` para comparar `os.tmpdir()`.
    Comentario no topo: registrar que (a) a assertion de args (passo 2) e CLI-free e e o guard deterministico de nao-regressao SPEC-01; (b) o passo 3 exercita o CLI e e rodado no Plano 04 em modo `tsx` (src) E `node dist` (prod) para fechar o Pitfall 1.
  </action>
  <verify>
    <automated>cd ankinator-app/server && npm run build 2>&1 | tail -2; echo "build exit: ${PIPESTATUS[0]:-$?}"; test -f src/scripts/smoke-runner.ts && grep -q 'runClaudeCli' src/scripts/smoke-runner.ts && grep -q 'loadPrompt' src/scripts/smoke-runner.ts && grep -q 'buildSpawnArgs' src/scripts/smoke-runner.ts && grep -c 'specialists/runner.js' src/scripts/smoke-runner.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ankinator-app/server/src/scripts/smoke-runner.ts` existe e `cd ankinator-app/server && npm run build` sai 0 (imports resolvem — blocker fechado: criado apos runner.ts/prompt-loader.ts).
    - Contem chamada a `loadPrompt` iterando os 5 nomes canonicos, chamada a `buildSpawnArgs` com a assertion deterministica de args/cwd/env (passo 2), e chamada a `runClaudeCli` (passo 3).
    - Importa `runClaudeCli`/`buildSpawnArgs` de `../core/specialists/runner.js`, `loadPrompt` de `../core/specialists/prompt-loader.js` e `SYSTEM_FIDELITY` de `../core/prompts.js` (extensao `.js`).
    - Comentario no topo registra que o passo 2 (args) e CLI-free (guard SPEC-01) e que o passo 3 (CLI) roda no Plano 04 em modo tsx E node dist.
  </acceptance_criteria>
  <done>smoke-runner.ts criado APOS os contratos da Task 1-2 (build verde, sem import pendente — blocker fechado); inclui a assertion deterministica CLI-free de args (guard SPEC-01); execucao do CLI fica para o Plano 04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| processo Node -> `claude` CLI (spawn) | runClaudeCli reusa o spawn do CliProvider (array de args + prompt por stdin). |
| `nome` (parametro) -> filesystem (readFileSync) | loadPrompt le um arquivo do repo com base no `nome` recebido. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02a | Tampering | runClaudeCli (spawn) | mitigate | Manter spawn com array de args (NUNCA shell string) + prompt por stdin — heranca byte-a-byte do CliProvider; a assertion de `buildSpawnArgs` (smoke-runner passo 2) verifica deterministicamente que `userMessage` NAO vaza para `args` (continua via stdin). |
| T-01-02b | Tampering | loadPrompt(nome) | mitigate | Validar `nome` contra allowlist dos 5 nomes canonicos antes do readFileSync (evita path traversal); `nome` nunca vem de input de usuario nesta fase. |
| T-01-02c | Information Disclosure | prompts/*.md | mitigate | Os .md sao instrucoes de especialista; garantir que NAO contenham segredos/credenciais (sem ANTHROPIC_API_KEY — o path de assinatura nao usa chave). |

Nota: sanitizacao de SVG (sem `<script>`/URLs externas) e diferida para a Fase 4 (IMG-02); aqui o mnemonic-image.md apenas descreve a intencao.
</threat_model>

<verification>
- `cd ankinator-app/server && npm run build` sai 0 apos cada uma das 3 tasks (src/ nunca com import pendente — blocker fechado).
- `runner.ts` exporta `runClaudeCli` + `buildSpawnArgs`; `cli-provider.ts` delega com `SYSTEM_FIDELITY` e nao contem mais `spawn(`.
- Os 5 `.md` existem com nomes canonicos; `prompt-loader.ts` valida allowlist e le de `src/`.
- `smoke-runner.ts` existe, importa os contratos das Tasks 1-2 + SYSTEM_FIDELITY, e contem a assertion deterministica de args (guard SPEC-01 CLI-free).
- (Wave merge) A execucao do smoke-runner (cross-mode + ping ao CLI) e a comparacao com `baseline-cli.txt` acontecem no Plano 04.
</verification>

<success_criteria>
- runClaudeCli reutilizavel extraido sem regressao; helper puro buildSpawnArgs introspectavel; CliProvider delega passando SYSTEM_FIDELITY.
- loadPrompt le os 5 .md canonicos resolvendo de src/ com allowlist + cache; tecnica + caveat A1 registrados no SUMMARY.
- 5 .md canonicos criados como fonte unica (D-04/D-07); nenhum estagio plugado no pipeline.
- smoke-runner.ts criado apos os contratos (blocker fechado) com assertion deterministica de args (guard SPEC-01).
- Build server verde em todo limite de tarefa.
</success_criteria>

<output>
Create `.planning/phases/01-andaime-dos-especialistas/01-02-SUMMARY.md` when done
</output>
</content>
