---
phase: 01-andaime-dos-especialistas
plan: 04
type: execute
wave: 2
depends_on: [01-02, 01-03]
files_modified:
  - ankinator-app/server/src/core/specialists/image-provider.ts
autonomous: false
requirements: [SPEC-01, SPEC-02, SPEC-04]
must_haves:
  truths:
    - "Existe a interface ImageProvider com a impl SvgClaudeImageProvider (usa runner + prompt canonico mnemonic-image) e a factory createImageProvider() retorna svg-claude por default; nenhum provider raster e implementado e nada e plugado no pipeline."
    - "loadPrompt resolve os 5 .md em AMBOS os modos: tsx (src) e node dist (prod) — fechando o Pitfall 1."
    - "Existe cobertura AUTOMATIZADA e CLI-free da nao-regressao SPEC-01: a assertion de args do smoke-runner (passo 2) prova que args/cwd/env de buildSpawnArgs sao byte-identicos ao conjunto pre-refator do CliProvider.invoke(), sem spawnar o CLI (guard de Pitfall 2)."
    - "O CliProvider refatorado continua gerando questoes equivalentes ao baseline (SPEC-01 sem regressao) e o fluxo atual (upload->generate->export) roda igual (PIPE-03); quando nao ha fixture PDF, o checkpoint humano e HARD-BLOCK ate um PDF ser fornecido."
  artifacts:
    - path: "ankinator-app/server/src/core/specialists/image-provider.ts"
      provides: "interface ImageProvider + SvgClaudeImageProvider + createImageProvider()"
      exports: ["ImageProvider", "SvgClaudeImageProvider", "createImageProvider"]
  key_links:
    - from: "ankinator-app/server/src/core/specialists/image-provider.ts"
      to: "ankinator-app/server/src/core/specialists/runner.ts"
      via: "import runClaudeCli (.js)"
      pattern: "runClaudeCli"
    - from: "ankinator-app/server/src/core/specialists/image-provider.ts"
      to: "ankinator-app/server/src/core/specialists/prompt-loader.ts"
      via: "loadPrompt('mnemonic-image')"
      pattern: "loadPrompt\\('mnemonic-image'\\)"
    - from: "ankinator-app/server/src/scripts/smoke-runner.ts (criado no Plano 02)"
      to: "ankinator-app/server/src/core/specialists/runner.ts (buildSpawnArgs)"
      via: "assertion deterministica CLI-free de args/cwd/env (guard SPEC-01)"
      pattern: "buildSpawnArgs\\("
---

<objective>
Fechamento do andaime (SPEC-04) + validacao cross-mode + guarda de regressao. (a) Criar a interface `ImageProvider` e a impl `SvgClaudeImageProvider` (usa o runner + o prompt canonico `mnemonic-image`), com factory `createImageProvider()` retornando `svg-claude` por default (D-08/D-09/D-10) — sem provider raster, sem plugar no pipeline. (b) Rodar a ASSERTION DETERMINISTICA de args (CLI-free) do `smoke-runner.ts` — guard automatizado de SPEC-01 (Pitfall 2): prova que `args`/`cwd`/`env` pos-refator sao byte-identicos ao conjunto pre-refator, sem spawnar o CLI. (c) Rodar o `smoke-runner.ts` em modo `tsx` (src) E `node dist` (prod) para provar que `loadPrompt` resolve os 5 `.md` nos dois modos (fecha o Pitfall 1). (d) Checkpoint de verificacao humana HARD-BLOCK: comparar a geracao pos-refator com o baseline (SPEC-01) e rodar o fluxo E2E (PIPE-03) — sem regressao; quando o baseline foi simbolico (sem fixture PDF), o checkpoint NAO libera ate o usuario fornecer um PDF.

Purpose: O `image-provider.ts` depende dos modulos do Plano 02 (runner + prompt-loader), por isso e Wave 2. Esta tambem e a hora de fechar a fase: dar cobertura AUTOMATIZADA a nao-regressao (a assertion de args nao depende do CLI nem de um PDF), validar o invariante de producao (`.md` em src/, nao dist/) e confirmar com o usuario que nada regrediu no fluxo real.
Output: image-provider.ts + assertion deterministica de args verde + smoke cross-mode verde + sign-off de regressao (hard-block).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-andaime-dos-especialistas/01-CONTEXT.md
@.planning/phases/01-andaime-dos-especialistas/01-RESEARCH.md
@.planning/phases/01-andaime-dos-especialistas/01-PATTERNS.md
@.planning/phases/01-andaime-dos-especialistas/01-VALIDATION.md

<interfaces>
<!-- Contratos ja criados no Plano 02 que o image-provider consome (imports com .js — NodeNext). -->
De ./runner.js:        export function runClaudeCli({ systemPrompt, userMessage, model? }): Promise<string>
De ./runner.js:        export function buildSpawnArgs({ systemPrompt, userMessage, model }): SpawnPlan  // { bin, args, cwd, env } — puro, nao spawna
De ./prompt-loader.js: export function loadPrompt(nome: string): string  // usar 'mnemonic-image'

<!-- Shape a criar (D-08/D-09/D-10), espelhando a factory de providers/index.ts:21-27 (default no fim). -->
export interface ImageProvider {
  readonly nome: string;
  generate(mnemonic: string, context: string): Promise<{ svg: string }>;
}
export class SvgClaudeImageProvider implements ImageProvider {
  readonly nome = 'svg-claude';
  async generate(mnemonic, context): Promise<{ svg: string }> { ... usa loadPrompt + runClaudeCli ... }
}
export function createImageProvider(): ImageProvider { return new SvgClaudeImageProvider(); } // TODO(v2): selecao por env

<!-- smoke-runner.ts foi criado no Plano 02 (movido da Wave 0 por causa do blocker de import pendente); aqui ele e EXECUTADO. -->
<!-- passo 1: loadPrompt dos 5 nomes (cross-mode) ; passo 2: assertion CLI-free de args (guard SPEC-01) ; passo 3: ping ao runClaudeCli (exercita o CLI). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar image-provider.ts (interface + SvgClaudeImageProvider + createImageProvider)</name>
  <read_first>
    - ankinator-app/server/src/core/providers/index.ts (createProvider linhas 21-27 — molde de factory, default no fim)
    - ankinator-app/server/src/core/providers/cli-provider.ts (class ... implements ... com `readonly nome` — molde de impl)
    - ankinator-app/server/src/core/specialists/runner.ts e prompt-loader.ts (contratos criados no Plano 02)
    - .planning/phases/01-andaime-dos-especialistas/01-PATTERNS.md secao "image-provider.ts"
    - .planning/phases/01-andaime-dos-especialistas/01-RESEARCH.md "Pattern 3", "Pitfall 5" (noUnusedParameters), "Code Examples -> image-provider.ts"
  </read_first>
  <action>
    Criar `ankinator-app/server/src/core/specialists/image-provider.ts`. Imports (NodeNext `.js`): `runClaudeCli` de `./runner.js`, `loadPrompt` de `./prompt-loader.js`.
    Definir e exportar `interface ImageProvider` com `readonly nome: string` e `generate(mnemonic: string, context: string): Promise<{ svg: string }>` (D-08).
    Definir e exportar `class SvgClaudeImageProvider implements ImageProvider` com `readonly nome = 'svg-claude'`; o metodo `generate` faz: `const systemPrompt = loadPrompt('mnemonic-image');` monta `userMessage` usando `mnemonic` E `context` (usar ambos os parametros de fato — Pitfall 5: nao deixar parametro nao usado), chama `const svg = await runClaudeCli({ systemPrompt, userMessage });` e retorna `{ svg }`. Adicionar comentario `// TODO(IMG-02 fase 4): sanitizar SVG (remover <script>/URLs externas)` (D-09: a impl existe mas a sanitizacao e da fase 4).
    Definir e exportar a factory `createImageProvider(): ImageProvider` que retorna `new SvgClaudeImageProvider()` por default, espelhando `createProvider()` (default no fim). Adicionar comentario `// TODO(v2): selecionar provider raster por env quando IMGR-01 existir` (D-10 — NAO implementar raster, D-09).
    NAO plugar `ImageProvider` em `generation.ts` nem nos exporters (fora de escopo — fases 3-5). A interface vive 100% no server.
  </action>
  <verify>
    <automated>cd ankinator-app/server && npm run build 2>&1 | tail -2; echo "build exit: ${PIPESTATUS[0]:-$?}"; grep -q 'export interface ImageProvider' src/core/specialists/image-provider.ts && grep -q "loadPrompt('mnemonic-image')" src/core/specialists/image-provider.ts && grep -q 'export function createImageProvider' src/core/specialists/image-provider.ts && echo "image-provider ok"; grep -c 'generation.ts\|exporters' src/core/specialists/image-provider.ts</automated>
  </verify>
  <acceptance_criteria>
    - `image-provider.ts` exporta `ImageProvider`, `SvgClaudeImageProvider` (nome `svg-claude`) e `createImageProvider()`.
    - `generate` usa `loadPrompt('mnemonic-image')` + `runClaudeCli`, consumindo `mnemonic` E `context` (sem parametro nao usado).
    - `createImageProvider()` retorna `SvgClaudeImageProvider` por default; comentario TODO(v2) para selecao por env.
    - Comentario TODO(IMG-02) marca a sanitizacao de SVG como diferida para a fase 4.
    - `image-provider.ts` NAO importa/pluga `generation.ts` nem exporters; `cd ankinator-app/server && npm run build` sai 0.
  </acceptance_criteria>
  <done>ImageProvider + SvgClaudeImageProvider + createImageProvider criados; build server verde; nao plugado no pipeline.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Guard deterministico CLI-free de SPEC-01 — assertion de args/cwd/env (smoke-runner passo 2)</name>
  <read_first>
    - ankinator-app/server/src/scripts/smoke-runner.ts (criado no Plano 02; passo 2 = assertion CLI-free de args)
    - ankinator-app/server/src/core/specialists/runner.ts (buildSpawnArgs — helper puro; SpawnPlan)
    - .planning/phases/01-andaime-dos-especialistas/01-RESEARCH.md "Pitfall 2" (regressao silenciosa no CliProvider — comportamento byte-a-byte)
    - .planning/phases/01-andaime-dos-especialistas/01-VALIDATION.md linha 01-SPEC-01c (assertion de args)
  </read_first>
  <action>
    Rodar a ASSERTION DETERMINISTICA de args (CLI-free — Warning 2): `cd ankinator-app/server && npx tsx src/scripts/smoke-runner.ts --assert-args` (ou a flag/segmento que o smoke-runner do Plano 02 expoe para rodar SO o passo 2 sem spawnar o CLI; se o smoke-runner sempre roda o passo 2 antes do passo 3, basta capturar a saida do passo 2). O passo 2 deve provar, sem spawnar o `claude` CLI, que `buildSpawnArgs({ systemPrompt: SYSTEM_FIDELITY, userMessage: '<fixo>', model: 'sonnet' })` produz: `args` na ordem canonica exata (`-p`,`--output-format`,`json`,`--model`,`sonnet`,`--system-prompt`,SYSTEM_FIDELITY,`--exclude-dynamic-system-prompt-sections`,`--strict-mcp-config`), `cwd === os.tmpdir()`, `env` derivado de `process.env`, e que `userMessage` NAO aparece em `args` (vai por stdin). Este e o conjunto byte-identico ao que o `CliProvider.invoke()` pre-refator montava — logo a ausencia de divergencia E a prova automatizada de SPEC-01 (Pitfall 2), independente do CLI e de qualquer PDF.
    Se a assertion falhar (qualquer diferenca de ordem/flag/cwd), a refatoracao do Plano 02 introduziu regressao — registrar como gap e NAO prosseguir ao checkpoint ate verde.
    Registrar o resultado da assertion no SUMMARY (qual conjunto de args foi verificado).
  </action>
  <verify>
    <automated>cd ankinator-app/server && npx tsx src/scripts/smoke-runner.ts --assert-args 2>&1 | tail -8; echo "assert-args exit: ${PIPESTATUS[0]:-$?}"</automated>
  </verify>
  <acceptance_criteria>
    - O passo de assertion do smoke-runner sai com codigo 0 SEM spawnar o `claude` CLI (nenhuma chamada de rede/quota; roda offline).
    - A assertion confirma `args` na ordem canonica exata (com `--exclude-dynamic-system-prompt-sections` e `--strict-mcp-config`), `cwd === os.tmpdir()`, `env` derivado de `process.env`, e `userMessage` ausente de `args` (via stdin).
    - Resultado registrado no SUMMARY como guard deterministico de SPEC-01 (Pitfall 2).
  </acceptance_criteria>
  <done>Nao-regressao SPEC-01 com cobertura AUTOMATIZADA e CLI-free (assertion de args/cwd/env byte-identica ao pre-refator); verde.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Validar loadPrompt cross-mode (tsx src E node dist) executando o smoke-runner</name>
  <read_first>
    - ankinator-app/server/src/scripts/smoke-runner.ts (criado no Plano 02; aqui passo 1 = loadPrompt cross-mode e EXECUTADO)
    - .planning/phases/01-andaime-dos-especialistas/01-RESEARCH.md "Pitfall 1" (.md ausente em dist/), "Assumptions Log -> A1" e §"Sampling Rate -> Phase gate"
    - .planning/phases/01-andaime-dos-especialistas/01-VALIDATION.md §"Wave 0 Requirements" (verificacao cross-mode)
  </read_first>
  <action>
    Fazer um build limpo do server para reproduzir producao: `cd ankinator-app/server && rm -rf dist && npm run build` (remove o artefato stale `dist/core/question-generator.js` notado na RESEARCH).
    Modo SRC (dev): rodar `cd ankinator-app/server && npx tsx src/scripts/smoke-runner.ts` — deve logar os 5 nomes com tamanho em chars > 0 (loadPrompt resolvendo de src/), rodar a assertion de args (passo 2, ja coberto na Task 2) e a resposta do runner ao ping (passo 3 — exercita o CLI).
    Modo DIST (prod): o ponto do Pitfall 1 e que o `.js` compilado em `dist/core/specialists/` resolva o `.md` de `src/`, nao de `dist/`. Validar isso executando o loadPrompt a partir do modulo COMPILADO: `cd ankinator-app/server && node --input-type=module -e "import('./dist/core/specialists/prompt-loader.js').then(m => { for (const n of ['anki-orchestrator','deck-classifier','card-builder','mnemonic','mnemonic-image']) console.log(n, m.loadPrompt(n).length); })"`. Cada nome deve imprimir um length > 0 (prova que o `.js` em dist/ resolveu o `.md` de src/ — Pitfall 1 fechado; confirma a Assumption A1: a arvore src/ acompanha o deploy).
    Se o modo DIST falhar com ENOENT, a tecnica de resolucao do `loadPrompt` (Plano 02) precisa de ajuste (heuristica dist->src ou Plano B de copiar `.md` para dist no build) — registrar como gap e nao prosseguir ao checkpoint ate verde.
    Registrar os resultados no SUMMARY.
  </action>
  <verify>
    <automated>cd ankinator-app/server && rm -rf dist && npm run build 2>&1 | tail -1; node --input-type=module -e "import('./dist/core/specialists/prompt-loader.js').then(m => { let ok=1; for (const n of ['anki-orchestrator','deck-classifier','card-builder','mnemonic','mnemonic-image']) { const L=m.loadPrompt(n).length; console.log(n, L); if(!L) ok=0; } process.exit(ok?0:1); }).catch(e=>{console.error(e.message);process.exit(1);})"</automated>
  </verify>
  <acceptance_criteria>
    - Build limpo (`rm -rf dist && npm run build`) sai 0; `dist/core/question-generator.js` stale nao reaparece.
    - Modo DIST: `loadPrompt(n)` a partir de `dist/core/specialists/prompt-loader.js` retorna length > 0 para os 5 nomes (Pitfall 1 fechado — `.md` resolvido de src/).
    - Modo SRC: `npx tsx src/scripts/smoke-runner.ts` loga os 5 nomes com tamanho > 0 e a resposta do runner (executado; resultado registrado no SUMMARY).
  </acceptance_criteria>
  <done>loadPrompt validado em tsx (src) e node dist (prod); Pitfall 1 fechado; build limpo verde.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint HARD-BLOCK — confirmar zero regressao com PDF real (SPEC-01 + PIPE-03)</name>
  <action>
    HARD-BLOCK (Warning 1): este checkpoint NAO pode ser auto-aprovado nem pulado (ignora `workflow.auto_advance`). Se nao ha fixture PDF no repo (baseline simbolico no Plano 01), o executor DEVE pausar e exigir do usuario um PDF curto — sem PDF, a verificacao E2E de regressao nao roda e o checkpoint permanece bloqueado. A prova byte-identica de args/cwd/env ja foi dada automaticamente na Task 2 (assertion CLI-free); este checkpoint confirma o comportamento de geracao real ponta-a-ponta.
  </action>
  <what-built>
    O andaime completo: runner extraido (+ helper puro buildSpawnArgs) + CliProvider delegando, prompt-loader + 5 .md canonicos, 5 SKILL.md espelho, Questao estendido (server+web), e ImageProvider/SvgClaudeImageProvider/createImageProvider — nenhum estagio plugado no pipeline. A nao-regressao ja tem cobertura AUTOMATIZADA CLI-free (Task 2: assertion de args). loadPrompt validado em src e dist (Task 3). Este checkpoint confirma o comportamento E2E com um PDF real.
  </what-built>
  <how-to-verify>
    1. Regressao do CliProvider (SPEC-01): com um PDF curto FORNECIDO, rodar `cd ankinator-app/server && npx tsx src/scripts/smoke-cli.ts <caminho.pdf>`. Comparar com `.planning/phases/01-andaime-dos-especialistas/baseline-cli.txt` (Plano 01). A geracao do LLM nao e deterministica — confirmar EQUIVALENCIA de comportamento (gera uma quantidade plausivel de questoes, no formato `[tipo] Q:/A:`, sem erro novo do CLI), nao igualdade textual.
    2. Fluxo E2E (PIPE-03): subir server (`cd ankinator-app/server && npm run dev` ou `npm start` apos build) e web (`cd ankinator-app/web && npm run dev`); fazer upload do PDF, gerar questoes e exportar CSV. Confirmar que o resultado e equivalente ao comportamento atual (cards gerados e exportaveis como antes — campos novos de Questao ausentes/ignorados).
    3. Skills (SPEC-03): opcionalmente reiniciar a sessao do Claude Code e confirmar que `/anki-orchestrator` (e os outros 4) aparecem no menu e apontam para o `.md` canonico.
  </how-to-verify>
  <verify>
    <human-check>Um PDF real foi usado E o usuario confirmou equivalencia de comportamento (sem regressao SPEC-01 + PIPE-03). Sem PDF, o checkpoint NAO avanca.</human-check>
  </verify>
  <acceptance_criteria>
    - Um PDF real foi usado (fixture do repo OU fornecido pelo usuario); sem PDF, o checkpoint NAO libera.
    - smoke-cli pos-refator gera questoes em comportamento equivalente ao baseline (sem regressao SPEC-01).
    - Fluxo upload->generate->export CSV funciona igual ao atual (PIPE-03 — sem regressao).
    - Usuario confirma "approved" ou descreve discrepancias.
  </acceptance_criteria>
  <done>Usuario confirmou, com PDF real, zero regressao (SPEC-01 + PIPE-03); checkpoint nao avancou sem PDF.</done>
  <resume-signal>Forneca o caminho de um PDF curto e digite "approved" apos confirmar zero regressao, ou descreva as diferencas observadas. O checkpoint nao avanca sem um PDF.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| processo Node -> `claude` CLI (spawn) | SvgClaudeImageProvider chama runClaudeCli (reusa spawn do CliProvider). A assertion de args (Task 2) verifica o plano de spawn sem cruzar a fronteira. |
| SVG retornado pelo LLM -> card exportado | NAO ocorre nesta fase (impl nao plugada); apenas existe a impl. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04a | Tampering | SVG malicioso (`<script>`/URLs externas) | accept (defer) | Sanitizacao real e da Fase 4 (IMG-02). A impl SvgClaudeImageProvider NAO e plugada no pipeline nesta fase; marcada com TODO(IMG-02). Sem superficie ativa. |
| T-01-04b | Tampering | runClaudeCli via image-provider | mitigate | Reusa o spawn herdado (array de args + stdin); a assertion de `buildSpawnArgs` (Task 2) prova deterministicamente que `userMessage` nao vaza para `args` (sem shell string). |
| T-01-SC | Tampering | sem novos pacotes | accept | Nenhum `npm install` nesta fase (RESEARCH: Package Legitimacy N/A). |

Nota: nenhuma nova superficie de ataque ativa. O SvgClaudeImageProvider existe mas nao e chamado pelo runtime; a sanitizacao de SVG (IMG-02) e explicitamente diferida para a Fase 4 e marcada com TODO no codigo.
</threat_model>

<verification>
- `cd ankinator-app/server && npm run build` sai 0 com `image-provider.ts`.
- `image-provider.ts` exporta ImageProvider/SvgClaudeImageProvider/createImageProvider, usa loadPrompt('mnemonic-image') + runClaudeCli, e nao pluga o pipeline.
- Assertion de args (smoke-runner passo 2) sai 0 SEM spawnar o CLI — guard deterministico de SPEC-01 (Pitfall 2).
- loadPrompt resolve os 5 .md em tsx (src) E node dist (prod) — Pitfall 1 fechado.
- Checkpoint humano HARD-BLOCK confirma zero regressao com PDF real (SPEC-01 + PIPE-03).
</verification>

<success_criteria>
- ImageProvider + SvgClaudeImageProvider + createImageProvider() criados (svg-claude default); sem raster; nao plugado no pipeline.
- Nao-regressao SPEC-01 com cobertura AUTOMATIZADA CLI-free (assertion de args/cwd/env byte-identica ao pre-refator).
- loadPrompt validado cross-mode (Pitfall 1 fechado); build limpo verde.
- Geracao pos-refator equivalente ao baseline e fluxo E2E intacto, confirmados pelo usuario com PDF real (checkpoint hard-block).
</success_criteria>

<output>
Create `.planning/phases/01-andaime-dos-especialistas/01-04-SUMMARY.md` when done
</output>
</content>
