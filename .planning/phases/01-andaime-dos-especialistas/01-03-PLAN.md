---
phase: 01-andaime-dos-especialistas
plan: 03
type: execute
wave: 1
depends_on: [01-01]
files_modified:
  - ankinator-app/server/src/core/types.ts
  - ankinator-app/web/src/types.ts
  - .claude/skills/anki-orchestrator/SKILL.md
  - .claude/skills/deck-classifier/SKILL.md
  - .claude/skills/card-builder/SKILL.md
  - .claude/skills/mnemonic/SKILL.md
  - .claude/skills/mnemonic-image/SKILL.md
autonomous: true
requirements: [SPEC-03, SPEC-05]
must_haves:
  truths:
    - "O tipo Questao ganha 4 campos opcionais (deck, tags, mnemonico, mnemonicoSvg) com a MESMA assinatura em server e web; o fluxo atual continua compilando e funcionando (campos ignorados quando ausentes)."
    - "Cada um dos 5 especialistas tem uma SKILL.md em .claude/skills/<nome>/ que REFERENCIA (nao copia) o .md canonico correspondente — sem copia divergente."
  artifacts:
    - path: "ankinator-app/server/src/core/types.ts"
      provides: "Questao (server) com deck?/tags?/mnemonico?/mnemonicoSvg?"
      contains: "mnemonicoSvg"
    - path: "ankinator-app/web/src/types.ts"
      provides: "Questao (web) espelhado, assinatura identica"
      contains: "mnemonicoSvg"
    - path: ".claude/skills/anki-orchestrator/SKILL.md"
      provides: "Skill espelho referenciando prompts/anki-orchestrator.md"
    - path: ".claude/skills/deck-classifier/SKILL.md"
      provides: "Skill espelho referenciando prompts/deck-classifier.md"
    - path: ".claude/skills/card-builder/SKILL.md"
      provides: "Skill espelho referenciando prompts/card-builder.md"
    - path: ".claude/skills/mnemonic/SKILL.md"
      provides: "Skill espelho referenciando prompts/mnemonic.md"
    - path: ".claude/skills/mnemonic-image/SKILL.md"
      provides: "Skill espelho referenciando prompts/mnemonic-image.md"
  key_links:
    - from: ".claude/skills/anki-orchestrator/SKILL.md"
      to: "ankinator-app/server/src/core/specialists/prompts/anki-orchestrator.md"
      via: "referencia textual por path (progressive disclosure)"
      pattern: "specialists/prompts/anki-orchestrator.md"
    - from: "ankinator-app/web/src/types.ts"
      to: "ankinator-app/server/src/core/types.ts"
      via: "mesma assinatura de campos (espelho manual)"
      pattern: "mnemonicoSvg\\?: string"
---

<objective>
Trilho paralelo do andaime (SPEC-05 + SPEC-03), totalmente independente do runner — sem sobreposicao de arquivos com o Plano 02, roda na mesma wave. (a) Estender o tipo `Questao` com 4 campos opcionais em AMBOS os arquivos duplicados (server + web), com assinatura identica, sem quebrar o fluxo atual (D-11/D-12/D-13). (b) Criar as 5 SKILL.md espelho em `.claude/skills/anki-<nome>/` que REFERENCIAM (nao copiam) o `.md` canonico — padrao de fonte unica via progressive disclosure (D-06).

Purpose: O tipo precisa existir nos dois lados (cruza a fronteira HTTP) para as fases 3-4 popularem deck/tags/mnemonico/svg. As Skills tornam cada especialista usavel manualmente no Claude Code apontando para a unica fonte do texto.
Output: Questao estendido (server+web) + 5 SKILL.md de referencia.

Nota de dependencia: este plano CRIA SKILL.md que referenciam por path os `.md` canonicos do Plano 02. A referencia e textual (path) — nao ha import em build; logo nao ha dependencia de compilacao entre 02 e 03. Por isso ambos rodam na Wave 1. As Skills podem ser criadas antes mesmo do .md existir (a referencia e resolvida sob demanda pelo Claude Code interativo).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-andaime-dos-especialistas/01-CONTEXT.md
@.planning/phases/01-andaime-dos-especialistas/01-PATTERNS.md

<interfaces>
<!-- Extensao aditiva EXATA, verificada no codebase (server types.ts:87-96; web types.ts:28-36). -->
<!-- Os 4 campos novos, IDENTICOS nos dois arquivos, inseridos apos `metadata?`: -->
  deck?: string;          // hierarquia Anki "Materia::Assunto::Subtopico"
  tags?: string[];        // banca, ano, nivel, tema
  mnemonico?: string;     // texto do mnemonico
  mnemonicoSvg?: string;  // SVG autocontido do mnemonico

<!-- Estado atual Questao (server types.ts:87-96): id, tipo, pergunta, resposta, pageStart?, pageEnd?, metadata? -->
<!-- Estado atual Questao (web types.ts:28-36): id, tipo, pergunta, resposta, pageStart?, pageEnd?, metadata? -->
<!-- mapRawQuestoes() (generation.ts:43-55) NAO precisa mudar — campos opcionais nao quebram o map (D-13). -->

<!-- Frontmatter de SKILL.md (constraints da spec): name <=64 chars, so [a-z0-9-], SEM "claude"/"anthropic"; description <=1024 chars. -->
<!-- Os 5 nomes (D-07) sao validos: anki-orchestrator, deck-classifier, card-builder, mnemonic, mnemonic-image. -->
<!-- Pasta da skill = .claude/skills/<nome>/SKILL.md (a pasta vira o comando /<nome>). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Estender Questao com 4 campos opcionais em server E web (mesma assinatura, mesmo commit)</name>
  <read_first>
    - ankinator-app/server/src/core/types.ts (interface Questao, linhas 87-96)
    - ankinator-app/web/src/types.ts (interface Questao, linhas 28-36)
    - .planning/phases/01-andaime-dos-especialistas/01-PATTERNS.md secao "types.ts (MODIFY)" (server e web)
    - .planning/phases/01-andaime-dos-especialistas/01-RESEARCH.md "Pitfall 4" (Questao divergente server/web)
  </read_first>
  <action>
    Adicionar os 4 campos OPCIONAIS a `interface Questao` em `ankinator-app/server/src/core/types.ts`, inseridos imediatamente APOS `metadata?: QuestaoMetadata;` (linha 95): `deck?: string;` (hierarquia Anki "Materia::Assunto::Subtopico"), `tags?: string[];` (banca/ano/nivel/tema), `mnemonico?: string;` (texto do mnemonico), `mnemonicoSvg?: string;` (SVG autocontido). Comentar a secao como "SPEC-05 (opcionais; ignorados quando ausentes; embed real nas fases 3-4)".
    Aplicar a MESMA mudanca, com assinatura IDENTICA (camelCase `mnemonicoSvg`), em `ankinator-app/web/src/types.ts`, apos `metadata?: QuestaoMetadata;` (linha 35) — Pitfall 4: tipo duplicado manual, manter sincronizado no MESMO plano/commit.
    Todos os campos sao `?:` (opcionais) — D-11. NAO tocar em `mapRawQuestoes()` (generation.ts:43-55), nos exporters (csv/ankiconnect), nem em qualquer outro consumidor: campos opcionais nao exigem mudanca e o fluxo atual deve continuar intacto (D-13). NAO unificar o tipo em workspace compartilhado (fora de escopo).
  </action>
  <verify>
    <automated>cd ankinator-app/server && npm run build 2>&1 | tail -2; echo "server exit: ${PIPESTATUS[0]:-$?}"; cd ../web && npm run build 2>&1 | tail -2; echo "web exit: ${PIPESTATUS[0]:-$?}"; grep -c 'mnemonicoSvg?: string' ../server/src/core/types.ts src/types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ankinator-app/server/src/core/types.ts` Questao contem `deck?`, `tags?: string[]`, `mnemonico?`, `mnemonicoSvg?` apos `metadata?`.
    - `ankinator-app/web/src/types.ts` Questao contem os MESMOS 4 campos com assinatura identica (`grep 'mnemonicoSvg?: string'` casa nos dois arquivos).
    - Todos os 4 campos sao opcionais (`?:`).
    - `cd ankinator-app/server && npm run build` sai 0 E `cd ankinator-app/web && npm run build` sai 0 (fluxo atual intacto).
    - `mapRawQuestoes()` e os exporters NAO foram modificados.
  </acceptance_criteria>
  <done>Questao estendido identicamente em server e web; ambos os builds verdes; fluxo atual intacto.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar as 5 SKILL.md espelho referenciando (nao copiando) os .md canonicos</name>
  <read_first>
    - .planning/phases/01-andaime-dos-especialistas/01-PATTERNS.md secao ".claude/skills/anki-*/SKILL.md" (frontmatter + corpo de referencia)
    - .planning/phases/01-andaime-dos-especialistas/01-RESEARCH.md "Pattern 2" (fonte unica via progressive disclosure), "Pitfall 3" (name reservado/formato), e "Code Examples -> SKILL.md"
    - ~/.claude/skills/gsd-complete-milestone/SKILL.md (analog de formato de frontmatter, se acessivel)
  </read_first>
  <action>
    Criar 5 arquivos `SKILL.md`, um por especialista, em `.claude/skills/<nome>/SKILL.md` (a pasta `.claude/skills/` ainda nao existe — criar). Os nomes de pasta sao exatamente os 5 canonicos (D-07): `anki-orchestrator`, `deck-classifier`, `card-builder`, `mnemonic`, `mnemonic-image`.
    Cada SKILL.md tem: (1) frontmatter YAML com `name: <nome>` (igual a pasta; <=64 chars, so [a-z0-9-], SEM "claude"/"anthropic" — todos os 5 ja passam) e `description:` curta em PT focada no caso de uso (<=1024 chars); (2) corpo curto que REFERENCIA por path o `.md` canonico — texto do tipo: "O conteudo canonico (system prompt + conhecimento) deste especialista vive em `ankinator-app/server/src/core/specialists/prompts/<nome>.md` — esta e a UNICA fonte. Leia esse arquivo e siga-o como sua especificacao. NAO duplique o texto aqui."; (3) uma secao "## Quando usar" com 2-3 bullets do papel do especialista.
    ANTI-PATTERN PROIBIDO (D-06): NAO copiar o corpo do `.md` canonico para dentro da SKILL.md (cria duas fontes divergentes). Usar referencia textual por path (progressive disclosure — tecnica recomendada da RESEARCH Pattern 2). Cada SKILL.md deve conter a string do path `specialists/prompts/<nome>.md` correspondente.
    Documentar no SUMMARY que o usuario pode precisar reiniciar a sessao do Claude Code para as novas Skills aparecerem no menu `/` (live change detection de diretorio top-level — RESEARCH Open Question 3).
  </action>
  <verify>
    <automated>ok=1; for n in anki-orchestrator deck-classifier card-builder mnemonic mnemonic-image; do f=".claude/skills/$n/SKILL.md"; test -f "$f" || { echo "MISSING $f"; ok=0; continue; }; grep -q "name: $n" "$f" || { echo "BAD name $f"; ok=0; }; grep -q "specialists/prompts/$n.md" "$f" || { echo "NO ref $f"; ok=0; }; grep -qiE 'anthropic|claude' <(grep '^name:' "$f") && { echo "RESERVED name $f"; ok=0; }; done; echo "skills ok=$ok"; test $ok -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - Existem 5 arquivos `.claude/skills/<nome>/SKILL.md`, um por nome canonico (D-07).
    - Cada um tem frontmatter com `name: <nome>` (sem "claude"/"anthropic") e `description` em PT.
    - Cada corpo REFERENCIA por path o `.md` canonico (`grep 'specialists/prompts/<nome>.md'` casa) e NAO copia o corpo do prompt.
    - Cada um tem uma secao "## Quando usar".
  </acceptance_criteria>
  <done>5 SKILL.md criados em .claude/skills/ referenciando (sem copiar) os .md canonicos; nomes validos pela spec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tipo Questao | cruza a fronteira HTTP server<->web (duplicado manualmente). |
| SKILL.md -> arquivo do repo | referencia apenas arquivos do proprio repo (fonte confiavel), sem fetch externo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03a | Tampering | SKILL.md (referencia por path) | mitigate | SKILL.md referencia apenas `server/src/core/specialists/prompts/*.md` do proprio repo; sem URL externa nem `@import` de fonte nao confiavel. |
| T-01-03b | Information Disclosure | SKILL.md (description/corpo) | accept | Conteudo descreve o papel do especialista; sem segredos. App local single-user. |

Nota: campos novos de Questao sao opcionais e nao introduzem novo input externo nesta fase (sem nova superficie). Embed real de svg/deck/tags nos exporters e das fases 3-4.
</threat_model>

<verification>
- `cd ankinator-app/server && npm run build` E `cd ankinator-app/web && npm run build` saem 0 com os 4 campos novos.
- `grep 'mnemonicoSvg?: string'` casa nos dois `types.ts`.
- 5 `.claude/skills/<nome>/SKILL.md` existem, com `name` valido e referencia por path ao `.md` canonico (sem copia).
</verification>

<success_criteria>
- Questao estendido identicamente em server e web (4 campos opcionais); fluxo atual intacto; ambos os builds verdes.
- 5 SKILL.md espelho criadas referenciando (nao copiando) os .md canonicos — fonte unica preservada.
</success_criteria>

<output>
Create `.planning/phases/01-andaime-dos-especialistas/01-03-SUMMARY.md` when done
</output>
