---
phase: quick-260606-k3b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/markitdown_loader.py
  - tools/requirements-markitdown.txt
  - tools/turbovec_index.py
  - tools/requirements-turbovec.txt
  - ankinator-app/server/src/core/markitdown-loader.ts
  - ankinator-app/server/src/core/markitdown-loader.test.ts
  - ankinator-app/server/src/core/turbovec.ts
  - ankinator-app/server/src/core/turbovec.test.ts
  - ankinator-app/server/src/core/document-loader.ts
  - ankinator-app/server/src/core/document-loader.test.ts
  - ankinator-app/server/src/config.ts
  - ankinator-app/.env.example
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [LOADER-MARKITDOWN, SCAFFOLD-TURBOVEC, DOCS-ENV]
user_setup:
  - service: markitdown (sidecar Python)
    why: "Loader de PDF alternativo (opt-in) sem Java — pip install é validação AO VIVO (checkpoint humano), NÃO roda no executor automático."
    env_vars:
      - name: ANKINATOR_MARKITDOWN_PYTHON
        source: "Interpretador do venv dedicado: python3 -m venv ~/.venvs/markitdown && ~/.venvs/markitdown/bin/pip install -r tools/requirements-markitdown.txt"
  - service: turbovec (sidecar Python — scaffold)
    why: "Engine de busca vetorial quantizada — fundação para dedup semântico FUTURO. Não plugado no pipeline. pip install é validação AO VIVO (checkpoint humano)."
    env_vars:
      - name: ANKINATOR_TURBOVEC_PYTHON
        source: "Interpretador do venv dedicado: python3 -m venv ~/.venvs/turbovec && ~/.venvs/turbovec/bin/pip install -r tools/requirements-turbovec.txt"

must_haves:
  truths:
    - "Com ANKINATOR_PDF_LOADER=markitdown + venv real, o app extrai um PDF via markitdown (1 Document, page=1) e gera cards — validável AO VIVO."
    - "O loader DEFAULT (node/langchain) permanece byte-idêntico; markitdown só roda sob env explícito."
    - "O sidecar turbovec roda o subcomando smoke (índice + vetores fake + search) e imprime JSON {ok:true,...} quando o venv existe."
    - "isMarkitdownAvailable() e isTurbovecAvailable() são fail-closed: retornam false sem env/sem pacote, sem derrubar o app."
    - "O dedup Jaccard atual (deck-organizer.ts/existing-deck.ts) permanece INTOCADO; turbovec NÃO está plugado no pipeline."
  artifacts:
    - path: "tools/markitdown_loader.py"
      provides: "Sidecar PDF->Markdown (1 entrada JSON page=1) espelhando o contrato do odl_langchain_loader.py"
      contains: "from markitdown import MarkItDown"
    - path: "ankinator-app/server/src/core/markitdown-loader.ts"
      provides: "spawn + isMarkitdownAvailable() + runMarkitdownLoader() reusando normalize()"
      exports: ["isMarkitdownAvailable", "runMarkitdownLoader"]
    - path: "tools/turbovec_index.py"
      provides: "Sidecar com subcomando smoke + interface add/search JSON stdin/stdout"
      contains: "from turbovec import TurboQuantIndex"
    - path: "ankinator-app/server/src/core/turbovec.ts"
      provides: "isTurbovecAvailable() fail-closed + runTurbovecSmoke() wrapper, marcado TODO(dedup-semântico)"
      exports: ["isTurbovecAvailable", "runTurbovecSmoke"]
    - path: "tools/requirements-markitdown.txt"
      provides: "Pin markitdown[pdf]==0.1.6"
      contains: "markitdown[pdf]==0.1.6"
    - path: "tools/requirements-turbovec.txt"
      provides: "Pin turbovec==0.7.0"
      contains: "turbovec==0.7.0"
  key_links:
    - from: "ankinator-app/server/src/core/document-loader.ts"
      to: "runMarkitdownLoader"
      via: "branch loader === 'markitdown' em loadDocument; chooseLoader retorna markitdown sob env explícito"
      pattern: "runMarkitdownLoader"
    - from: "ankinator-app/server/src/core/markitdown-loader.ts"
      to: "normalize"
      via: "reuso direto de langchain-normalize (1 doc, page=1) SEM alterá-la"
      pattern: "normalize\\("
---

<objective>
Adicionar **markitdown** (loader de PDF opt-in, sem Java) e **turbovec** (scaffold de busca vetorial provado por smoke) ao Ankinator, espelhando o padrão sidecar Python da Phase 2.

Purpose: dar ao Ankinator uma 3a opção de loader de PDF que não depende de Java (markitdown) e estabelecer uma fundação EXERCITADA para dedup semântico futuro (turbovec) — sem tocar o default loader nem o dedup Jaccard atual.

Output:
- `tools/markitdown_loader.py` + `markitdown-loader.ts` + branch em `document-loader.ts`/`config.ts` (loader opt-in `ANKINATOR_PDF_LOADER=markitdown`).
- `tools/turbovec_index.py` + `turbovec.ts` (scaffold fail-closed, NÃO plugado no pipeline).
- `requirements-markitdown.txt`/`requirements-turbovec.txt`, `.env.example`, nota de setup, item de fase futura no ROADMAP/STATE.

**Validação AO VIVO PENDENTE (NÃO é critério de done):** o `pip install` em venvs reais + rodar os sidecars com PDF/turbovec real é um **checkpoint humano** (gate auto-mode; lição UAT Phases 4: teste mockado não conta como "pronto via venv real"). O *done* destas tarefas é **código + testes/guards verdes + scaffolds rodáveis quando o venv existir** — NÃO declarar "validado ao vivo".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260606-k3b-adicionar-markitdown-e-turbovec-a-esse-p/260606-k3b-CONTEXT.md
@.planning/quick/260606-k3b-adicionar-markitdown-e-turbovec-a-esse-p/260606-k3b-RESEARCH.md

# Padrão Phase 2 a ESPELHAR (contrato confirmado pelo planner):
@tools/odl_langchain_loader.py
@ankinator-app/server/src/core/langchain-loader.ts
@ankinator-app/server/src/core/langchain-normalize.ts
@ankinator-app/server/src/core/document-loader.ts
@ankinator-app/server/src/core/document-loader.test.ts
@ankinator-app/server/src/config.ts
@ankinator-app/.env.example

<interfaces>
<!-- Contratos que o executor DEVE usar diretamente — sem explorar o codebase. -->

De ankinator-app/server/src/core/langchain-normalize.ts (REUSAR SEM ALTERAR):
```typescript
export interface RawDoc {
  page_content: string;
  metadata: { source?: string; format?: string; page?: number };
}
export function normalize(docs: RawDoc[], pdfPath: string): LoadedDocument;
```
Para markitdown: emitir UMA entrada RawDoc com page=1. normalize() é compatível p/ 1 doc
(buildSectionsFromMarkdown reparseia headings ATX; numPages vira 1 — limitação documentada).
NÃO modificar normalize().

De ankinator-app/server/src/core/document-loader.ts (ESTENDER, default INTOCADO):
```typescript
export interface LoadOptions { ocr?: boolean; pages?: string; password?: string; }
export interface LoaderChoice { loader: 'langchain' | 'node'; reason: string; }
export function chooseLoader(envValue: string | undefined, langchainAvailable: boolean): LoaderChoice;
export async function loadDocument(pdfPath: string, opts?: LoadOptions): Promise<LoadedDocument>;
```

De ankinator-app/server/src/core/langchain-loader.ts (PADRÃO a espelhar — spawn/run/guardas):
```typescript
// run(cmd, args) -> { code, stdout, stderr, spawnError? }  (CR-01: Buffer[] + toString('utf8') UMA vez)
// pythonBin() -> env var || null
// SIDECAR_SCRIPT = path.resolve(here, '../../../../tools/<script>.py')  (import.meta.url, +4 níveis)
// isLangchainAvailable() = pythonBin() != null && run(py,['-c','import <pkg>']).code === 0
// runLangchainLoader: spawnError->throw; code!=0->throw (head do stderr); JSON.parse + guardas CR-02 -> normalize()
```

De ankinator-app/server/src/config.ts (ESTENDER):
```typescript
type PdfLoaderKind = 'node' | 'langchain';   // estender p/ 'markitdown'
// pdfLoaderEnv = (process.env.ANKINATOR_PDF_LOADER?.trim().toLowerCase() as PdfLoaderKind) || 'node'
// langchainPython: process.env.ANKINATOR_LANGCHAIN_PYTHON || process.env.ODL_PYTHON || ''
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: markitdown loader opt-in (sidecar + ts + config + requirements + teste)</name>
  <files>tools/markitdown_loader.py, tools/requirements-markitdown.txt, ankinator-app/server/src/core/markitdown-loader.ts, ankinator-app/server/src/core/markitdown-loader.test.ts, ankinator-app/server/src/core/document-loader.ts, ankinator-app/server/src/core/document-loader.test.ts, ankinator-app/server/src/config.ts</files>
  <action>
Adicionar markitdown como 3a opção de loader de PDF, opt-in via `ANKINATOR_PDF_LOADER=markitdown`, espelhando EXATAMENTE o trio da Phase 2. Default (node/langchain) INTOCADO (decisão A1 / R1; guard-default-loader.ts deve continuar verde).

1. `tools/markitdown_loader.py` — espelhar o contrato do `odl_langchain_loader.py` mas emitindo 1 entrada (RESEARCH A.2/C.3). Usar a API Python (não a CLI): `from markitdown import MarkItDown`; `result = MarkItDown().convert(args.pdf)`; pegar `result.markdown` (atributo canônico — `.text_content` é alias soft-deprecated, NÃO usar). argparse com `pdf` posicional + `--pages`/`--password` aceitos para paridade de assinatura mas documentados como no-op neste loader (a API `convert(path)` não os expõe). Saída stdout: `json.dump([{"page_content": result.markdown, "metadata": {"source": args.pdf, "format": "markdown", "page": 1}}], sys.stdout, ensure_ascii=False)`. Try/except no boundary emitindo 1 linha limpa no stderr (`print(f"markitdown_loader: {type(e).__name__}: {e}", file=sys.stderr)`) + `return 1` (espelha WR-03). Docstring documentando: blob ÚNICO (sem split por página — limitação honesta, numPages=1), sem OCR nativo (PDF escaneado -> markdown vazio; manter caminho OCR existente), sem Java.

2. `ankinator-app/server/src/core/markitdown-loader.ts` — espelhar `langchain-loader.ts`. `pythonBin()` lê `process.env.ANKINATOR_MARKITDOWN_PYTHON?.trim() || null` (SEM fallback p/ ODL_PYTHON — venv dedicado, RESEARCH C.1). Duplicar a função `run()` idêntica (CR-01: Buffer[] + `Buffer.concat(...).toString('utf8')` UMA vez; `spawnError` no evento 'error'). `SIDECAR_SCRIPT = path.resolve(here, '../../../../tools/markitdown_loader.py')` via `import.meta.url`. `isMarkitdownAvailable()` = `pythonBin() != null && (await run(py,['-c','import markitdown'])).code === 0` (SEM checar Java). `runMarkitdownLoader(pdfPath, opts)`: monta args (`[SIDECAR, pdfPath]` + `--pages`/`--password` se presentes, com a mesma redação WR-04 da senha), spawnError->throw, code!=0->throw (head das 5 primeiras linhas do stderr), `JSON.parse` + guardas CR-02 (é array? cada item `page_content: string`?), e `return normalize(parsed as RawDoc[], pdfPath)` reusando `langchain-normalize.js` SEM alterá-la. numPages=1 é limitação documentada — NÃO tentar split por página.

3. `config.ts` — estender `type PdfLoaderKind = 'node' | 'langchain' | 'markitdown'`; o cast de `pdfLoaderEnv` já cobre o novo valor; ajustar a linha `pdfLoader:` para aceitar 'markitdown' (ternário/switch mantendo 'node' como fallback default — default byte-idêntico). Adicionar `markitdownPython: process.env.ANKINATOR_MARKITDOWN_PYTHON?.trim() || ''` com docstring de setup (venv dedicado, sem Java).

4. `document-loader.ts` — estender `LoaderChoice.loader` p/ `'langchain' | 'node' | 'markitdown'`; estender `chooseLoader(envValue, langchainAvailable, markitdownAvailable)` (novo 3o parâmetro injetável p/ testabilidade): `env === 'markitdown'` -> se `markitdownAvailable` retorna markitdown senão throw com instrução de setup clara (espelha o branch langchain). Default INTOCADO: unset -> mantém a lógica auto langchain/node atual; markitdown NUNCA é auto-selecionado (só sob env explícito — decisão A1). Em `loadDocument`, calcular `markitdownAvail = await isMarkitdownAvailable()` e adicionar branch `if (loader === 'markitdown') { console.info('[ankinator] PDF loader: markitdown ...'); return runMarkitdownLoader(pdfPath, opts); }` ANTES do branch langchain. NÃO tocar o branch OCR nem o node.

5. `tools/requirements-markitdown.txt` — header explicando venv dedicado (Python >=3.10, SEM Java, opt-in) + `markitdown[pdf]==0.1.6` (RESEARCH D — só extra PDF; `[all]` é follow-up futuro). NÃO tocar `tools/requirements.txt` (langchain).

6. Testes CLI-free:
   - `markitdown-loader.test.ts`: NÃO spawna Python. Testar a lógica de parse/contrato — ex.: que `normalize([{page_content:'# Titulo\n\ntexto', metadata:{page:1}}], '/x/a.pdf')` (reuso) produz LoadedDocument com `numPages===1` e seções a partir de headings ATX (prova a limitação documentada + a compatibilidade do reuso). Se preferir isolar o parse das guardas, extrair um helper puro `parseSidecarOutput(stdout)` em markitdown-loader.ts e testá-lo (array válido vs não-array vs item sem page_content -> erro). Escolha do executor; o teste deve provar o CONTRATO sem rodar o sidecar.
   - `document-loader.test.ts`: adicionar casos ao describe existente cobrindo o 3o parâmetro de `chooseLoader`: `env='markitdown' + disponível -> markitdown`; `env='markitdown' + indisponível -> throw com instrução`; `env=undefined -> NUNCA markitdown` (default intocado); `env='node'/'langchain'` inalterados (passar o novo parâmetro como false nesses casos).

Restrições: byte-seguro; default loader INTOCADO (guard-default-loader verde); server-only (sem UI/upload). NÃO modificar `langchain-normalize.ts`, `langchain-loader.ts`, `odl_langchain_loader.py`, `requirements.txt`.
  </action>
  <verify>
  <automated>cd ankinator-app/server && npx tsc -p tsconfig.json --noEmit && npx vitest run src/core/markitdown-loader.test.ts src/core/document-loader.test.ts && npx tsx src/scripts/guard-default-loader.ts --assert && grep -q "markitdown\[pdf\]==0.1.6" ../../tools/requirements-markitdown.txt && grep -q "from markitdown import MarkItDown" ../../tools/markitdown_loader.py && python3 -m py_compile ../../tools/markitdown_loader.py</automated>
  </verify>
  <done>
  tsc verde; testes markitdown + document-loader passam; guard-default-loader `--assert` verde (default byte-idêntico, sem Java/langchain importados); requirements pinado em 0.1.6; sidecar compila (`py_compile`) e usa a API canônica. Validação AO VIVO (pip install em venv real + extrair PDF real via markitdown) fica PENDENTE — NÃO é critério deste done.
  </done>
</task>

<task type="auto">
  <name>Task 2: turbovec scaffold PROVADO (sidecar smoke + add/search + ts fail-closed + requirements + teste)</name>
  <files>tools/turbovec_index.py, tools/requirements-turbovec.txt, ankinator-app/server/src/core/turbovec.ts, ankinator-app/server/src/core/turbovec.test.ts</files>
  <action>
Adicionar turbovec como fundação exercitada (decisão B1), NÃO como dedup ligado. Anti-andaime-morto: o scaffold só é aceitável porque é PROVADO por smoke rodável + teste. NÃO plugar no pipeline. O dedup Jaccard atual (`deck-organizer.ts`/`existing-deck.ts`) permanece INTOCADO (R1/R3) — esta task NÃO os importa nem os referencia.

1. `tools/turbovec_index.py` — espelhar o estilo do sidecar (argparse com subcomandos, try/except boundary, stdout JSON, stderr 1 linha). Subcomandos:
   - `smoke` (prova-de-vida no nível do componente, RESEARCH C.4/B.3): `import numpy as np`; `from turbovec import TurboQuantIndex`; gerar vetores fake determinísticos float32 via `np.random.default_rng(42).standard_normal((n, dim)).astype(np.float32)` (NÃO normalizar — turbovec normaliza internamente, RESEARCH B.4); `idx = TurboQuantIndex(dim=dim, bit_width=bit_width)`; `idx.add(vecs)`; `scores, indices = idx.search(vecs[0], k=k)`; `print(json.dumps({"ok": True, "k": k, "top_indices": indices.tolist()[:k]}))`. Args opcionais `--dim`/`--n`/`--bit-width`/`--k` com defaults (dim=64, n=128, bit_width=4, k=5).
   - `add` e `search` (interface mínima futura, JSON stdin/stdout): `add` lê `{"vectors": [[...]]}` do stdin, constrói índice, retorna `{"ok": true, "count": N}`; `search` lê `{"vectors": [[...]], "query": [...], "k": K}`, retorna `{"ok": true, "scores": [...], "indices": [...]}`. Vetores -> `np.asarray(..., dtype=np.float32)` antes de `add`/`search`. (Interface para uso FUTURO; não chamada por nada agora.)
   - Documentar no docstring: só consome vetores (não gera embeddings — fonte deferida, Eixo 3); pin estrito por ser v0.x.

2. `ankinator-app/server/src/core/turbovec.ts` — espelhar o padrão de spawn (`run()` duplicada idêntica CR-01; `pythonBin()` lê `process.env.ANKINATOR_TURBOVEC_PYTHON?.trim() || null`; `SIDECAR_SCRIPT = path.resolve(here, '../../../../tools/turbovec_index.py')`). `isTurbovecAvailable()` fail-closed: `pythonBin() != null && (await run(py,['-c','import turbovec'])).code === 0` (cobre ausência de wheel/pacote -> false, nunca throw). `runTurbovecSmoke()`: spawna `[SIDECAR, 'smoke']`, spawnError/code!=0 -> throw com head do stderr, `JSON.parse` + guarda (objeto com `ok === true`?) -> retorna o parsed tipado. Comentário no topo do arquivo: `// TODO(dedup-semântico: fase futura) — scaffold provado por smoke; NÃO plugado no pipeline (dedup Jaccard atual permanece intocado).` Exportar `isTurbovecAvailable`, `runTurbovecSmoke` (e um helper puro de parse, se extraído para o teste).

3. `tools/requirements-turbovec.txt` — header (venv dedicado, Python >=3.9, wheel binária linux x86_64 sem Rust, v0.x novo -> pin estrito) + `turbovec==0.7.0` (RESEARCH D; numpy>=1.20 vem como dep transitiva — NÃO pinar numpy manualmente).

4. `turbovec.test.ts` (CLI-free): NÃO spawna Python. Testar o parse determinístico do JSON do smoke — mockar stdout `'{"ok":true,"k":5,"top_indices":[0,12,3,7,1]}'` via o helper de parse extraído (ou testar a guarda diretamente): `ok===true` aceito; objeto sem `ok` / não-objeto -> erro claro. Prova o CONTRATO sem rodar Python (detecta drift de API turbovec — RESEARCH E2).

Restrições: byte-seguro; NÃO importar/referenciar `deck-organizer.ts`/`existing-deck.ts` (dedup INTOCADO); fail-closed (sem env/pacote -> false, nunca derruba o app); contingência turbovec NÃO acionada (research confirmou instalável), mas `isTurbovecAvailable()` permanece fail-closed.
  </action>
  <verify>
  <automated>cd ankinator-app/server && npx tsc -p tsconfig.json --noEmit && npx vitest run src/core/turbovec.test.ts && grep -q "turbovec==0.7.0" ../../tools/requirements-turbovec.txt && grep -q "TODO(dedup-semântico" src/core/turbovec.ts && grep -q "from turbovec import TurboQuantIndex" ../../tools/turbovec_index.py && python3 -m py_compile ../../tools/turbovec_index.py && ! grep -Eq "deck-organizer|existing-deck" src/core/turbovec.ts</automated>
  </verify>
  <done>
  tsc verde; teste turbovec passa; requirements pinado em 0.7.0; sidecar compila (`py_compile`) e usa a API confirmada; wrapper marcado `TODO(dedup-semântico)` e fail-closed; turbovec.ts NÃO referencia deck-organizer/existing-deck (dedup INTOCADO). Validação AO VIVO (pip install do wheel turbovec + rodar `smoke` com numpy real) fica PENDENTE — NÃO é critério deste done.
  </done>
</task>

<task type="auto">
  <name>Task 3: docs + env (env.example + nota de setup + item de fase futura no ROADMAP/STATE)</name>
  <files>ankinator-app/.env.example, tools/requirements-markitdown.txt, tools/requirements-turbovec.txt, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
Documentar a ativação dos dois sidecars e registrar a fase futura de dedup semântico. SEM código novo (só docs/env).

1. `ankinator-app/.env.example` — adicionar 2 blocos novos (espelhando o estilo dos blocos existentes), SEM tocar os existentes:
   - Bloco markitdown: comentário explicando loader opt-in sem Java; `ANKINATOR_MARKITDOWN_PYTHON=` (venv dedicado: `python3 -m venv ~/.venvs/markitdown && ~/.venvs/markitdown/bin/pip install -r tools/requirements-markitdown.txt`); e a linha de ativação comentada `# ANKINATOR_PDF_LOADER=markitdown` (deixar comentada — default INTOCADO).
   - Bloco turbovec: comentário explicando que é scaffold para dedup semântico FUTURO (não plugado no pipeline); `ANKINATOR_TURBOVEC_PYTHON=` (venv dedicado: `python3 -m venv ~/.venvs/turbovec && ~/.venvs/turbovec/bin/pip install -r tools/requirements-turbovec.txt`).

2. Nota de setup/ativação curta — colocá-la nos headers dos `tools/requirements-markitdown.txt` e `tools/requirements-turbovec.txt` (já criados nas Tasks 1/2; aqui só garantir/complementar): venvs dedicados; markitdown sem Java (vs langchain que exige Java); turbovec pin estrito por ser v0.x; e a frase explícita de que a validação ao vivo (pip install em venv real) é um passo humano pendente.

3. `.planning/ROADMAP.md` — adicionar um item de FASE FUTURA na seção de Phase Details (após Phase 7, antes de `## Progress`), ex.: `### Phase 8 (futura): Dedup semântico por embeddings` com Goal curto ("dedup de cards por similaridade semântica usando turbovec + modelo de embeddings LOCAL gratuito — ex.: sentence-transformers multilíngue PT — via sidecar; API de embeddings paga REJEITADA por R2; operação destrutiva no Anki exige consentimento explícito do usuário — R3"). Marcar como `**Status**: Fundação (scaffold turbovec) pronta em quick-260606-k3b; dedup NÃO construído.` Atualizar a tabela `## Progress` com a linha da fase futura se couber no formato.

4. `.planning/STATE.md` — registrar 1 item curto em "Pendente"/Accumulated Context apontando a fase futura "dedup semântico por embeddings (turbovec + sentence-transformers local)" como deferida, e que o scaffold markitdown+turbovec foi adicionado nesta quick task (validação ao vivo pendente).

Restrições: só docs/env; não tocar nenhum arquivo de código; manter o tom e o formato dos arquivos existentes.
  </action>
  <verify>
  <automated>grep -q "ANKINATOR_MARKITDOWN_PYTHON" ankinator-app/.env.example && grep -q "ANKINATOR_TURBOVEC_PYTHON" ankinator-app/.env.example && grep -q "ANKINATOR_PDF_LOADER=markitdown" ankinator-app/.env.example && grep -qi "dedup semântico" .planning/ROADMAP.md && grep -qi "turbovec" .planning/STATE.md</automated>
  </verify>
  <done>
  `.env.example` tem os 2 envs novos + a linha (comentada) `ANKINATOR_PDF_LOADER=markitdown`; os requirements têm a nota de setup (venvs dedicados, markitdown sem Java, turbovec pin) e mencionam a validação ao vivo pendente; ROADMAP tem o item de fase futura "dedup semântico"; STATE registra o scaffold + a fase deferida. Nenhum arquivo de código tocado.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Node -> sidecar Python (spawn) | argv passado ao subprocesso; stdout/stderr atravessam de volta |
| sidecar Python -> markitdown/turbovec (pip) | dependências de terceiros instaladas em venv (PyPI) |
| PDF do usuário -> markitdown | entrada não-confiável processada pelo conversor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-k3b-01 | Tampering | spawn do sidecar (argv) | mitigate | argv-array (sem shell) — sem injeção; espelha langchain-loader (T-02-05). Senha redigida em Error (WR-04) |
| T-k3b-02 | Information Disclosure | stderr/Error logs (--password) | mitigate | redact da senha antes de interpolar em Error.message (espelha WR-04); markitdown trata `--password` como no-op mas a redação é mantida por paridade |
| T-k3b-03 | Denial of Service | stdout corrompido / não-JSON | mitigate | guardas CR-02 (array? page_content:string?) antes de normalize(); JSON.parse em try/catch com mensagem contextual |
| T-k3b-04 | Tampering (encoding) | UTF-8 PT-BR partido entre chunks | mitigate | run() bufferiza Buffer[] e decodifica UMA vez (CR-01); sidecar usa ensure_ascii=False |
| T-k3b-05 | Elevation of Privilege | default loader / dedup alterados sem querer | mitigate | default INTOCADO (guard-default-loader --assert verde); turbovec NÃO importa deck-organizer/existing-deck (assert no verify) |
| T-k3b-SC | Tampering | pip install markitdown[pdf]==0.1.6 / turbovec==0.7.0 | mitigate | slopcheck bloqueado pelo gate auto-mode -> degradado p/ verificação na fonte (PyPI JSON 200 + READMEs canônicos microsoft/RyanCodrai, sem sinal de slop — RESEARCH "Package Legitimacy"). Pins EXATOS. Checkpoint:human-verify obrigatório ANTES do pip install na validação AO VIVO (T-k3b-SC permanece aberto até lá; o executor automático NÃO instala pacotes) |
</threat_model>

<verification>
- `npx tsc -p tsconfig.json --noEmit` verde (server) — sem regressão de tipos.
- `npx vitest run` (suíte completa) verde — sem regressão (deck-organizer/existing-deck/langchain intactos).
- `npx tsx src/scripts/guard-default-loader.ts --assert` verde — default loader byte-idêntico (R1).
- `python3 -m py_compile tools/markitdown_loader.py tools/turbovec_index.py` — sidecars sintaticamente válidos.
- Pins EXATOS presentes (`markitdown[pdf]==0.1.6`, `turbovec==0.7.0`).
- `turbovec.ts` não referencia `deck-organizer`/`existing-deck` (dedup INTOCADO).
</verification>

<success_criteria>
- markitdown é um loader opt-in (`ANKINATOR_PDF_LOADER=markitdown`) que reusa `normalize()` (1 doc, page=1) sem alterá-la; default INTOCADO.
- turbovec é um scaffold provado por smoke (sidecar rodável + teste CLI-free do parse), fail-closed, NÃO plugado no pipeline, dedup Jaccard INTOCADO.
- `.env.example`/requirements/ROADMAP/STATE documentam ativação + fase futura de dedup semântico (embeddings local).
- Toda a suíte + tsc + guard verdes (zero regressão).
- **Validação AO VIVO (pip install em venvs reais + rodar sidecars com PDF/turbovec real) declarada PENDENTE — checkpoint humano, NÃO critério de done.**
</success_criteria>

<output>
Create `.planning/quick/260606-k3b-adicionar-markitdown-e-turbovec-a-esse-p/260606-k3b-SUMMARY.md` when done
</output>
