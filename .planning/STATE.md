---
gsd_state_version: 1.0
milestone: v1.0
milestone_name: milestone
status: executing
last_updated: "2026-06-05T19:45:00Z"
last_activity: 2026-06-06 (q-live3) -- 2º RUN AO VIVO (autônomo) na seção "LISTA DE QUESTÕES" (idx 9, 16 cards, orquestrado, deck descartável AnkinatorTesteCriada): orquestrador SEM fallback (~4min, $0.426), stats deck:16 tags:16 resposta:2 mnemonico:16 svg:16 de 16, híbrido sem timeout; via AnkiConnect subdecks RICOS (AnkinatorTesteCriada::Arquitetura ANSI-SPARC::Independência de Dados, ::Controle de Concorrência::Bloqueio e Deadlock, ::Objetos de Banco de Dados::Views, ...) + tags planas. ACHADO+FIX (bug real exposto pela validação): no orquestrado 4/16 notas saíam com tag de origem DUPLA e contraditória (criada+extraida) -- o classificador do orquestrador emite origem::criada por conta própria E o exporter adiciona origem::<q.tipo> -> o Set juntava as duas. Origem é fato do PIPELINE (q.tipo), não classificação. FIX cirúrgico no boundary tagsDaQuestao (ankiconnect.ts + csv.ts): descarta tags do classificador que achatam p/ criada/extraida; só q.tipo define origem. +1 teste de regressão. 250 testes (249->+1) + tsc + guards verdes. Anterior (q-live2) -- VALIDAÇÃO AUTÔNOMA AO VIVO (o usuário liberou: "vc nao precisa depender de mim", PDF em /home/t316360/plottwist/material; deletou o loop de 15min, criou um de 2h). Rodei EU MESMO o pipeline real via novo driver server/src/scripts/live-validate.ts (espelha /generate+/export, mesmos knobs via config), ANKINATOR_ORCHESTRATED=1, seções "Tipos de BD"+"ACID" -> 6 cards. DESTINO #4 QUALIDADE = PASS: orquestrador rodou SEM fallback (claude -p --agent ankinator-orchestrator + Task, ~81s, $0.213); stats por-campo deck:6 tags:6 mnemonico:6 svg:6 de 6 -> faz TRABALHO REAL (resolve a duvida do p-live; svg:0 NO orquestrador e esperado=hibrido, os 6 SVGs vieram do pool determinístico, 1 reprovado por 15 rotulos->retry->6/6 0 reprovadas); enrich ~2,3min (longe dos 10min que estouravam). Mnemonicos bons (ACID="A Chuva Isola a Duracao"+intruso PRIVACIDADE da LGPD; ganchos com discriminacao de distrator; deck hierarquico rico). p-fix2 #1/#2/#3 VALIDADOS via AnkiConnect (notesInfo/deckNames): subdecks ANINHADOS (AnkinatorTeste::Transacoes::Propriedades ACID etc.), tags PLANAS (ultimo segmento, sem ::, dedup: ['2023','fundatec','acid',...]), hibrido sem timeout. 6 cards no deck descartavel AnkinatorTeste. 249 testes + tsc verdes com o driver novo. Tudo (p/p-fix/p-fix2 + driver) NAO-COMMITADO. PENDENTE: decidir se orquestrado vira DEFAULT (funciona+qualidade mas custa mais que enrichAll; recomendo manter opt-in -- Destino #4 ja SATISFEITO como capacidade). Anterior (p-fix2) -- RUN #2 + 3 fixes (escolhas do usuário via AskUserQuestion). Run #2: SVGs OK mas o ORQUESTRADOR ESTOUROU o timeout de 10min (forçar ~50 imagens num call) → fallback determinístico gerou as imagens (50/50, $8.55) — ~10min desperdiçados. Inspeção AnkiConnect: 52 cards em deck PLANO "Banco de Dados" (o MERGE achatou a hierarquia que o export cria); tags duplicadas+com `::`. FIXES: (1) TAGS PLANAS — `achatarTag` em tagsDaQuestao (ankiconnect+csv) pega o último segmento e deduplica (sem `::`, sem duplicar); (2) SUBDECKS ANINHADOS — reRootDeck + nestUnderDeck em pushToAnki (troca a Matéria pelo deck do envio → Banco de Dados::Assunto::Subtópico), wired no /export + web api + checkbox "Aninhar subdecks" no ExportBar (default ON); (3) HÍBRIDO no orquestrado — runEnrich roda o orquestrador SEM imagem e faz a imagem pelo estágio determinístico PARALELO (elimina o timeout). 249 testes (245→+4) + tsc + guard SPEC-01/enrich/default-loader + web build verdes. Re-rodar p/ confirmar subdecks/tags/sem-timeout. Anterior (p-fix) -- FOLLOW-UP do feedback do usuário (run ao vivo): (1) a maioria dos cards veio SEM imagem apesar de imagem=ON — CAUSA: o prompt do agente ankinator-orchestrator manda usar imagem "com parcimônia"; FIX em código (sem gate): buildOrchestratorMessage agora, quando imagem=ON, instrui explicitamente gerar mnemonicoSvg p/ a GRANDE MAIORIA dos cards com mnemônico (a user-message sobrepõe o default soft do system-prompt do agente) — re-rodar p/ confirmar via a linha de stats (svg:N); (2) UI "Organizar decks" (DeckOrganizer.tsx) melhorada: filtro de busca + "Selecionar tudo/filtrados"/"Limpar" + contador "(N de M)" + lista mais alta (max-h-44→64), padrão do StructurePanel. 245 testes (242→+3) + tsc + guard SPEC-01/enrich/default-loader + web tsc-b/vite build verdes. Anterior (p-live) -- VALIDAÇÃO AO VIVO de #4/#5 (run real do usuário, PDF curso-230990, ANKINATOR_ORCHESTRATED=1) = PASS NO MECANISMO. #5 confirmado (log "PDF loader: langchain"). #4: o orquestrador rodou da RAIZ (bypass:false, preflight passou), retornou JSON válido SEM erro e SEM fallback, 36/36 cards fundidos por id, 36 no Anki 0 erros → `claude -p --agent` + Task headless FUNCIONA na assinatura (a maior incerteza, resolvida). 2 achados do run, ambos CORRIGIDOS nesta sessão: (a) o custo do orquestrador ($0.8164) ficava INVISÍVEL no resumo (que mostrava só geração $1.43; runOrchestratorCli não gravava usage) → novo estágio 'orquestrador' em usage.ts + recordUsage no orquestrador (job real ≈$2.25); (b) "36/36 enriquecido" só significa casou-por-id, não diz se populou mnemônico/SVG (o $0.82/9min é barato vs. o determinístico → suspeita de poucos SVGs) → mergeOrchestratorResult agora conta e loga {deck,tags,resposta,mnemonico,svg,svgDescartado}. 242 testes + tsc + guard SPEC-01 + guard-enrich + guard-default-loader verdes após os fixes. PENDENTE: julgar a QUALIDADE dos 36 cards no Anki (decks/tags/mnemônicos/SVGs) ou re-rodar 1× p/ ler a nova linha de stats — decidir se o orquestrado faz o trabalho real ou só deck/tags, e se vira default/precisa endurecer o prompt do agente. Anterior (p) -- RUMO AO DESTINO (autônomo, ultracode): #5 FECHADO + #4 CONSTRUÍDO. DESTINO #5 (loader LangChain de fato usado) FECHADO e verificado por mim sem run ao vivo: criei ~/.venvs/odl + langchain-opendataloader-pdf==2.0.0 (Python 3.13 + Java 25), fiei ANKINATOR_LANGCHAIN_PYTHON no .env; sidecar tools/odl_langchain_loader.py extrai Documents normalizados de curso-8.pdf (exit 0, valida wrapper Java), isLangchainAvailable()=true, chooseLoader(unset)=langchain → app AUTO-PREFERE LangChain (Phase 2 deixa de ser "loader inativo"); suíte verde com .env novo = zero regressão. DESTINO #4 (orquestrador por-card wired) CONSTRUÍDO (opt-in OFF, falta validação AO VIVO): novo core/specialists/orchestrator.ts — dispatcher runEnrich que com ANKINATOR_ORCHESTRATED=1 roda `claude -p --agent ankinator-orchestrator` da RAIZ do repo (--allowedTools Task Read --permission-mode dontAsk), o subagent decide por-card e delega via Task aos 4 workers, resultado fundido POR ID; FALLBACK DETERMINÍSTICO p/ enrichAll em qualquer falha; default OFF = byte-equivalente (guard SPEC-01 generation spawn INTOCADO). Flags verificados em `claude --help` (não chutados); mecânica headless pesquisada via claude-code-guide. REVISÃO ADVERSARIAL multi-lente (workflow ultracode, 4 lentes + verificação): 3 achados MEDIUM confirmados e corrigidos — (1 seg) os 4 workers não declaravam tools: → herdariam TODO o toolset (Bash/Write) ao serem delegados via Task (--allowedTools do pai NÃO restringe o filho); mitigado EM CÓDIGO por PRECONDIÇÃO workersToolRestricted (modo orquestrado se recusa a rodar e cai p/ enrichAll se os workers não estiverem tool-restritos); (2 seg) escape ANKINATOR_ORCHESTRATED_BYPASS ganhou AVISO ALTO + coberto pela precondição; (3 corr) mergeOrchestratorResult agora faz GATING por toggle (deck/tags⟸classificar, resposta⟸cardBuilder, mnemônico⟸mnemonico, svg⟸imagem) = paridade com enrichAll, e o SVG do orquestrador passa pelo MESMO boundary sanitizarSvg(fail-closed)+avaliarQualidadeSvg. PENDENTE: aplicar tools:[] nos 4 workers (.claude/agents/*.md) — BLOQUEADO pelo classificador auto-mode (edição de definição de agente = self-mod, precisa OK do usuário); até lá o modo orquestrado fica SEGURAMENTE desabilitado (preflight → fallback). 242 testes (220→+22) + tsc --noEmit + guard SPEC-01 + guard-enrich + guard-default-loader verdes; web intocado. Anterior (o-rollout) -- ROLLOUT medir-c APLICADO (escolha do usuário = "virar default ON + reforço anti-leak"). Knob ANKINATOR_CRIADA_FIDELITY agora DEFAULT ON: config.ts invertido (vazio/livre/1/on/true=livre; escape estrito ANKINATOR_CRIADA_FIDELITY=estrita|estrito|strict|0|false|off|no -> selectSystemFidelity(false) -> SYSTEM_FIDELITY byte-idêntico, guard SPEC-01 INTACTO). REFORÇO ANTI-LEAK no SYSTEM_FIDELITY_CRIADA_LIVRE (âncora item 2): MECANISMOS/SIGLAS-TÉCNICAS/EXEMPLOS/distratores externos proibidos "AINDA QUE corretos no mundo real" — fecha o leak #45 (write-ahead log) e o #44 (firmware), travado por 2 asserts novos em prompts.test.ts. 220 testes + tsc --noEmit + guard SPEC-01 + guard-enrich verdes. A/B agora inverte: run default = LIVRE; p/ a estrita use ANKINATOR_CRIADA_FIDELITY=estrita. PENDENTE (não-bloqueante): A/B p/ isolar knob×cardBuilder; medir-b (custo/perf); Fatia 2; runner orquestrado. Anterior (o-run) -- VEREDITO medir-c AO VIVO = PASS (com 1 flag mild). Run real (PDF curso-230990, CLI/assinatura, deck base "Banco de Dados", TUDO ligado): knob CONFIRMADO no log (criadaFidelidade:livre). Puxei os 63 cards do Anki via AnkiConnect (notesInfo, tag origem::criada) e li as 47 [CRIADA]. EIXO ATOMICIDADE = PASS FORTE: zero enunciado colado; discriminacao (#4/#5 internos x restritos, #9-#14, #41/#42) + atomizacao progressiva (#33-#37 quebram 2PC->fases->problema->3PC). Resolve Destino #1 / UAT 2026-06-04 ("colada crua"). EIXO ANCORA = PASS com 1 flag: verifiquei tokens suspeitos contra o PDF-fonte (pdftotext) — 8.777(2x), NTEXT/TEXT/VARCHAR, 2PC/3PC/Pre-comprometimento/preparacao/bloqueio, Round Robin/Least Connections, Dirty Read, ANSI/SPARC TODOS ancorados. UNICO vazamento = #45 "write-ahead log" (durabilidade 15x mas write-ahead/WAL/log-de-transacao 0x na fonte) — elaboracao externa CORRETA no mundo real porem fora do trecho (leak MILD, nao-perigoso). #44 "firmware" distrator benigno fora do trecho. 46-47/47 limpos, ZERO alucinacao perigosa. CAVEAT ATRIBUICAO: run com cardBuilder ON -> ganho e knob+cardBuilder combinado, NAO isolado; A/B estrita pendente p/ atribuir ao knob. CUSTO $11,60 nocional/$0,18 card/119 chamadas (imagem 44%+cardBuilder 27%+geracao 26%; geracao AINDA thinking ON ~11min; split 42->63 sem teto — alavancas l/m nao usadas). PENDENTE: decisao de ROLLOUT do usuario (default ON / endurecer prompt p/ #45 / A/B / manter opt-in). Anterior (o) -- MEDIR-C DE-RISCADO (autônomo, pré-run): verificada a fiação COMPLETA do knob ANKINATOR_CRIADA_FIDELITY (env->config.criadaFidelityLivre->api.ts:412->createProvider->ambos providers->selectSystemFidelity), que o `tipo` sobrevive ate o card (mapRawQuestoes carimba; split do card-builder herda tipo; extraida nunca divide) e que a UI tem superficie de inspecao (CardTable: filtro "Criadas (N)" + badge + pergunta/resposta + fonte p.X). FURO TAPADO (1 linha cirurgica em api.ts /generate START): loga criadaFidelidade:'livre'|'estrita' p/ a validacao ser AUTOVERIFICAVEL — sem isso um env mal-digitado produziria cards colados sem aviso (run enganoso, FAIL falso). 220 testes + tsc --noEmit + guard SPEC-01 (byte-identico) + guard-enrich verdes (zero regressao; web nao tocado). Receita medir-c REFINADA (isolar o knob): enrich TODO OFF (cardBuilder tambem atomiza e confundiria), 1-2 blocos "lista de questoes", confirmar log 'livre', A/B livre x estrita, PASS=atomica/discriminacao SEM fato fora do trecho. FALTA so o JULGAMENTO AO VIVO do usuario (run com a assinatura). Anterior (n) -- FRENTE (c) QUALIDADE [CRIADA] (escolhida pelo usuário): fidelidade DIFERENCIADA por tipo na geração, opt-in, default byte-idêntico. Nova SYSTEM_FIDELITY_CRIADA_LIVRE (prompts.ts): [EXTRAÍDA] mantém FIDELIDADE TOTAL (terminologia exata, todas as alternativas); [CRIADA] afrouxa (reformular/atomizar/DISCRIMINAR/APLICAR por inferência DIRETA, não colar o enunciado); ÂNCORA DURA p/ AMBAS (zero conhecimento externo; números/datas/nomes/gabaritos só do trecho — risco crítico do projeto). Seletor puro selectSystemFidelity(criadaFidelityLivre): default false → SYSTEM_FIDELITY byte-idêntico (SYSTEM_FIDELITY NÃO tocado, guard SPEC-01 verde). Knob config.criadaFidelityLivre (env ANKINATOR_CRIADA_FIDELITY=livre|1|true|on, default OFF) threadado igual ao precedente cliDisableThinking: config→api createProvider({...})→ProviderConfig.criadaFidelityLivre→construtor de AMBOS os providers (CLI real + API; system prompt vale p/ os dois). Endereça Destino #1 / UAT 2026-06-04 (questão criada colada crua). Server-only (sem mudança de UI). 220 testes (214→+6 em core/prompts.test.ts) + tsc --noEmit + guard SPEC-01 (spawn byte-idêntico) + guard-enrich verdes. DESENHO EXPOSTO ANTES DE CODAR (lição DEC-m aplicada): o usuário escolheu o NÍVEL Moderado (discriminação/aplicação por inferência direta) sobre Conservador, e o ROLLOUT knob opt-in default-OFF sobre novo-default-direto; rejeitada a alternativa de 2 chamadas separadas (dobraria custo da geração). FALTA AO VIVO (não-bloqueante): ligar ANKINATOR_CRIADA_FIDELITY=livre num run real, comparar a qualidade da [CRIADA] (vira pergunta atômica/de discriminação, não enunciado colado) e confirmar que NÃO alucina; decidir se vira default. Anterior (m) -- FRENTE (b) CUSTO/PERF (escolhida pelo usuário): duas alavancas determinísticas opt-in, default byte-idêntico. (1) IMAGEM SELETIVA: capturado o campo `tecnica` do mnemônico (antes descartado) em Questao.mnemonicoTecnica (server+web, SPEC-05); novo imageSelective (env ANKINATOR_IMAGE_SELECTIVE, default OFF) pula SVG p/ mnemônicos VERBAIS (denylist imageSkipTecnicas default história,rima, acento/caixa-insensível, via ANKINATOR_IMAGE_SKIP_TECNICAS) e mantém visuais (loci/acrônimo) + técnica desconhecida (lado seguro). Endereça imagem=42% do custo do run com mnemônico liberalizado p/ a maioria. (2) TETO NO SPLIT do cardBuilder: cardBuilderMaxSplit (env ANKINATOR_CARDBUILDER_MAX_SPLIT, default 0=ilimitado/byte-idêntico) trava a explosão 'criada' 1→N (run 41→58) que cascateia custo. Fiação config→api(env vazia→undefined)→EnrichOpts. 214 testes (203→+11) + tsc + guard SPEC-01 (spawn byte-idêntico) + guard-enrich + web tsc/vite verdes. FALTA AO VIVO: medir queda de custo de imagem com ANKINATOR_IMAGE_SELECTIVE=1 e contenção do split com ANKINATOR_CARDBUILDER_MAX_SPLIT=N. Anterior (l) -- KNOB thinking-OFF na geração (opt-in, default OFF): config.generationDisableThinking (env ANKINATOR_GENERATION_DISABLE_THINKING) → ProviderConfig.cliDisableThinking → CliProvider(model, disableThinking) → runClaudeCli (MAX_THINKING_TOKENS=0 no env, igual RT-imagem). Default OFF = spawn byte-idêntico (guard SPEC-01 verde). Endereça o achado da validação ao vivo (geração com thinking ON ~10min/26% custo). 203 testes + tsc + guard verdes. Anterior (VALIDAÇÃO AO VIVO) -- RUN COMPLETO no Anki (PDF curso-230990, CLI/assinatura, deck base "Banco de Dados", tudo ligado). 0 ERROS EM TUDO. Fecha pendências ao vivo das sessões g–k: (1) SELETOR DE DECK (k) ✅ — 2º envio usou o picker p/ deck existente "Banco de Dados"; (2) ORGANIZADOR DE DECKS Fatia 1 ✅ — múltiplas rodadas (merge 106 cards+26 decks apagados+26 dup; merge 44+36 apagados+30 dup), 0 erros, subdecks preservados; (3) GATE DE QUALIDADE DE IMAGEM ✅ — 3 SVGs reprovados ao vivo (sobreposição/>14 rótulos) recuperados no retry → 48/48, 0 reprovadas finais; (4) GERAÇÃO INCREMENTAL ✅ — leu 114→158 do deck base, gerou só novas (45/41); (5) PIPELINE COMPLETO ✅ — 58 cards todos os estágios → Anki. OBSERVAÇÕES não-bloqueantes: custo $10,68 NOCIONAL (assinatura cobre)/$0,18 card/111 chamadas/2,2M cache-read; quebra imagem 42%+cardBuilder 28%+geração 26%; geração AINDA com thinking ON (~10min); cardBuilder dobrou 41→58 (split sem teto). Fatia 2 do reorganizador AGORA DESBLOQUEADA. Anterior (k) -- EXPORT-DECK-PICKER: a barra "Enviar ao Anki" (ExportBar.tsx) ganhou um SELETOR EXPLÍCITO de deck existente (pedido do usuário, "recursos semelhantes ao organizar deck"). Botão 📚 (IconLayers) ao lado do input abre um dropdown FILTRÁVEL e rolável com os decks do Anki (consulta AnkiConnect deckNames via api.ankiDecks — mesma fonte do DeckOrganizer/DeckBasePicker), seleção atual com ✓, click-fora fecha (useRef + mousedown listener). Mantém input livre + datalist (criar deck novo). Desabilita o botão se Anki offline / sem decks. tsc -b + vite build (40 módulos) verdes. Anterior (j) -- ORG-DECK Fatia 1 UI COMPLETA: modal "Organizar decks" no header (web/src/components/DeckOrganizer.tsx) — configurar (decks + dedup/merge) → PRÉVIA/diff (cards movidos, decks a apagar, subdecks preservados, grupos de repetidos) → aplicar (erros parciais visíveis). + api.organizePreview/organizeApply + tipos espelho no web. tsc -b + vite build verdes (40 módulos). Fatia 1 (server+UI) COMPLETA e verificada por testes/build; FALTA validação AO VIVO no Anki real (usuário) + Fatia 2 (re-hierarquizar/tags via IA — NÃO construída ainda: evitar compor mais código destrutivo não-validado ao vivo, que é o modo de falha histórico do projeto). Anterior (i) -- ORG-DECK Fatia 1 ENDURECIDA via revisão adversarial multi-agente (workflow ultracode, 5 lentes; 7 achados, 5 confirmados e CORRIGIDOS + testes de regressão): (1) decks sobrepostos pai+subdeck marcavam nota ÚNICA como duplicata de si mesma → colapsa por noteId no previewOrganize + guarda noteId em groupDuplicates; (2) merge achatava subdecks silenciosamente e apagava a subárvore → move só cards DIRETOS (novo cardIdsDiretosDoDeck com query `-deck:"X::*"`), preserva subdecks, MergePlan.preservedWithSubdecks revela o que fica; (3) for-of sobre plano malformado estourava FORA do try (502) e quebrava o isolamento → rota valida shape (400) + applyOrganize normaliza arrays; (4) apply confiava no decksToDelete do cliente (podia apagar o ALVO) → re-deriva de plan.decks SEMPRE excluindo o target; (5) flag sem seção do plano = no-op silencioso → erro explícito. + clamp dedupThreshold a (0,1]. 203 testes + tsc verdes. Próximo (autônomo): UI da Fatia 1 (já informada pelos achados) → Fatia 2. Anterior (h) -- ORG-DECK Fatia 1 (reorganizador de decks, SERVER, determinístico): novo core/deck-organizer.ts (groupDuplicates Jaccard + planMerge puros + previewOrganize/applyOrganize em 2 fases prévia→aplicar, isolamento por-op, dedup marca tag 'duplicata' NÃO-destrutivo) + ops AnkiConnect (notesDoDeck/cardIdsDoDeck/moverCards/apagarDecks/adicionarTags) + rotas /deck/organize/preview (read-only) e /deck/organize/apply. 195 testes + tsc verdes. Falta UI + Fatia 2 (re-hierarquizar+tags via IA). Também: simplificação Karpathy do svg-quality (removido o parâmetro morto `thr`; DEFAULT_SVG_QUALITY segue editável; knob de retry mantido p/ a calibração ao vivo). Anterior (g) -- QUAL-IMG-1 (gestor de qualidade do SVG de mnemônico): novo módulo PURO svg-quality.ts (avaliarQualidadeSvg render-free, zero LLM — barra nuvem de palavras / fonte estourando / texto fora da moldura / rótulos sobrepostos, o lixo do print do usuário). Integrado no estágio 4 do enrich.ts: gerar→sanitizar→avaliar→retry 1× guiado (motivos injetados no prompt via ImageProvider.generate(...,feedback?))→descarta a imagem se persistir (fail-closed p/ "sem imagem", nunca "imagem-lixo"; card mantém o mnemônico em texto). Knobs ANKINATOR_IMAGE_QUALITY (default ON) + ANKINATOR_IMAGE_MAX_RETRY (default 1) via config→api→EnrichOpts. Endurecido o prompt mnemonic-image.md + espelho .claude/agents/ankinator-image.md ("REGRAS DURAS": ≤6 rótulos, sem frases, font ≤ H/6, sem sobreposição, dentro da moldura). 185 testes + tsc + guard SPEC-01 + PIPE-03 verdes. Falta VISUAL AO VIVO (re-rodar imagem ON, ver no Anki que não chega mais lixo). Anterior (f) -- COST-1 (custo de tokens POR ESTÁGIO nos logs: usage.ts + runner captura usage/total_cost_usd do MESMO envelope JSON, zero chamadas extras; resumo por estágio no fim do job) + INCR-1 (geração incremental contra deck base OPCIONAL — fontes AnkiConnect ao vivo E arquivo .txt/.apkg; anti-duplicata semântica no prompt + mecânica via Jaccard; "deck já completo" quando 0 novas; UI DeckBasePicker + banner). Addon AnkiWeb 2036732292 = Anki Connect Plus (soft fork, API-compatível → não precisa trocar). 168 testes + tsc + vite + guard SPEC-01 verdes. Falta AO VIVO (assinatura+Anki). Anterior (e) -- VALIDAÇÃO AO VIVO (run completo no Anki): PDF curso-230990 (86 pág, 147k chars), CLI, tudo ligado → 100 cards no Anki, 0 erros. Confirmado: CHUNK-1 (buildSections 110→groupSections 11 blocos coerentes, fratura RESOLVIDA em PDF real), CHUNK-3 (overlap, 2 chunks com contextoAnterior), PERF-2 (card-builder pool=3, 45→100 cards split, ~2min vs ~6min seq), imagem 99/99 pool=3, logging abriu tempos por chamada. PERFIL: job total ~21,7min — GARGALO = estágio imagem ~12,5min (57%); geração ~3min08s com THINKING AINDA LIGADO no CliProvider (67-91s/bloco); mnemônico batch único 100 cards ~3min; classificar 68s. Oportunidades p/ próxima rodada: (1) imagem seletiva/concorrência maior, (2) disableThinking na geração, (3) paralelizar mnemônico/classificar, (4) teto no split do card-builder. Rodada (d) -- 4 frentes: (OBS-1) logging verboso — novo logger.ts (níveis + timer hrtime + ANKINATOR_LOG_LEVEL) instrumentando api/generation/enrich/runner/chunker/odl-parse; runner.ts loga cada chamada claude (model/systemChars/userChars/outChars/ms) abrindo a caixa-preta de tempo. (UI-7) dark mode Tailwind v4 — @custom-variant dark + anti-flash + ThemeToggle (localStorage, default SO) + variantes dark: nos 7 componentes (falta conferência visual). (PERF-2) card-builder paralelizado com runPool preservando ordem (split 1→N mantém posição) + ANKINATOR_CARDBUILDER_CONCURRENCY default 3. (AGENT-2) especialista Cientista da Aprendizagem: canônico learning-scientist.md (Make It Stick/Carey/Willingham/Bjork/Dunlosky) FUNDIDO no card-builder (sem chamada extra) + standalone agent+skill; allowlist 6 nomes. 154 testes src + tsc + vite build verdes. Workflow de 8 agentes (logging + 7 dark) usado p/ as frentes 1-2. Rodada anterior (c) -- Chunking redesenhado (causa-raiz do print): OpenDataLoader marcava rótulos de lista em negrito ("• Dados numéricos:") como headings nível 6 → buildSections fraturava "1.4 Formatos de dados digitais" em 6 blocos minúsculos. Fix: novo módulo PURO section-grouping.ts (regroupSections hierárquico, boundary adaptativo default 3; sub-headings profundos dobram no pai com rótulo rebaixado p/ negrito) aplicado nos 2 loaders + revisor determinístico reviewSections (funde órfãos <40 chars, sinaliza curtos <140 via Section.aviso → badge ⚠ na UI) + overlap entre blocos (chunker overlapChars, env ANKINATOR_CHUNK_OVERLAP default 400; cauda do bloco anterior entregue como CONTEXTO "não gere questões daqui", anti-duplicata). 151 testes src (vitest contava em dobro com dist/ presente) + tsc + vite build verdes. Falta validação AO VIVO. Rodada anterior (b) -- Rodada UX+perf: (UX-5) prévia POR BLOCO na tela "Estrutura" — antes só o início do doc era visível; agora cada seção tem botão 👁 e painel com o markdown completo do bloco (servidor passou a enviar `markdown` por seção no /extract, doc já em cache). (UX-6) ESTÁGIO 4 (imagem) paralelizado: era `for` sequencial (~25s/card, o gargalo ~80% do tempo); agora pool runPool() de concorrência limitada (default 3, env ANKINATOR_IMAGE_CONCURRENCY) preservando gate q.mnemonico (D-04), isolamento por-card (D-13) e fail-closed (D-08); progresso agora usa contador de concluídas (corrige index posicional). 273 testes verdes (eram 270) + tsc + vite build verdes. ⏳ Falta validação AO VIVO: medir ganho de tempo real e confirmar que a quota aguenta concurrency=3.
progress:
  # NOTA (reconciliação C1): estas métricas contam só FASES FORMALMENTE FECHADAS POR PLANO
  # (1/2/3 = 3 de 8). NÃO medem o progresso rumo ao 🎯 Destino (~99%, no corpo abaixo + ROADMAP).
  # Fases 4/4.1/5/6/7 estão validadas AO VIVO, mas com ciclo de plano não fechado (ver ROADMAP [~]).
  total_phases: 8
  completed_phases: 3
  total_plans: 16
  completed_plans: 16
  percent: 45
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-06-03)

**Core value:** Flashcards que maximizam retenção a partir de qualquer texto de concurso, via assinatura Claude (sem custo por token).
**Current focus:** Rumo ao 🎯 Destino — das 5 metas, #1/#2/#3 fechadas; sessão (p) atacou as 2 restantes. **#5 (loader LangChain de fato usado) FECHADO** (verificado por mim sem run ao vivo: venv+pacote, sidecar extrai PDF real, auto-prefere langchain). **#4 (orquestrador por-card wired) CONSTRUÍDO** (opt-in OFF, fallback determinístico, hardened por revisão adversarial; falta validação AO VIVO + aplicar `tools:[]` nos workers — bloqueado pelo classificador, precisa OK do usuário). **Restam:** validar #4/#5 AO VIVO; aprovar o `tools:[]` dos workers; e frentes não-Destino (medir-b custo/perf, Fatia 2 destrutiva, paralelizar mnemônico/classificar). Ver ROADMAP "🎯 Destino".

## Current Position

**Pipeline completo VALIDADO AO VIVO no Anki (2026-06-05) — 0 erros.** Um run real (PDF curso-230990, CLI/assinatura, deck base "Banco de Dados", todos os estágios) produziu 58 cards no Anki, fechando as pendências de "falta validação ao vivo" das sessões g–l:
- Phase 04 (Mnemônicos + Imagem SVG): ✅ AO VIVO — mnemônicos (48/58) + SVGs (48/48) chegam no Anki. O **gate de qualidade de imagem** barrou 3 SVGs-lixo (sobreposição/>14 rótulos) e os recuperou no retry guiado → 0 reprovadas finais.
- Phase 04.1 (Estabilização de Runtime): ✅ erro do CLI visível, parse tolerante, loader langchain auto. RT-imagem confirmado (thinking-off nos 4 especialistas de enrich).
- Phase 06 (Cards Educativos Ricos): ✅ CONFIRMADO no Anki (layout rico, alternativas como lista ordenada).
- Phase 07 (parcial): mnemônico liberalizado + alternativas capturadas + 5 subagents reais em `.claude/agents/`.
- **Organizador de decks Fatia 1** (merge + dedup, fora do ROADMAP de fases): ✅ AO VIVO — múltiplas rodadas, 0 erros (106+44 cards movidos, 62 decks apagados, 56 notas "duplicata"), subdecks preservados.
- **Seletor de deck no envio** (sessão k) + **geração incremental** contra deck base: ✅ AO VIVO.
- **Knob thinking-OFF na geração** (sessão l, opt-in default OFF): entregue para endereçar o achado de custo/tempo do run (geração ~10min/26% com thinking ON).

**Achados não-bloqueantes do run:** custo $10,68 NOCIONAL (assinatura cobre)/$0,18 card/111 chamadas; quebra imagem 42% + cardBuilder 28% + geração 26%; cardBuilder dobrou 41→58 (split sem teto).

**Sessão m (2026-06-05):** frente (b) custo/perf ENTREGUE — imagem seletiva por técnica do mnemônico + teto no split do cardBuilder (opt-in, default byte-idêntico). Decisão DEC-m formalizada (manter env knob). 214 testes + guards + builds verdes. Falta só medir AO VIVO.

**Sessão n (2026-06-05):** frente (c) qualidade `[CRIADA]` ENTREGUE — fidelidade DIFERENCIADA por tipo na geração (variante `SYSTEM_FIDELITY_CRIADA_LIVRE` + seletor), via knob opt-in `ANKINATOR_CRIADA_FIDELITY=livre` (default OFF = byte-idêntico). `[CRIADA]` afrouxada (atomizar/discriminar/aplicar por inferência direta), `[EXTRAÍDA]` estrita, âncora dura p/ ambas. Desenho exposto antes de codar (Moderado + knob, escolha do usuário). 220 testes (+6) + tsc + guard SPEC-01 + guard-enrich verdes. Falta só medir AO VIVO (melhora sem alucinar).

**Sessão o (2026-06-05) — medir-c VALIDADO AO VIVO = PASS → `[CRIADA]` afrouxada vira DEFAULT ON.** Run real (PDF curso-230990, tudo ligado, `ANKINATOR_CRIADA_FIDELITY=livre` confirmado no novo log). Veredito lendo as 47 `[CRIADA]` puxadas do Anki (AnkiConnect) + cruzando fatos com o PDF-fonte: atomicidade forte (resolve Destino #1), âncora 46–47/47 ancorada, zero alucinação perigosa (único leak *mild* = #45 "write-ahead log"). Rollout (escolha do usuário): default ON + escape `=estrita` (→ `SYSTEM_FIDELITY` byte-idêntico) + reforço anti-leak no prompt (DEC-o). 220 testes + tsc + guards verdes.

**Sessão q-live2 (2026-06-06) — VALIDAÇÃO AUTÔNOMA AO VIVO = PASS (Destino #4 qualidade + p-fix2 fixes).** O usuário me liberou p/ rodar a validação eu mesmo (PDF em `/home/t316360/plottwist/material`) e deletou o loop de 15min (criou um de 2h). Driver novo `server/src/scripts/live-validate.ts` (espelha /generate+/export) com `ANKINATOR_ORCHESTRATED=1`, seções "Tipos de BD"+"ACID" → 6 cards. **Orquestrador rodou SEM fallback** (~81s, $0.213); stats `deck:6 tags:6 mnemonico:6 svg:6 de 6` (trabalho REAL, não só deck/tags); **híbrido sem timeout** (imagem via pool, 1 SVG reprovado→retry→6/6). Via AnkiConnect: **subdecks ANINHADOS** (`AnkinatorTeste::Assunto::Subtópico`) + **tags PLANAS** (sem `::`, dedup). 6 cards no deck descartável `AnkinatorTeste`. 249 testes + tsc verdes. Recomendo manter o orquestrado **opt-in** (custa mais que `enrichAll`; Destino #4 já satisfeito como capacidade). Detalhe em `.planning/TAREFAS.md`.

**Pendente (escolha do usuário):** ✅ ~~(medir-c)~~ FECHADO (PASS → default ON adotado, DEC-o). Restam: (medir-b) validar custo/perf da sessão m AO VIVO (`ANKINATOR_GENERATION_DISABLE_THINKING=1` + `ANKINATOR_IMAGE_SELECTIVE=1` + `ANKINATOR_CARDBUILDER_MAX_SPLIT=N`); (a) Fatia 2 do reorganizador (re-hierarquizar + tags — destrutiva, expor desenho antes); (d) runner ORQUESTRADO opt-in (Phase 5); (perf+) paralelizar mnemônico/classificar; (A/B opcional) isolar o knob do cardBuilder (`ANKINATOR_CRIADA_FIDELITY=estrita`). Detalhe em `.planning/TAREFAS.md`.
Last activity: 2026-06-06 - Quick task 260606-k3b: markitdown (loader PDF opt-in, sem Java) + turbovec (scaffold busca vetorial) via padrão sidecar Phase 2 — server-only, default loader/dedup INTOCADOS, suíte+tsc+guard verdes. **VALIDADO AO VIVO por mim (venvs reais)**: markitdown[pdf]==0.1.6 extrai o PDF real (166k chars) e `loadDocument`+`ANKINATOR_PDF_LOADER=markitdown` retorna LoadedDocument (numPages=1, blob plano = limitação esperada); turbovec==0.7.0 smoke/add/search + `runTurbovecSmoke()` E2E OK. **Bug real achado+corrigido ao vivo** (commit 1c1d9ea): turbovec `search` exige query 2D (batch) + `dim` múltiplo de 8 — o teste mockado não pegava (lição: mock não prova ao vivo). Venvs wired no `.env` local.

Progress (capacidades construídas / render — eixo (a) da Definição de Pronto): [█████████▍] ~99% (#1/#2/#3/#5 fechadas; #4 orquestrador VALIDADO AO VIVO mecanismo+QUALIDADE 2026-06-06 — deck:6/tags:6/mnemonico:6/svg:6 de 6, sem fallback, híbrido sem timeout — e p-fix2 #1/#2/#3 validados via AnkiConnect; resta só decidir se o opt-in orquestrado vira default)

Progress (valor — retenção do DONO estudando, eixo (b) da Definição de Pronto): [░░░░░░░░░░] ~0% — nenhum sinal de retenção medido ainda (again-rate/FSRS ou auto-teste cego D+7/D+30). O ~99% acima é só capacidades/render; este eixo é o gargalo real do Core Value. Ver ROADMAP "Definição de Pronto" + `.planning/FRAMING-REVIEW.md` P0 (DEC-q).

## Performance Metrics

> ⚠️ **Telemetria parcial, parada por volta da Phase 04** (a tabela "Detail" abaixo só vai até Phase 04 P04-03). Os números aqui (ex.: "12 planos completos") refletem o que foi cronometrado até então; o total de planos formais está no frontmatter (`completed_plans: 16`). Sessões posteriores (d–q-live3) entregaram muito além disso via sessões/quick-tasks não cronometradas. (Reconciliação C7.)

**Velocity:**

- Total plans completed: 12
- Average duration: ~6 min
- Total execution time: ~25 min

**By Phase:**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| 01 | 4/4 | ~25 min | ~6 min |
| 02 | 4 | - | - |
| 03 | 4 | - | - |

**Recent Trend:**

- Last 5 plans: 01-01 (~8 min), 01-02 (~6 min), 01-03 (~5 min), 01-04 (~6 min + checkpoint)
- Trend: estável ~6 min/plano

**Detail:**

| Plan | Duration | Tasks | Files |
|------|----------|-------|-------|
| Phase 01 P01 | 8 min | 1 | 1 |
| Phase 01 P02 | 6 | 3 tasks | 9 files |
| Phase 01 P03 | 5 min | 2 tasks | 7 files |
| Phase 01 P04 (a/b/c) | ~6 min | 3 auto tasks (+1 checkpoint pendente) | 2 files |
| Phase 03 P01 | 10min | 3 tasks | 6 files |
| Phase 03 P02 | 8min | 3 tasks | 4 files |
| Phase 03 P03 | 5min | 4 tasks | 4 files |
| Phase 04 P01 | 5 min | 2 tasks | 6 files |
| Phase 04 P04-03 | 8 min | 3 tasks | 6 files |

## Accumulated Context

### Decisions

Decisões completas em PROJECT.md (Key Decisions). Recentes:

- **[DEC-p 2026-06-05] Destino #4 (orquestrador) construído como caminho OPT-IN safe-by-construction; #5 (langchain) ativado.** Sessão autônoma (ultracode) atacou as 2 metas restantes do Destino. (1) #5: ativei o loader LangChain (venv + pacote + env) e PROVEI E2E (sidecar extrai PDF real; auto-select) — fecha sem run ao vivo (verificável por mim). (2) #4: o wiring do orquestrador é um caminho SEPARADO (não toca o generation spawn / guard SPEC-01), gated por `ANKINATOR_ORCHESTRATED` (default OFF = byte-equivalente), com FALLBACK determinístico p/ `enrichAll` em qualquer falha. Flags do `claude -p --agent` verificados em `claude --help` (não chutados). (3) Decisão de SEGURANÇA pós-revisão-adversarial: como `--allowedTools` do pai NÃO restringe subagents delegados via Task e os 4 workers não declaravam `tools:`, adicionei uma PRECONDIÇÃO em código (`workersToolRestricted`) que DESABILITA o modo orquestrado (fallback) enquanto os workers não estiverem tool-restritos — torna o privilégio safe-by-construction independentemente de o usuário lembrar de endurecer os agentes. (4) O endurecimento final (`tools: []` nos 4 `.claude/agents/*.md`) ficou PENDENTE de aprovação do usuário: o classificador auto-mode bloqueou a edição de definição de agente como self-modification não-direcionada — respeitado (não contornado). Lição reusável: **editar `.claude/agents/*.md` é self-mod gated → exige direção explícita do usuário**; mecânica headless de subagent é `claude -p --agent <nome>` da raiz + `--allowedTools` + `--permission-mode dontAsk`, e a restrição de tools de um filho vem SÓ do frontmatter dele.
- **[DEC-o 2026-06-05] `[CRIADA]` afrouxada ADOTADA como DEFAULT ON após medir-c PASS AO VIVO.** A validação que DEC-n exigia antes de virar default passou: 47 `[CRIADA]` lidas do Anki via AnkiConnect (`notesInfo`, tag `origem::criada`) — atomicidade forte (discriminação/atomização progressiva, zero enunciado colado → resolve Destino #1 / UAT 2026-06-04) e âncora verificada token-a-token contra o PDF-fonte (`pdftotext`): 46–47/47 ancorados, ZERO alucinação perigosa; único leak *mild* não-perigoso = #45 "write-ahead log" (correto-no-mundo mas fora do trecho). Decisão do usuário = **virar default ON + reforço anti-leak**: (1) `config.ts` default INVERTIDO (vazio/`livre`/`1`/`on`=livre; escape `ANKINATOR_CRIADA_FIDELITY=estrita|strict|0|false|off|no` → `SYSTEM_FIDELITY` byte-idêntico, guard SPEC-01 intacto); (2) reforço no `SYSTEM_FIDELITY_CRIADA_LIVRE` (âncora item 2) proibindo mecanismos/siglas/exemplos/distratores externos "ainda que corretos no mundo real" (fecha #45/#44), travado por 2 asserts. 220 testes + tsc + guard SPEC-01 + guard-enrich verdes. **Caveat:** o reforço é prompt → efeito real só se prova no próximo run (princípio: mockado não prova ausência de alucinação); A/B (`=estrita`) opcional p/ isolar do cardBuilder (o run de validação teve cardBuilder ON → ganho combinado). Método novo reusável: **puxar os cards exportados do Anki via AnkiConnect e cruzar fatos contra o PDF-fonte** fecha a validação de fidelidade sem depender de inspeção manual.
- **[DEC-n 2026-06-05] Qualidade `[CRIADA]`: nível Moderado + rollout knob opt-in.** Desenho exposto ao usuário ANTES de codar (lição DEC-m). (1) NÍVEL = **Moderado** (afrouxa `[CRIADA]` p/ reformular/atomizar/discriminar/aplicar por inferência DIRETA do trecho) sobre Conservador — alinhado ao Destino #1 + cientista-da-aprendizagem, com a âncora dura (zero conhecimento externo) contendo o risco crítico. (2) ROLLOUT = **knob opt-in `ANKINATOR_CRIADA_FIDELITY` default-OFF** (byte-idêntico) sobre "novo default direto" — consistente com o padrão de env-knobs (sessões l/m) e reversível p/ o risco crítico; valida-se AO VIVO (teste mockado não prova ausência de alucinação) antes de virar default. Rejeitada a alternativa "2 chamadas separadas" (dobraria o custo da geração; contra Karpathy #2 e o foco de custo das sessões l/m).
- **[DEC-m 2026-06-05] Manter o env knob `ANKINATOR_IMAGE_SKIP_TECNICAS`** (não enxugar). Modo RÁPIDO da skill tomada-de-decisao (reversível, baixo impacto, autoridade delegada pelo usuário). Razão: consistência com os ~10 env-knobs de custo do projeto (Karpathy #3 > #2 aqui, pois remover criaria assimetria) + reversibilidade assimétrica (manter-e-errar é trivial/invisível; remover-e-errar gera re-trabalho) + o usuário tuna knobs após medir. Nenhuma mudança de código (a impl. da sessão m já é a recomendada). Lição de processo: expor o DESENHO (denylist vs allowlist; default keep-on-unknown) antes de codar quando há >1 interpretação (Karpathy #1).
- Bootstrap: Loader LangChain = sidecar Python opt-in (Node segue default).
- Bootstrap: Especialistas = fonte única `.md` → Skill + estágio no app (híbrido).
- Bootstrap: Imagem de mnemônico = SVG gerado pelo Claude + interface `ImageProvider`.
- Bootstrap: Cadeia = modo "educativo" opcional (toggles + defaults), fase `enrichAll()`.
- [Phase 01 P01]: `npm ci` no ROOT do workspace (npm-workspaces, lockfile único hoisted) alinha tsc a 5.9.3 — não per-workspace; baseline simbólico do CliProvider gravado (regressão SPEC-01 coberta por assertion determinística CLI-free + checkpoint hard-block no Plano 04).
- [Phase 01 P02]: runClaudeCli + buildSpawnArgs (helper PURO) extraídos do CliProvider; delegação com SYSTEM_FIDELITY sem regressão; não-regressão SPEC-01 provada CLI-free (args/cwd/env/sem-vazamento-de-userMessage).
- [Phase 01 P02]: loadPrompt resolve .md SEMPRE de src/ via heurística dist→src (não __dirname, não dist/) com allowlist+cache; validado cross-mode. Caveat A1: deploy precisa enviar src/ com os .md (tsc não copia); dist-only exigiria Plano B.
- [Phase 01 P03]: SPEC-05 — Questao +4 campos opcionais (deck/tags/mnemonico/mnemonicoSvg) com assinatura IDÊNTICA em server (core/types.ts) e web (src/types.ts), no mesmo commit (Pitfall 4 tipo duplicado); fluxo intacto (mapRawQuestoes/exporters não tocados), ambos os builds verdes.
- [Phase 01 P03]: SPEC-03 — 5 SKILL.md em .claude/skills/<nome>/ referenciam por path os .md canônicos (progressive disclosure, sem cópia divergente — D-06). Nomes de pasta = nomes canônicos do PLAN/<verify> (anki-orchestrator, deck-classifier, card-builder, mnemonic, mnemonic-image). Reiniciar a sessão do Claude Code p/ as Skills aparecerem no menu /.
- [Phase 01 P04]: SPEC-04 — ImageProvider (interface) + SvgClaudeImageProvider (svg-claude; loadPrompt('mnemonic-image') + runClaudeCli, usa mnemonic E context) + createImageProvider() default svg-claude (D-08/09/10). 100% no server; sem raster; TODO(IMG-02) sanitização e TODO(v2) raster por env; NÃO plugado no pipeline. Build server verde.
- [Phase 01 P04]: Guard SPEC-01 AUTOMATIZADO e CLI-free — smoke-runner --assert-args (flag adicionada, Rule 3) sai 0 sem spawnar o claude; args/cwd/env byte-idênticos ao CliProvider.invoke() pré-refator (Pitfall 2). loadPrompt validado cross-mode tsx(src)+node(dist) — Pitfall 1 fechado (lengths idênticos 2360/1955/2036/1653/1727).
- [Phase 01 P04]: Task 4 é HARD-BLOCK humano e ficou CHECKPOINT-PENDING — baseline foi simbólico (sem PDF). A regressão E2E (SPEC-01 geração real + PIPE-03) exige um PDF fornecido pelo usuário; o executor não-interativo não fabrica sign-off.
- [Phase ?]: Phase 03 P02: enrichAll wired behind deveRodarEnrich gate in /generate .then() — await before done (PIPE-01/PIPE-03 closed)
- [Phase ?]: Phase 03 P02: CSV temDeck gate — byte-identity when no q.deck; #deck column:5 only when some card has q.deck (DECK-01)
- [Phase ?]: Phase 03 P02: ankiconnect subdeck routing — createDeck per distinct q.deck; deckName q.deck ?? opts.deck per-nota (DECK-01/DECK-02)
- [Phase ?]: Phase 03 P03: Enrich progress renderizado DENTRO do passo generating sem 5o step (D-16); defaults via !== false / === true para toggles ON/OFF
- [Phase 04 P01]: sanitizarSvg fail-closed returns null if output not starting with <svg — D-08/Pitfall 4; isomorphic-dompurify@3.15.0 installed
- [Phase 04 P01]: ALLOWED_TAGS geometric-only allowlist (15 tags); style absent from ALLOWED_ATTR (F-09 depends on this) — D-06/D-07; 13 adversarial fixtures pass
- [Phase 04 P01]: mnemonic.md ## Saída rewritten to JSON batch format — prerequisite for Plan 02 parser — Pitfall 5 closed
- [Phase 04 P01]: mnemonic-image.md: no id/version on <svg> root (Anki 25.02.x strips them — A1); SVG must start with <svg and end with </svg>
- [Phase ?]: versoHtml tornada export de forma aditiva para teste positivo IMG-03
- [Phase ?]: Phase 04 P03: D-12 defaults mnemonico=true imagem=false em enrichOpts do /generate
- [Phase ?]: Phase 04 P03: SVG embed cru em csv.ts e ankiconnect.ts — NUNCA escapeHtml no mnemonicoSvg (T-04-11)
- [RT-fidelidade 2026-06-04]: nós `list` do OpenDataLoader guardam itens em `"list items"` (NÃO `content`/`kids`). `flatten()` em `core/odl-parse.ts` ignorava esse campo → descartava 100% das listas (alternativas A-E, róis) dos chunks. Fix: branch `type==='list'`+`renderList()`. Lição: `doc.markdown` cru ≠ `sections`/chunks (modelo vê os chunks). Cobertura: `odl-parse.test.ts`.

### Pending Todos

None yet.

### Blockers/Concerns

- Quota da assinatura: cadeia por-card multiplica chamadas ao CLI → exige batching/seletividade (tratado na Phase 5).
- Tipo `Questao` duplicado (server+web) → estender com cuidado (Phase 1, SPEC-05).

### Quick Tasks Completed

| # | Description | Date | Commit | Directory |
|---|-------------|------|--------|-----------|
| 260606-k3b | adicionar markitdown e turbovec a esse projeto | 2026-06-06 | 7469be3 | [260606-k3b-adicionar-markitdown-e-turbovec-a-esse-p](./quick/260606-k3b-adicionar-markitdown-e-turbovec-a-esse-p/) |

## Deferred Items

| Category | Item | Status | Deferred At |
|----------|------|--------|-------------|
| Fase futura | **Dedup semântico por embeddings** — substituir/complementar o Jaccard usando o scaffold turbovec (já pronto, provado por smoke) + fonte de embeddings **LOCAL grátis** (ex.: `sentence-transformers` multilíngue PT via sidecar; API paga REJEITADA por R2). Operação destrutiva no Anki exige **consentimento explícito** (R3). | Deferido (scaffold pronto, dedup NÃO construído) | quick-260606-k3b (2026-06-06) |

## Session Continuity

Stopped-p-fix2 (2026-06-06): **3 fixes pós-run#2 (escolhas do usuário via AskUserQuestion) — TUDO NO WORKING TREE, NADA COMMITADO.** Run#2 do usuário: SVGs apareceram, MAS o orquestrador ESTOUROU o timeout de 10min (forçar ~50 imagens num call único) → fallback determinístico gerou as imagens (50/50, $8.55), ~10min desperdiçados. Inspeção via AnkiConnect: os 52 cards ficaram em deck PLANO "Banco de Dados" (o MERGE do organizador achata a hierarquia que o export CRIA) + tags duplicadas e com `::`. **Fixes (249 testes + tsc + guard SPEC-01/enrich/default-loader + web tsc-b/vite verdes):** (1) **TAGS PLANAS** — `achatarTag` em `tagsDaQuestao` (`core/exporters/ankiconnect.ts`+`csv.ts`): pega o último segmento (`banca::fgv`→`fgv`) e deduplica → sem `::`, sem duplicar; (2) **SUBDECKS ANINHADOS sob o deck do envio** — `reRootDeck`+`nestUnderDeck` em `pushToAnki` (troca a Matéria pelo deck escolhido → `Banco de Dados::Assunto::Subtópico`, cabeçalho+deckName consistentes); wired `api.ts /export/ankiconnect` + web `api.pushToAnki` + **checkbox "Aninhar subdecks sob este deck" no `ExportBar.tsx` (DEFAULT ON)**; (3) **HÍBRIDO no orquestrado** — `runEnrich` (`orchestrator.ts`) roda o orquestrador SEM imagem e faz a IMAGEM pelo estágio determinístico PARALELO (elimina o timeout de 10min). Antes (p-fix): UI "Organizar decks" (`DeckOrganizer.tsx`) ganhou filtro de busca + "Selecionar tudo/Limpar"; e a diretiva de imagem no `buildOrchestratorMessage`. **▶ RE-RODAR p/ confirmar AO VIVO:** subdecks `Banco de Dados::…`, tags planas, orquestrado+imagem SEM timeout. **Avisos:** (a) NÃO usar o "Unir decks (merge)" nestes cards (achata) — só ENVIAR com "Aninhar subdecks" ON; (b) os 52 cards já no Anki ficaram PLANOS → apagar+re-exportar OU construir a **Fatia 2 (re-hierarquizar in-place, destrutiva, já oferecida ao usuário)**. Anterior:

Stopped-p: **Rumo ao Destino (autônomo, ultracode): #5 FECHADO + #4 CONSTRUÍDO + hardened por revisão adversarial.** (1) **#5 (loader LangChain de fato usado) — FECHADO e verificado por mim sem run ao vivo:** `~/.venvs/odl` + `langchain-opendataloader-pdf==2.0.0` (Python 3.13 + Java 25), `ANKINATOR_LANGCHAIN_PYTHON` no `.env`; o sidecar extrai Documents normalizados de `curso-8.pdf` (exit 0), `isLangchainAvailable()=true`, `chooseLoader(unset)=langchain` → app auto-prefere LangChain; suíte verde com `.env` novo. (2) **#4 (orquestrador por-card wired) — CONSTRUÍDO, opt-in OFF, falta validação AO VIVO:** `core/specialists/orchestrator.ts` (dispatcher `runEnrich` → `claude -p --agent ankinator-orchestrator` da raiz + Task; merge por id; FALLBACK determinístico p/ `enrichAll`; default OFF byte-equivalente, guard SPEC-01 generation spawn INTOCADO). Flags verificados em `claude --help`. (3) **Revisão adversarial (workflow 4 lentes + verificação): 3 MEDIUM corrigidos** — (seg) PRECONDIÇÃO `workersToolRestricted` desabilita o modo se os workers não tiverem `tools:` (pois `--allowedTools` do pai não restringe filhos via Task); (seg) aviso ALTO no `ANKINATOR_ORCHESTRATED_BYPASS`; (corr) gating por toggle no `mergeOrchestratorResult` (paridade `enrichAll`) + SVG pelo mesmo boundary sanitizar/qualidade. **242 testes (+22) + tsc + guard SPEC-01 + guard-enrich + guard-default-loader verdes; web intocado.** **APLICADO ao fim da sessão (usuário aprovou):** `tools: []` + `disallowedTools` nos 4 workers `.claude/agents/*.md` → `workersToolRestricted()` `{ok:true}`, modo orquestrado destravado E seguro. **VALIDADO AO VIVO (run do usuário) = PASS NO MECANISMO:** orquestrador rodou da raiz, 36/36 fundidos por id, sem fallback, 36 no Anki 0 erros → `claude -p --agent`+Task headless funciona na assinatura. 2 achados do run CORRIGIDOS (custo do orquestrador agora contabilizado em `usage.ts` estágio 'orquestrador'; merge agora loga stats por-campo). **242 testes + tsc + guards verdes. PENDENTE (julgamento do usuário):** qualidade dos 36 cards no Anki (ou re-rodar p/ ler a linha de stats) — o orquestrado faz o trabalho real (mnemônico/SVG) ou só deck/tags? vira default? endurecer o prompt do agente p/ mais SVGs? Anterior (o):
Stopped-o: **medir-c VALIDADO AO VIVO = PASS → `[CRIADA]` afrouxada ADOTADA como DEFAULT ON + reforço anti-leak. Sessão FECHADA (nada pendente em código).** Sequência: (1) **DE-RISCO autônomo pré-run** — verifiquei a fiação completa do knob (env→`config.criadaFidelityLivre`→`api.ts:412`→AMBOS providers→`selectSystemFidelity`), que `tipo` sobrevive até o card (`CardTable` tem filtro "Criadas (N)"+badge+pergunta/resposta+fonte `p.X`), e tapei o furo do log (`/generate` START agora loga `criadaFidelidade:'livre'|'estrita'` — sem isso env mal-digitado daria FAIL falso). (2) **RUN REAL do usuário** (PDF `curso-230990`, CLI/assinatura, deck "Banco de Dados", TUDO ligado) — knob confirmado no log (`criadaFidelidade:livre`). (3) **VEREDITO** — puxei os 63 cards do Anki via AnkiConnect (`notesInfo`, tag `origem::criada`), li as 47 `[CRIADA]`: **ATOMICIDADE = PASS forte** (zero enunciado colado; discriminação #4/#5,#9-#14,#41/#42 + atomização progressiva 2PC/3PC #33-#37 — resolve Destino #1 / UAT 2026-06-04); **ÂNCORA = PASS com 1 flag** — cruzei tokens suspeitos contra o PDF-fonte (`pdftotext`): `8.777`, `NTEXT/TEXT/VARCHAR`, `2PC/3PC/Pré-comprometimento`, `Round Robin/Least Connections`, `Dirty Read`, `ANSI/SPARC` TODOS ancorados; ÚNICO leak = #45 "write-ahead log" (fora do trecho, correto-no-mundo, *mild*/não-perigoso); #44 "firmware" distrator benigno. **46-47/47 limpos, ZERO alucinação perigosa.** (4) **ROLLOUT (escolha do usuário = "virar default ON + reforço anti-leak")** — `config.ts` default INVERTIDO p/ ON (vazio/`livre`/`1`/`on`=livre; **escape** `ANKINATOR_CRIADA_FIDELITY=estrita|strict|0|false|off|no` → `selectSystemFidelity(false)` → `SYSTEM_FIDELITY` **byte-idêntico**, guard SPEC-01 intacto) + **REFORÇO** no `SYSTEM_FIDELITY_CRIADA_LIVRE` (âncora item 2: MECANISMOS/SIGLAS-TÉCNICAS/EXEMPLOS/distratores externos proibidos "AINDA QUE corretos no mundo real" → fecha #45/#44), travado por 2 asserts em `prompts.test.ts`. Arquivos: `config.ts`, `core/prompts.ts` (+`prompts.test.ts`), `api.ts` (log). **220 testes + tsc --noEmit + guard SPEC-01 + guard-enrich verdes.** **Caveats não-bloqueantes:** o reforço é prompt → efeito real só se prova no PRÓXIMO run; A/B (`=estrita`) opcional p/ isolar knob×cardBuilder. Custo do run $11,60 nocional / $0,18 card / 119 chamadas (imagem 44% + cardBuilder 27% + geração 26%; geração AINDA thinking ON ~11min; split 42→63 sem teto → alavancas l/m disponíveis = medir-b). Anterior (n):
Last session: 2026-06-05 (n) (Frente (c) QUALIDADE [CRIADA]: fidelidade DIFERENCIADA por tipo na geração — variante SYSTEM_FIDELITY_CRIADA_LIVRE + seletor, knob opt-in ANKINATOR_CRIADA_FIDELITY=livre default-OFF byte-idêntico. [CRIADA] afrouxada/[EXTRAÍDA] estrita/âncora dura p/ ambas. 220 testes (+6) + tsc + guard SPEC-01 + guard-enrich verdes. Desenho exposto antes de codar. Falta só medir AO VIVO.) Anterior (m): Frente (b) custo/perf + DEC-m.
Stopped-n: **Frente (c) Qualidade `[CRIADA]` entregue + desenho exposto antes de codar.** O `SYSTEM_FIDELITY` único prendia `[EXTRAÍDA]` e `[CRIADA]` nas mesmas "Regras invioláveis" → `[CRIADA]` saía colada crua (reprovada no UAT, contra o Destino #1). Fix: nova `SYSTEM_FIDELITY_CRIADA_LIVRE` (contratos por tipo numa só chamada) + seletor puro `selectSystemFidelity(criadaFidelityLivre)` (default `false` → `SYSTEM_FIDELITY` byte-idêntico; `SYSTEM_FIDELITY` NÃO tocado → guard SPEC-01 intacto). Knob `config.criadaFidelityLivre` (env `ANKINATOR_CRIADA_FIDELITY=livre|1|true|on`, default OFF) threadado igual ao `cliDisableThinking`: `config`→`api.ts createProvider({...})`→`ProviderConfig`→AMBOS providers (CLI+API). `[EXTRAÍDA]` fidelidade total; `[CRIADA]` afrouxada (reformular/atomizar/discriminar/aplicar por inferência DIRETA, não colar enunciado); ÂNCORA DURA p/ ambas (zero conhecimento externo; números/datas/nomes/gabaritos só do trecho). Arquivos: `core/prompts.ts` (+`core/prompts.test.ts` 6 testes), `config.ts`, `core/providers/{index,cli-provider,api-provider}.ts`, `api.ts`. Server-only (sem UI). **220 testes (214→+6) + tsc --noEmit + guard SPEC-01 + guard-enrich verdes.** Decisões DEC-n: nível Moderado + rollout knob (escolha do usuário). **FALTA só AO VIVO (não-bloqueante):** `ANKINATOR_CRIADA_FIDELITY=livre` num run real → confirmar [CRIADA] vira pergunta atômica/de discriminação SEM alucinar; decidir se vira default. Anterior (m):
Stopped-m: **Frente (b) entregue + decisão formalizada.** Duas alavancas de custo determinísticas opt-in (default byte-idêntico): (1) imagem seletiva — capturado `tecnica` do mnemônico em `Questao.mnemonicoTecnica` (server+web, SPEC-05); `imageSelective` (env `ANKINATOR_IMAGE_SELECTIVE`, default OFF) pula SVG p/ técnicas verbais (denylist `história,rima` via `ANKINATOR_IMAGE_SKIP_TECNICAS`, acento/caixa-insensível), mantém visuais (loci/acrônimo) + desconhecida; (2) teto no split — `cardBuilderMaxSplit` (env `ANKINATOR_CARDBUILDER_MAX_SPLIT`, default 0=ilimitado) trava a explosão 'criada' 1→N. Fiação config→api(env vazia→undefined)→EnrichOpts. **Decisão DEC-m:** manter o env knob das skip-técnicas (ver Decisions acima). **214 testes + tsc + guard SPEC-01 (byte-idêntico) + guard-enrich + web tsc/vite verdes.** **FALTA só AO VIVO (não-bloqueante):** medir queda de custo de imagem com `ANKINATOR_IMAGE_SELECTIVE=1` e contenção do split com `ANKINATOR_CARDBUILDER_MAX_SPLIT=N`; decidir se viram default. **Próxima frente = escolha do usuário** (medir-b / a-Fatia2 / c-qualidade-[CRIADA] / d-orquestrador / perf+). Anterior (k):
Stopped-k: **Seletor de deck na barra de envio** (pedido do usuário a partir de print da ExportBar): antes o campo "Nome do deck" só tinha datalist (autocomplete escondido); agora há um botão 📚 que abre um dropdown filtrável/rolável com os decks consultados ao Anki (api.ankiDecks → AnkiConnect deckNames, igual ao DeckOrganizer), seleção atual marcada com ✓, fecha ao clicar fora. Input livre preservado p/ criar deck novo. Arquivo único: `web/src/components/ExportBar.tsx`. tsc -b + vite build (40 módulos) verdes. **Falta validação VISUAL do usuário** (abrir app, ver o dropdown listar os decks reais). Anterior (j):
Stopped-j: **Reorganizador de decks Fatia 1 (merge+repetidos) COMPLETA e ENDURECIDA.** Server (`core/deck-organizer.ts` + ops AnkiConnect: notesDoDeck/cardIdsDoDeck/cardIdsDiretosDoDeck/moverCards/apagarDecks/adicionarTags) com 2 fases prévia→aplicar; dedup marca tag `duplicata` (não-destrutivo); merge move só cards DIRETOS (preserva subdecks). Revisão adversarial multi-agente (workflow ultracode, 5 lentes) achou 7 / confirmou 5 bugs de segurança — TODOS corrigidos + travados por testes (`deck-organizer.apply.test.ts`, AnkiConnect mockado): nota única não vira duplicata de si (decks sobrepostos), merge não achata/apaga subdecks, plano malformado → 400 (não 502), apply NUNCA apaga o target, flag sem seção → erro explícito, clamp do dedupThreshold. UI = modal "Organizar decks" no header (configurar→prévia/diff→aplicar; mostra subdecks preservados + erros parciais; `web/src/components/DeckOrganizer.tsx`). Também: gate de imagem simplificado (removido o param morto `thr`). **203 testes + tsc + web build verdes.** **FALTA AO VIVO (usuário):** (1) validar o gate de imagem (imagem ON → Anki sem lixo); (2) validar o reorganizador (header "Organizar decks" → dedup primeiro, depois merge). **Fatia 2 (re-hierarquizar+tags via IA) propositalmente NÃO construída** — evitar compor código destrutivo não-validado ao vivo (modo de falha histórico). Anterior (g):
Stopped-g: **Gestor de qualidade de imagem implementado e provado em build/teste.** Causa-raiz do SVG-lixo (print): o estágio imagem só validava SEGURANÇA do SVG, nunca QUALIDADE visual → texto sobreposto/fonte gigante passava pro Anki. Fix: módulo PURO `svg-quality.ts` (`avaliarQualidadeSvg`, render-free) + integração no estágio 4 (gerar→sanitizar→avaliar→retry guiado 1×→descarta se persistir; fail-closed p/ "sem imagem") + knobs `ANKINATOR_IMAGE_QUALITY`/`ANKINATOR_IMAGE_MAX_RETRY` + endurecimento do prompt mnemonic-image (REGRAS DURAS espelhadas no agent). 185 testes + tsc --noEmit + guard SPEC-01 + PIPE-03 verdes. **Falta VISUAL AO VIVO**: re-rodar geração com imagem ON e confirmar no Anki que o SVG-lixo não chega mais (e que cards bons mantêm a imagem). **Parte B ENFILEIRADA (escopo CONFIRMADO pelo usuário 2026-06-04, fazer DEPOIS da validação visual):** "organizador de decks do Anki" com os 4 escopos — (1) **re-hierarquizar** (reagrupar em Matéria::Assunto::Subtópico via o classificador existente), (2) **dedupe + mesclar** (achar quase-duplicatas entre decks, reusar Jaccard do INCR-1), (3) **mover cards do Ankinator** (rotear o que o app gera para o subdeck certo, não jogar tudo em "Ankinator"), (4) **padronizar tags** (normalizar/renomear tags inconsistentes). Já existe: AnkiConnect (escrita: addNotes/createDeck/deckName por-nota), existing-deck.ts (leitura ao vivo), ankinator-deck-classifier. FALTA o lado de REORGANIZAÇÃO no AnkiConnect: `changeDeck`, `deleteDecks`, renomear (`deckNames`+mover), `findNotes`/`notesInfo` para dedupe, `addTags`/`removeTags`/`replaceTags`. Próximo passo do usuário ANTES disso: validar o gate de imagem ao vivo (re-rodar imagem ON, ver no Anki que o lixo sumiu). Anterior (f):
Stopped-f: **COST-1 + INCR-1 implementados e provados em build/teste.** (COST-1) `usage.ts` + `runner` capturam tokens/custo do envelope JSON (de graça) e o `/generate` fecha o job com resumo de custo POR ESTÁGIO — agora há a 3ª dimensão (custo) ao lado de tempo. (INCR-1) deck base OPCIONAL: lê deck existente (AnkiConnect ao vivo OU .txt/.apkg), injeta fronts como anti-duplicata no prompt, filtra quase-duplicatas (Jaccard) antes do enrich, e responde "deck já completo" quando 0 novas; UI `DeckBasePicker` na tela Estrutura + banner na revisão. Sem deck → fluxo idêntico (PIPE-03 ok). Addon 2036732292 = Anki Connect Plus (API-compatível, não precisa trocar). 168 testes + tsc + vite build + guard SPEC-01 verdes. **Falta AO VIVO** (assinatura + Anki aberto): conferir custo/estágio num run real; rodar PDF já estudado contra o próprio deck (só novas; "deck completo"; ler .apkg real). Anterior (e):
Stopped at: **Pipeline novo PROVADO AO VIVO de ponta a ponta** — 100 cards no Anki a partir de PDF real, com agrupamento hierárquico (110→11 blocos), overlap, card-builder paralelo (45→100) e cientista-aprendizagem fundido. Logging expôs o perfil de tempo: **gargalo = imagem (~12,5min de ~21,7min totais)**; geração ainda com thinking ON; mnemônico/classificar são chamadas únicas grandes. **Próximo passo recomendado: rodada de PERFORMANCE** (ver "🎯 Oportunidades" no TAREFAS.md — imagem seletiva/concorrência, disableThinking na geração, paralelizar mnemônico). Rodada anterior (d):
Stopped-d: **OBS-1/UI-7/PERF-2/AGENT-2 implementados e provados em build/teste** — logging verboso (logger.ts + runner.ts expõe tempo por chamada claude), dark mode Tailwind v4 (toggle no header), card-builder paralelizado (runPool, ordem preservada, ANKINATOR_CARDBUILDER_CONCURRENCY) e especialista Cientista da Aprendizagem fundido no card-builder + standalone agent/skill. 154 testes src + tsc + vite build verdes. **Falta AO VIVO**: conferir o tema escuro visualmente; medir ganho do card-builder paralelo; avaliar a qualidade dos cards com os princípios de retenção. Rodada anterior (c):
Stopped-c: **CHUNK-1/2/3 implementados e provados em build/teste** — causa-raiz da fratura de blocos corrigida (sub-headings de lista profundos dobram no bloco pai), revisor determinístico (funde órfãos + sinaliza curtos via `Section.aviso`) e overlap entre blocos (contexto anti-duplicata) adicionados. Módulo PURO `core/section-grouping.ts` (13 testes) compartilhado pelos 2 loaders. 151 testes src + tsc + vite build verdes. **Falta validação AO VIVO**: re-extrair PDF → ver "1.4" como 1 bloco coerente na tela "Estrutura" + conferir que overlap não duplica questões. Rodada anterior (b):
Stopped-anterior: **UX-5 + UX-6 implementados e provados em build/teste** — (UX-5) tela "Estrutura" agora tem prévia por bloco selecionado (botão 👁 por seção + painel com markdown completo do bloco; `/extract` envia `markdown` por seção). (UX-6) ESTÁGIO 4 do `enrichAll` paralelizado via `runPool` (concorrência limitada, default 3 / `ANKINATOR_IMAGE_CONCURRENCY`), D-04/D-13/D-08 preservados, progresso por contador de concluídas. 273 testes + tsc + vite build verdes. **Falta validação AO VIVO do usuário** (ganho de tempo real + quota aguenta concurrency=3). Sessão anterior: RT-fidelidade (listas) corrigido e RT-imagem confirmado no Anki.
Resume file: `.planning/TAREFAS.md` → ler "▶ ESTADO ATUAL — RETOMAR AQUI (... fim da sessão p)" no topo (inclui FOLLOW-UP + FOLLOW-UP 2 com os 3 fixes). ⚠️ Mudanças NÃO commitadas (working tree) — rode `git status` p/ ver o escopo (orchestrator.ts/test novos; ankiconnect/csv/api/config/enrich/usage + web api/ExportBar/DeckOrganizer + .claude/agents/*.md + .planning/*).
Próximo passo: **3 fixes prontos no working tree (NÃO commitados; ver Stopped-p-fix2). 249 testes + tsc + guards + web build verdes.** ▶ **AÇÃO IMEDIATA — RE-RODAR p/ confirmar AO VIVO:** suba `ankinator-app/` (2 terminais: `npm run dev:server` + `npm run dev:web` → http://localhost:5173), gere de um PDF e **envie ao Anki com o checkbox "Aninhar subdecks sob este deck" ON (default)** — confirme (a) cards em `Banco de Dados::Assunto::Subtópico` (NÃO use o "Unir decks/merge", que achata), (b) tags PLANAS (sem `::`, sem duplicar), (c) com `ANKINATOR_ORCHESTRATED=1` o orquestrado termina SEM timeout e a imagem vem no estágio determinístico paralelo logo após. **Pendências abertas:** (1) os 52 cards já importados ficaram PLANOS → apagar+re-exportar OU construir a **Fatia 2 (re-hierarquizar in-place, DESTRUTIVA, DEC-m: expor desenho antes — já oferecida ao usuário)**; (2) decidir se o orquestrado vira default / endurecer o prompt; (3) frentes não-Destino: **(medir-b)** custo/perf (`ANKINATOR_GENERATION_DISABLE_THINKING=1` + `ANKINATOR_IMAGE_SELECTIVE=1` + `ANKINATOR_CARDBUILDER_MAX_SPLIT=N`), **(perf+)** paralelizar mnemônico/classificar, **(A/B)** isolar o knob do cardBuilder (`ANKINATOR_CRIADA_FIDELITY=estrita`).
